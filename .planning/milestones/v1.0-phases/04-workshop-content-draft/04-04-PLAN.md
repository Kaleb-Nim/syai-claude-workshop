---
phase: 04-workshop-content-draft
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/03-commands-skills-plugins.md
autonomous: true
requirements: [CONT-04]
must_haves:
  truths:
    - "D-08: chapter follows shape title → why care → mental model → live walkthrough → exercise → recap"
    - "D-17: first slide uses `layout: section`"
    - "D-20: chapter covers slash commands as `.claude/commands/<name>.md`, skills as `.claude/skills/<name>/SKILL.md` with progressive disclosure, plugins as a distribution layer (mention only — don't build a plugin)"
    - "D-20: exercise = write a `.claude/commands/standup.md` slash command"
    - "D-11/D-21: chapter contains ≥1 `✓ Check:` callout with copy-paste-precise expected output"
    - "D-10: every fenced code block ≤25 lines"
    - "D-16: real APIs — `.claude/commands/*.md`, `.claude/skills/<name>/SKILL.md`; mark `<!-- VERIFY -->` for unsure fields"
    - "D-22: no screenshots"
    - "D-23: confident terse no-hype tone"
  artifacts:
    - path: "pages/03-commands-skills-plugins.md"
      provides: "Slash commands, Skills, Plugins chapter content (~25 min)"
      contains: "standup"
  key_links:
    - from: "slides.md"
      to: "pages/03-commands-skills-plugins.md"
      via: "src: directive (created in 04-01)"
      pattern: "src:\\s*\\./pages/03-commands-skills-plugins\\.md"
---

<objective>
Draft the Slash Commands + Skills + Plugins chapter — `pages/03-commands-skills-plugins.md` — per D-20. ~25 min hands-on. Three primitives, one chapter: slash commands (immediate), skills (progressive disclosure), plugins (distribution layer — mention only). Exercise: write `.claude/commands/standup.md`.

Purpose: Capstone chapter per D-05 — "make Claude Code yours". After hooks (react) and subagents (delegate), this is package-and-share. Plugins are mentioned, not built — 25 min isn't enough.

Output: One markdown file with ~14-16 slides following D-08 shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workshop-content-draft/04-CONTEXT.md
@.planning/phases/02-theme-visual-identity/02-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft pages/03-commands-skills-plugins.md following D-08 shape and D-20 substance</name>
  <files>pages/03-commands-skills-plugins.md</files>
  <read_first>
    - .planning/phases/04-workshop-content-draft/04-CONTEXT.md (D-08, D-09..D-12, D-16, D-17, D-20, D-21, D-22, D-23, plus the specifics callout about ~/.claude/skills/ already having gsd-*, vercel:*, hookify:*)
  </read_first>
  <action>
Create `pages/03-commands-skills-plugins.md` with the slide-by-slide structure below. Three sub-sections in one chapter — pace tightly. Slide 1 has `layout: section` per D-17.

Use the "you already have these installed" hook for skills (per the specifics callout in 04-CONTEXT). Don't show every skill in the user's ~/.claude/skills/; just point them at the directory and let them inspect.

---

**Slide 1 — Section title** (`layout: section`)
```markdown
# Commands · Skills · Plugins

three primitives for making Claude Code yours
```

**Slide 2 — Why care (D-08 step 2)**
```markdown
# Why these three

- **Slash commands.** A reusable prompt with a name. Type `/standup` instead of writing the prompt.
- **Skills.** Progressive disclosure — Claude reads a skill's index, fetches details only when relevant.
- **Plugins.** A bundle of commands + skills + agents you can share or install.

Together: write once, invoke fast, share with the team.
```

**Slide 3 — Mental model: the three together**
```markdown
# The pyramid

```
plugin (bundle)
   │
   ├── slash commands  → fast, on-demand prompts
   ├── skills          → discoverable knowledge with progressive disclosure
   └── agents          → focused workers (last chapter)
```

A plugin is just packaging. Underneath, every entry is one of the primitives you already know.
```

---

**Slide 4 — Slash commands: shape (D-20)**
```markdown
# `.claude/commands/<name>.md`

A slash command is a markdown file. Type `/<name>` in Claude Code; the file's body becomes the prompt.

```markdown
---
description: Generate a daily standup summary from recent commits.
---

Read the last 24 hours of git activity in this repo.
For each meaningful commit, write one line: "<author>: <one-sentence summary>".
End with three bullets: in progress, blockers, next.
```

`description` shows up in the `/` menu autocomplete. The body is the prompt — just markdown. <!-- VERIFY: any other reserved frontmatter keys (e.g. allowed-tools) -->
```

**Slide 5 — Slash commands: arguments (concept)**
```markdown
# Arguments

Pass arguments after the slash:

```
/standup --since 3d
```

The full string after the command name is available to your prompt body via the standard argument convention. Use it like any other variable in your prompt.

<!-- VERIFY: exact placeholder syntax for arguments inside the command body — $ARGUMENTS or {args} -->
```

**Slide 6 — Live walkthrough: list and write**
```bash
# Project-local commands live in .claude/commands/.
# User-wide commands live in ~/.claude/commands/.
ls .claude/commands/ 2>/dev/null
ls ~/.claude/commands/ 2>/dev/null
```

```markdown
✓ Check: at least one of those directories listed (may be empty).
```

**Slide 7 — Exercise (D-20, D-08 step 5) — write `/standup`**
```markdown
# Exercise (5 min) — write `/standup`

1. Create `.claude/commands/standup.md`:

```markdown
---
description: Daily standup summary from recent commits.
---

Run `git log --since="24 hours ago" --oneline` in this repo.
For each commit, write one line: "<short-sha>: <one-sentence summary of what changed>".
Then three bullets: 'in progress', 'blockers', 'next'.
Keep it under 15 lines total.
```

2. In Claude Code, type `/standup` and press enter.
3. ✓ Check: Claude runs `git log --since="24 hours ago" --oneline`, then prints commit summaries and three bullets, all under ~15 lines.
```

---

**Slide 8 — Skills: shape (D-20)**
```markdown
# `.claude/skills/<name>/SKILL.md`

A skill is a directory with a `SKILL.md` index. Claude sees the index — short, descriptive — and only reads supporting files when relevant.

```
.claude/skills/
└── postgres-migrations/
    ├── SKILL.md           ← always-loaded index
    ├── examples/up-down.sql
    └── playbooks/safe-migration.md
```

The `SKILL.md` says *what* the skill is for and *what's inside*. Claude pulls the rest only when the situation calls for it.
```

**Slide 9 — Skills: progressive disclosure**
```markdown
# Progressive disclosure

The win: large knowledge bases don't blow up your context.

- **Always loaded:** every `SKILL.md` index in scope (a few hundred tokens each).
- **Loaded on demand:** the rest of the skill's files, only when Claude decides they're relevant.

You write the index to be a good *table of contents*. The skill's depth lives in the supporting files.
```

**Slide 10 — Skills: live look at what you already have**
```bash
# You almost certainly have skills installed already.
ls ~/.claude/skills/ 2>/dev/null | head -10
# Pick one and read its index:
cat ~/.claude/skills/$(ls ~/.claude/skills/ | head -1)/SKILL.md 2>/dev/null | head -30
```

```markdown
✓ Check: you see a `SKILL.md` index with a short description and a list of supporting files.
That's the contract — index = always loaded, files = on demand.
```

---

**Slide 11 — Plugins: distribution layer (D-20, mention only)**
```markdown
# Plugins

A plugin is a directory bundling **commands + skills + agents** for distribution.

```
my-plugin/
├── commands/
├── skills/
└── agents/
```

Install once, get every primitive inside. We won't build one in 25 minutes — but the pattern is just "your `.claude/` directory, packaged".

<!-- VERIFY: exact plugin manifest filename (plugin.json? package.json? .claude/plugin.yml?) -->
```

**Slide 12 — When commands, when skills, when agents**
```markdown
# Which primitive when

- **Slash command** — short reusable prompt. Body fits on one screen.
- **Skill** — body wouldn't fit on one screen. You want progressive disclosure.
- **Agent** — needs its own context, its own tool budget, its own personality.

Sliding scale, same family.
```

**Slide 13 — Recap + pointers (D-08 step 6)**
```markdown
# Commands · Skills · Plugins recap

- Slash commands = `.claude/commands/<name>.md`. Body is the prompt. Frontmatter `description` powers the menu.
- Skills = `.claude/skills/<name>/SKILL.md` index plus on-demand support files. Progressive disclosure.
- Plugins = a directory bundling all three for distribution.

Same backbone, different scope. Pick the smallest one that does the job.
```

---

**Constraints:**
- D-10: every fenced code block ≤25 lines.
- D-16: real APIs — `.claude/commands/`, `.claude/skills/<name>/SKILL.md`. Anything you're not sure of: `<!-- VERIFY -->`.
- D-22: no screenshots.
- D-23: confident, terse, no hype.
- D-12: no `<v-clicks>` — content is reference-shaped.
- D-17: slide 1 `layout: section` only.
- No inline hex.
- 12-16 slides total.
- Nested-fence handling: when showing a markdown command/skill file that itself contains frontmatter, use ` ```markdown ` ... ` ``` ` — no 4-backtick fences. (Shiki parses standard fences only.)
  </action>
  <verify>
    <automated>
      test -f pages/03-commands-skills-plugins.md &&
      head -5 pages/03-commands-skills-plugins.md | grep -q '^layout: section$' &&
      # D-20 substance
      grep -q '\.claude/commands' pages/03-commands-skills-plugins.md &&
      grep -q 'SKILL\.md' pages/03-commands-skills-plugins.md &&
      grep -qi 'progressive disclosure' pages/03-commands-skills-plugins.md &&
      grep -qi 'plugin' pages/03-commands-skills-plugins.md &&
      grep -q 'standup' pages/03-commands-skills-plugins.md &&
      # ≥1 ✓ Check:
      [ "$(grep -c '✓ Check:' pages/03-commands-skills-plugins.md)" -ge 1 ] &&
      # D-08 sections
      grep -qE '^# Why these three' pages/03-commands-skills-plugins.md &&
      grep -qE '^# Exercise' pages/03-commands-skills-plugins.md &&
      grep -qE 'recap|Recap' pages/03-commands-skills-plugins.md &&
      # no hype / no screenshots
      ! grep -qiE '!{2,}|amazing|awesome|incredible|<img |!\[[^]]+\]\(' pages/03-commands-skills-plugins.md &&
      # no inline hex
      ! grep -E '#[0-9a-fA-F]{6}' pages/03-commands-skills-plugins.md &&
      # ≥12 slides
      [ "$(grep -c '^---$' pages/03-commands-skills-plugins.md)" -ge 12 ] &&
      # all fenced code blocks ≤25 lines
      awk '/^```/{c++; if(c%2==1){n=0} else {if(n>25){print "OVERLONG block ending at line " NR; exit 1}}; next} c%2==1{n++}' pages/03-commands-skills-plugins.md
    </automated>
  </verify>
  <done>pages/03-commands-skills-plugins.md exists, has section-layout title, names all three primitives with their real file shapes, includes the standup.md exercise with ✓ Check, mentions plugins as distribution (no plugin build), and a recap. All code blocks ≤25 lines.</done>
</task>

</tasks>

<verification>
Wave 3 (04-05) verifies build/presenter/PDF.
</verification>

<success_criteria>
- pages/03-commands-skills-plugins.md exists, ≥12 slides
- All three primitives named with their real file paths
- Exercise slide writes `standup.md`
- "Progressive disclosure" appears in the skills section
- Plugins mentioned but not built
- ≥1 `✓ Check:` callout
- No screenshots, no inline hex, no hype
</success_criteria>

<output>
`.planning/phases/04-workshop-content-draft/04-04-SUMMARY.md` with slide count, all `<!-- VERIFY -->` markers and what each flags.
</output>
</content>
</invoke>