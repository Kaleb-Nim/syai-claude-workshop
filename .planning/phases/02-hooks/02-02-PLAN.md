---
phase: 02-hooks
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pages/02-hooks/02-why-hooks.md
autonomous: true
requirements: [HOOK-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 02 file contains 5 slides separated by mid-file `---` blank-line-flanked separators"
    - "Slides 1-2 establish the IFTTT framing (IF→THEN, then 'THEN is just a shell command')"
    - "Slides 3, 4, 5 each carry exactly one of the three D-03 locked quotes verbatim, byte-for-byte"
    - "Slide 4 includes a callback to Phase 1 Win 2 referencing the exact path `~/.claude/CLAUDE.md`"
    - "No exclamation marks appear in the body (QUAL-04)"
    - "D-12: `<v-clicks>` skipped by default in this section; presenter may add live if pacing benefits — drafted output contains zero `<v-clicks>` directives"
  artifacts:
    - path: "pages/02-hooks/02-why-hooks.md"
      provides: "Section 02 — Why hooks (IFTTT framing + 3 locked determinism quotes)"
      contains: "The key diff between Hooks and everything else is that Hooks are deterministic, they always run"
  key_links:
    - from: "Slide 4 callback prose"
      to: "Phase 1 Win 2 (~/.claude/CLAUDE.md)"
      via: "inline-coded path in dim helper div"
      pattern: "~/.claude/CLAUDE.md"
---

<objective>
Author Section 02 — Why hooks. Five slides in one file: IFTTT framing (Slides 1-2) then three user-LOCKED determinism quotes verbatim (Slides 3-5). Slide 4 includes a Phase 1 callback to the `~/.claude/CLAUDE.md` rule from Win 2, framing it as the imperfect-version-of-a-hook. Satisfies HOOK-01.

Purpose: Frame why hooks exist — what problem they solve.
Output: One new file `pages/02-hooks/02-why-hooks.md`, single atomic commit (QUAL-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-hooks/02-CONTEXT.md
@.planning/phases/02-hooks/02-RESEARCH.md
@.planning/phases/02-hooks/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 02 (5 slides — IFTTT + 3 locked quotes)</name>
  <files>pages/02-hooks/02-why-hooks.md</files>
  <read_first>
    - pages/01-audience-setup/02-prereqs-install.md (Phase 1 ANALOG — multi-slide pattern, mid-file `---` separators, bold inline labels)
    - pages/01-audience-setup/07-win-claude-md.md (Phase 1 ANALOG — blockquote/dim-helper rendering; ground truth for the `~/.claude/CLAUDE.md` Win 2 callback)
    - .planning/phases/02-hooks/02-CONTEXT.md (D-02, D-03 — locked quotes verbatim; D-04 — Phase 1 callback; D-12 — `<v-clicks>` skipped by default)
    - .planning/phases/02-hooks/02-PATTERNS.md (02-why-hooks.md section + Pattern E multi-slide + Pattern D dim helpers)
    - .planning/phases/02-hooks/02-RESEARCH.md (Pitfall 6 — Slide 4 callback path MUST be `~/.claude/CLAUDE.md` byte-exact)
  </read_first>
  <action>
Create the NEW file `pages/02-hooks/02-why-hooks.md` with EXACTLY these contents. No frontmatter — first line is `# IF this, THEN that`. Five slides separated by mid-file `---` lines, each `---` flanked by blank lines on both sides. The three quotes are USER-LOCKED — copy them byte-for-byte from this action; do NOT paraphrase, do NOT "fix" capitalization or punctuation, do NOT add or remove words.

```markdown
# IF this, THEN that

- **IF** Claude finishes  →  **THEN** play a sound
- **IF** Claude uses a tool  →  **THEN** log it
- **IF** you submit a prompt  →  **THEN** check it first

---

# The "THEN" is just a shell command

Anything you can run in your terminal can be the "THEN".

Today we make the "THEN" a sound.

---

# What makes hooks different

> The key diff between Hooks and everything else is that Hooks are deterministic, they always run

---

# Why use hooks

> We can tell claude in your claude.md file to ping us every time it needs an input from us. But it's not perfect. A hook makes it run every single time without exceptions

<div class="text-dim text-sm mt-6">
  the rule you put in <code>~/.claude/CLAUDE.md</code> in Chapter 1 — that's the imperfect version of a hook
</div>

---

# In essence

> If something needs to happen every single time, don't put it in a prompt, put it in a hook
```

Element-by-element notes:
- **No frontmatter** — file starts directly with `# IF this, THEN that` on line 1 (per PATTERNS Pattern A; 6 of 7 Phase 1 section files have zero frontmatter).
- **Slides 1-2 (IFTTT framing)** — bold-list and short prose. The arrow is `→` (U+2192) with single spaces around it. Two spaces between `Claude finishes` and `→` and between `→` and `**THEN**` to give visual rhythm (matches the spec line breaks).
- **Slide 3 — Quote 1 (LOCKED, verbatim):** `The key diff between Hooks and everything else is that Hooks are deterministic, they always run`
- **Slide 4 — Quote 2 (LOCKED, verbatim):** `We can tell claude in your claude.md file to ping us every time it needs an input from us. But it's not perfect. A hook makes it run every single time without exceptions`
- **Slide 4 callback dim helper** — exact path is `~/.claude/CLAUDE.md` inside `<code>` tags (Pitfall 6 — load-bearing). Uses Variant 2 dim helper (`text-dim text-sm mt-6`).
- **Slide 5 — Quote 3 (LOCKED, verbatim):** `If something needs to happen every single time, don't put it in a prompt, put it in a hook`
- Blockquotes use bare `> ` markdown prefix (no `<Quote>` component). Default theme renders them legibly against the locked palette.
- **No `<v-clicks>`** — D-12 says default is skip; presenter can add live if pacing benefits.
- **No exclamation marks** anywhere in the file (QUAL-04). Quotes are verified clean.

Atomic commit (QUAL-02): `feat(phase-2): section 02 why-hooks (IFTTT + locked determinism quotes)`.
  </action>
  <verify>
    <automated>test -f pages/02-hooks/02-why-hooks.md && grep -Fxq "> The key diff between Hooks and everything else is that Hooks are deterministic, they always run" pages/02-hooks/02-why-hooks.md && grep -Fxq "> We can tell claude in your claude.md file to ping us every time it needs an input from us. But it's not perfect. A hook makes it run every single time without exceptions" pages/02-hooks/02-why-hooks.md && grep -Fxq "> If something needs to happen every single time, don't put it in a prompt, put it in a hook" pages/02-hooks/02-why-hooks.md && grep -q "~/.claude/CLAUDE.md" pages/02-hooks/02-why-hooks.md && [ "$(grep -c '^!' pages/02-hooks/02-why-hooks.md)" = "0" ] && [ "$(grep -c '^---$' pages/02-hooks/02-why-hooks.md)" = "4" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/02-hooks/02-why-hooks.md` exists
    - File contains exactly 4 mid-file `---` slide separators (5 slides total): `grep -c "^---$" pages/02-hooks/02-why-hooks.md` returns `4`
    - File does NOT start with frontmatter — line 1 is `# IF this, THEN that`
    - Quote 1 line appears verbatim: `> The key diff between Hooks and everything else is that Hooks are deterministic, they always run`
    - Quote 2 line appears verbatim: `> We can tell claude in your claude.md file to ping us every time it needs an input from us. But it's not perfect. A hook makes it run every single time without exceptions`
    - Quote 3 line appears verbatim: `> If something needs to happen every single time, don't put it in a prompt, put it in a hook`
    - `grep -c "~/.claude/CLAUDE.md" pages/02-hooks/02-why-hooks.md` returns `1` (Slide 4 callback path)
    - `grep -c "text-dim text-sm mt-6" pages/02-hooks/02-why-hooks.md` returns `1` (Slide 4 dim helper)
    - File body contains no `!` character (QUAL-04): `grep -c "!" pages/02-hooks/02-why-hooks.md` returns `0`
    - No `<v-clicks>` element present
  </acceptance_criteria>
  <done>Section 02 file authored with IFTTT framing + 3 locked quotes verbatim + Phase 1 callback on Slide 4; committed atomically.</done>
</task>

</tasks>

<verification>
- All 3 locked quotes present byte-for-byte
- Slide 4 callback uses exact `~/.claude/CLAUDE.md` path
- 5 slides, 4 mid-file `---` separators
- No exclamation marks in body (QUAL-04)
</verification>

<success_criteria>
HOOK-01 satisfied: Chapter 2 frames why hooks exist via IFTTT framing + 3 determinism quotes; Phase 1 bridge present on Slide 4.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hooks/02-02-SUMMARY.md`.
</output>
