---
phase: 02-hooks
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified: []
autonomous: false
requirements: [QUAL-01, QUAL-04]
must_haves:
  truths:
    - "`bunx slidev build` exits 0 with all 5 Phase 2 section files wired"
    - "Build artifact `dist/index.html` exists after the build"
    - "Presenter mode renders Chapter 2 from cover through Section 05 closing line without console errors (human-verified)"
    - "Section 04 JSON block is legible at presenter resolution OR has been retrofitted with the Phase 1 `<style scoped>` font-shrink"
  artifacts:
    - path: "dist/index.html"
      provides: "Static SPA build output (build success indicator)"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "bunx slidev build"
      to: "dist/index.html"
      via: "Slidev → Vite SPA pipeline"
      pattern: "bunx slidev build"
---

<objective>
Close Chapter 2 with the QUAL-01 build + presenter spot-check gate. Mirrors Phase 1 Plan 01-08 shape. Run `bunx slidev build` (must exit 0 and produce `dist/index.html`), then have the presenter step through Chapter 2 in presenter mode to confirm the chapter renders clean — cover slide, IFTTT + 3 quote slides, events+JSON slide, paste-target JSON slide, fire-it slide with two ✓ Checks, no broken `src:` includes, no console errors.

This plan modifies NO source files by default. The ONE allowed source modification is a conditional retrofit: if Section 04's JSON visually overflows at presenter resolution, apply the Phase 1 `<style scoped>` font-shrink pattern (RESEARCH Open Question 3 / PATTERNS Pattern G). That retrofit is itself a small atomic commit.

Because the spot-check is human-driven, this plan is `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-hooks/02-CONTEXT.md
@.planning/phases/02-hooks/02-RESEARCH.md
@.planning/phases/02-hooks/02-PATTERNS.md
@.planning/phases/01-audience-setup/01-08-PLAN.md
@.planning/phases/01-audience-setup/01-08-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bunx slidev build and confirm exit 0 + dist/index.html</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (QUAL-01 wording)
    - .planning/phases/01-audience-setup/01-08-PLAN.md (Phase 1 build plan — ANALOG; same failure-signature playbook applies)
    - .planning/phases/01-audience-setup/01-08-SUMMARY.md (port-3030 stale-process gotcha noted at Phase 1 close)
    - CLAUDE.md (Bun-only — `bunx`, NEVER `npx`)
  </read_first>
  <action>
From the repo root, run the production build using Bun's bundled runner:

```bash
bunx slidev build
```

This must:
1. Exit with status `0`.
2. Produce `dist/index.html`.
3. Emit no Slidev / Vite / Shiki ERROR-level messages (warnings are tolerable; errors are not).

Common failure signatures and what they mean (diagnose, then return to the offending plan — do NOT mask):
- `Imported markdown file not found: ./pages/02-hooks/0N-*.md` → orchestrator `src:` path mismatch (Plan 02-01 regression — paths must be relative to `pages/`, not `./pages/...`).
- Missing file under `pages/02-hooks/0N-*.md` → corresponding Wave 2 section plan failed; do not patch around it.
- Shiki lang error for ` ```json ` → unlikely (`json` is in `slides.md` headmatter `langs:` list), but if seen, confirm `slides.md` lines 1-18 were not edited (Plan 02-01 Pitfall 7 regression).
- JSON-parse-like error inside Slidev build → confirm no JSON `//` or `#` comments slipped into a ```json fence (Plan 02-03 or 02-04 Pitfall 2 regression).
- `vite.config.ts` warning → if a `vite.config.ts` was accidentally introduced, delete it (CLAUDE.md constraint, Slidev issue #2043).

Do NOT modify any source file from this task to make the build pass. Build failures here indicate a Wave 2 plan has a defect; surface it as a blocker and return to that plan.
  </action>
  <verify>
    <automated>bunx slidev build && test -f dist/index.html && grep -q "<!DOCTYPE html>" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `bunx slidev build` exits with status `0`
    - File `dist/index.html` exists after the build
    - `grep -q "<!DOCTYPE html>" dist/index.html` succeeds
    - Build output contains no `ERROR` lines (warnings OK)
    - No `vite.config.ts` was created or modified by this plan
  </acceptance_criteria>
  <done>Build passes; static SPA artifact exists at `dist/index.html`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Presenter spot-check Chapter 2 (human-verify)</name>
  <files>(read-only — no source files modified by default; conditional retrofit in Task 3 if overflow seen)</files>
  <action>See `<how-to-verify>` below. The presenter runs the steps and resumes on the `<resume-signal>`.</action>
  <verify><automated>echo "human-verify checkpoint — see how-to-verify section"</automated></verify>
  <done>Presenter types `approved` (or `approved-with-retrofit` if Section 04 needs Pattern G font-shrink) after the per-section walkthrough completes without failures.</done>
  <what-built>
A complete Chapter 2 wired into the deck via `slides.md → pages/02-hooks.md → 5 section files`. Cover, why-hooks (IFTTT + 3 locked determinism quotes), how-hooks-work (events + minimal Stop-only JSON + cross-OS notes), hands-on build (full Stop+Notification paste-target JSON), hands-on fire (two micro-steps + two ✓ Checks + closing line).
  </what-built>
  <how-to-verify>
1. Start the dev server in presenter mode:

   ```bash
   bunx slidev --open
   ```

   If port 3030 is occupied (Phase 1 SUMMARY notes this gotcha), kill the stale process or use `bunx slidev --port 3031 --open`.

2. Navigate to Chapter 2's cover slide using the slide menu (`o` key for overview, click Chapter 2 cover, OR arrow-key through from the deck cover and past all of Chapter 1).

3. Step forward through every Chapter 2 slide using the right-arrow key. Confirm:

   - **Section 01 — Cover:** centered layout. Title `Chapter 2 — Hooks` with em-dash. One-line provocation visible. Dim subtitle reads `why hooks · how they work · one chime you'll wire live` with middle-dot separators.

   - **Section 02 — Slide 1 (IFTTT):** H1 `IF this, THEN that`. Three bolded `IF → THEN` bullets present in order: finishes→sound, uses tool→log, submits prompt→check.

   - **Section 02 — Slide 2 (THEN is shell):** H1 `The "THEN" is just a shell command`. Two short prose lines below. No exclamation marks.

   - **Section 02 — Slide 3 (Quote 1):** H1 `What makes hooks different`. Blockquote contains VERBATIM: `The key diff between Hooks and everything else is that Hooks are deterministic, they always run`.

   - **Section 02 — Slide 4 (Quote 2 + callback):** H1 `Why use hooks`. Blockquote contains VERBATIM: `We can tell claude in your claude.md file to ping us every time it needs an input from us. But it's not perfect. A hook makes it run every single time without exceptions`. Dim helper below reads `the rule you put in <code>~/.claude/CLAUDE.md</code> in Chapter 1 — that's the imperfect version of a hook`. Path `~/.claude/CLAUDE.md` must be byte-exact.

   - **Section 02 — Slide 5 (Quote 3):** H1 `In essence`. Blockquote contains VERBATIM: `If something needs to happen every single time, don't put it in a prompt, put it in a hook`.

   - **Section 03 — How hooks work:** H1 `How hooks work`. Two bolded event lines for `Stop` and `Notification`. Fenced ```json block contains the byte-exact Stop-only canonical structure with `afplay /System/Library/Sounds/Glass.aiff`. NO `//` comments inside the JSON fence. Dim helper BELOW the fence contains both cross-OS notes: Linux/WSL uses `/usr/share/sounds/alsa/Front_Center.wav` (NOT `.oga`); Windows note reads `easiest path today is to run Claude Code inside WSL so afplay/aplay and shell paths work` (the softened wording — verify it does NOT say `bash/jq/paths break otherwise`). Tiny "more exist" line at bottom inline-codes `PreToolUse, PostToolUse, UserPromptSubmit, SessionStart`.

   - **Section 04 — Hands-on build:** H1 `Wire it: paste this into \`~/.claude/settings.json\``. Reconcile prose line `Notification has the same shape — we just repeat it.` immediately above the JSON fence. Fenced ```json block contains BOTH `Stop` and `Notification` entries with `Glass.aiff` and `Ping.aiff` respectively. NO `"matcher"` field anywhere. NO comments inside the fence. Trailing dim helper has the Linux/WSL substitution note. **Visual overflow check:** the full JSON block fits within the slide viewport at default presenter resolution. If it overflows (text clipped, requires scroll), proceed to Task 3 below.

   - **Section 05 — Hands-on fire:** H1 `Fire it`. Two bold-prefixed steps with inline-coded `Stop` and `Notification`. Two `> ✓ Check:` blockquotes with literal Unicode `✓` glyph — first chime check, then different-chime check (italic `different`). Closing dim line `Two events, same shape. Add more later — that's the whole pattern.` visible below.

4. Open browser devtools Console. Confirm: no red errors during the Chapter 2 walkthrough. (Warnings about HMR / Vue dev-mode are acceptable.)

5. Confirm `global-top.vue` progress bar continues to advance as you step through.

6. Confirm Chapter 1 still renders cleanly (Phase 1 unchanged) — navigate back to verify no regression.

If ALL of the above pass with NO overflow on Section 04 → type `approved`. Skip Task 3.

If ALL pass EXCEPT Section 04 overflows → type `approved-with-retrofit`. Proceed to Task 3.

If ANY OTHER expectation fails, describe the issue precisely (which section, which expectation, what was rendered). Return to the offending plan for revision — DO NOT patch here.
  </how-to-verify>
  <resume-signal>Type "approved", "approved-with-retrofit", or describe failed expectations.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: (Conditional) Retrofit Section 04 with `<style scoped>` font-shrink if presenter saw overflow</name>
  <files>pages/02-hooks/04-hands-on-build.md</files>
  <read_first>
    - pages/01-audience-setup/05-claude-files-primer.md (Phase 1 ANALOG — lines 36-47, the `<style scoped>` block to mirror byte-exactly)
    - .planning/phases/02-hooks/02-PATTERNS.md (Pattern G — `<style scoped>` font-shrink rule + conditional decision policy)
    - .planning/phases/02-hooks/02-RESEARCH.md (Open Question 3 — defer-then-retrofit decision)
  </read_first>
  <action>
ONLY execute this task if Task 2 resumed with `approved-with-retrofit`. Otherwise skip.

Append the following block at the END of `pages/02-hooks/04-hands-on-build.md` (after the closing `</div>` of the existing trailing dim helper):

```html
<style scoped>
pre, code {
  font-size: 0.7rem !important;
  line-height: 1.15 !important;
}
.slidev-code {
  padding: 0.6em 0.9em !important;
}
h1 {
  margin-bottom: 0.4em !important;
}
</style>
```

This is byte-identical to the Phase 1 `05-claude-files-primer.md` font-shrink block.

After appending: re-run `bunx slidev build` (must still exit 0), then re-step through Section 04 in presenter mode to confirm the JSON now fits within the viewport.

Atomic commit: `style(phase-2): shrink section 04 JSON to fit presenter viewport`.
  </action>
  <verify>
    <automated>if grep -q "<style scoped>" pages/02-hooks/04-hands-on-build.md; then bunx slidev build && test -f dist/index.html && grep -q "font-size: 0.7rem" pages/02-hooks/04-hands-on-build.md; else echo "skipped — no retrofit needed"; fi</automated>
  </verify>
  <acceptance_criteria>
    - IF Task 2 resumed with `approved-with-retrofit`: `pages/02-hooks/04-hands-on-build.md` ends with the Phase 1 `<style scoped>` font-shrink block; `bunx slidev build` re-passes
    - IF Task 2 resumed with `approved`: this task is skipped; no file modified
    - No other Section 04 content is altered (JSON, reconcile prose, footnote all unchanged)
  </acceptance_criteria>
  <done>Either no retrofit needed (approved), or retrofit applied and build re-verified; chapter is presentable.</done>
</task>

</tasks>

<verification>
- `bunx slidev build` exit 0
- `dist/index.html` exists
- Human presenter walkthrough passes the per-section checklist (and optional Section 04 font-shrink retrofit applied if needed)
- Chapter 1 still renders clean (no regression)
</verification>

<success_criteria>
QUAL-01 satisfied: Chapter 2 builds clean and renders clean in presenter mode. Phase 2 of v1.1 is shippable; Phase 3 (Skills) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hooks/02-06-SUMMARY.md`.
</output>
