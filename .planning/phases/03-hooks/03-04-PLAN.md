---
phase: 03-hooks
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pages/03-hooks/04-hands-on-build.md
autonomous: true
requirements: [HOOK-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 04 is a single slide with the full Stop+Notification JSON ready for participants to paste"
    - "JSON contains BOTH events with the canonical nested hooks shape and no matcher"
    - "One-line reconcile prose appears BEFORE the JSON fence: 'Notification has the same shape — we just repeat it.'"
    - "A brief Linux/WSL footnote sits below the JSON in a dim helper div"
    - "D-11 — Section 04 JSON code block stays at or under 25 lines, rendered as a fenced json block; no custom Cmd Vue component"
  artifacts:
    - path: "pages/03-hooks/04-hands-on-build.md"
      provides: "Section 04 — Hands-on build (full paste-target JSON for ~/.claude/settings.json)"
      contains: "afplay /System/Library/Sounds/Ping.aiff"
  key_links:
    - from: "Section 04 JSON paste-target"
      to: "Section 03 minimal JSON"
      via: "structural repetition (same shape, two events)"
      pattern: '"Notification": \['
---

<objective>
Author Section 04 — Hands-on build. Single slide. One short reconcile line (`Notification has the same shape — we just repeat it.`) then a fenced ```json block containing BOTH Stop and Notification entries, then a brief Linux/WSL footnote. Participants copy-paste this whole JSON block into `~/.claude/settings.json`. Satisfies the HOOK-03 wire step.

Purpose: Give participants the concrete artifact they need to paste — extending Section 03's shape symmetrically for the second event.
Output: One new file, single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hooks/02-CONTEXT.md
@.planning/phases/03-hooks/02-RESEARCH.md
@.planning/phases/03-hooks/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 04 (single slide — full Stop+Notification paste-target JSON)</name>
  <files>pages/03-hooks/04-hands-on-build.md</files>
  <read_first>
    - pages/01-audience-setup/05-claude-files-primer.md (Phase 1 ANALOG — large-block slide; `<style scoped>` font-shrink pattern available if overflow occurs at Plan 06 spot-check)
    - .planning/phases/03-hooks/02-CONTEXT.md (D-07 paste shape; D-08 reconcile note wording)
    - .planning/phases/03-hooks/02-RESEARCH.md (Pattern 5 — recommended whole-file content; Pitfall 2 — no JSON comments; Pitfall 3 — no matcher in Notification block; Code Examples — canonical Stop+Notification)
    - .planning/phases/03-hooks/02-PATTERNS.md (04-hands-on-build.md section; Pattern G `<style scoped>` font-shrink rule — DEFER to Plan 06)
  </read_first>
  <action>
Create the NEW file `pages/03-hooks/04-hands-on-build.md` with EXACTLY these contents. No frontmatter. The JSON is byte-exact per CONTEXT D-07 (Stop+Notification together, no matchers, no comments inside the fence). Draft WITHOUT the `<style scoped>` font-shrink block — Plan 06's presenter spot-check decides whether to retrofit.

````markdown
# Wire it: paste this into `~/.claude/settings.json`

Notification has the same shape — we just repeat it.

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "afplay /System/Library/Sounds/Glass.aiff"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "afplay /System/Library/Sounds/Ping.aiff"
          }
        ]
      }
    ]
  }
}
```

<div class="text-dim text-sm mt-4">
  Linux / WSL → swap both <code>afplay</code> calls for <code>aplay /usr/share/sounds/alsa/Front_Center.wav</code> (use a different <code>.wav</code> for the second to hear a different chime)
</div>
````

Element-by-element notes:
- **No frontmatter** — line 1 is the H1.
- **H1** wraps `~/.claude/settings.json` in inline backticks (the path is information-bearing for participants).
- **Reconcile prose** — verbatim per D-08: `Notification has the same shape — we just repeat it.` (No exclamation marks. Period at end.)
- **JSON block (~22 lines)** — Stop entry first, Notification entry second; both omit `"matcher"` (Pitfall 3 — omitting catches all notification subtypes including `permission_prompt` which Section 05 triggers).
  - Stop command: `afplay /System/Library/Sounds/Glass.aiff`
  - Notification command: `afplay /System/Library/Sounds/Ping.aiff`
  - Indentation: 2-space, matching Section 03's canonical shape.
- **NO comments inside the JSON fence** (Pitfall 2). The Linux/WSL note sits OUTSIDE the fence as prose.
- **Trailing dim helper** uses Variant 2 (`text-dim text-sm mt-4`). Inline `<code>` for `afplay`, `aplay /usr/share/sounds/alsa/Front_Center.wav`, and `.wav`. The "use a different `.wav` for the second" parenthetical is intentional — gives Linux/WSL participants a path to the two-chime experience without prescribing a second specific file.
- **Do NOT add the `<style scoped>` font-shrink block in this plan.** Decision deferred to Plan 06 spot-check (RESEARCH Open Question 3).
- **No exclamation marks** anywhere (QUAL-04).

Atomic commit (QUAL-02): `feat(phase-3): section 04 hands-on build (Stop+Notification paste-target JSON)`.
  </action>
  <verify>
    <automated>test -f pages/03-hooks/04-hands-on-build.md && grep -q "^# Wire it: paste this into \`~/.claude/settings.json\`$" pages/03-hooks/04-hands-on-build.md && grep -Fxq "Notification has the same shape — we just repeat it." pages/03-hooks/04-hands-on-build.md && grep -q '"command": "afplay /System/Library/Sounds/Glass.aiff"' pages/03-hooks/04-hands-on-build.md && grep -q '"command": "afplay /System/Library/Sounds/Ping.aiff"' pages/03-hooks/04-hands-on-build.md && ! grep -q '"matcher"' pages/03-hooks/04-hands-on-build.md && [ "$(awk '/^```json$/,/^```$/' pages/03-hooks/04-hands-on-build.md | grep -cE '(^//|^#[^!])')" = "0" ] && ! grep -q "<style scoped>" pages/03-hooks/04-hands-on-build.md && [ "$(grep -c '!' pages/03-hooks/04-hands-on-build.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-hooks/04-hands-on-build.md` exists
    - H1 is exactly `# Wire it: paste this into \`~/.claude/settings.json\``
    - Reconcile prose line `Notification has the same shape — we just repeat it.` appears verbatim
    - JSON contains BOTH `"Stop": [` and `"Notification": [` blocks
    - JSON Stop command is `afplay /System/Library/Sounds/Glass.aiff`
    - JSON Notification command is `afplay /System/Library/Sounds/Ping.aiff`
    - JSON contains NO `"matcher"` field (Pitfall 3)
    - JSON fence contains NO `//` or `#`-prefixed comment lines (Pitfall 2)
    - File does NOT contain `<style scoped>` (deferred to Plan 06)
    - `grep -c "!" pages/03-hooks/04-hands-on-build.md` returns `0` (QUAL-04)
    - Linux/WSL footnote present with `/usr/share/sounds/alsa/Front_Center.wav`
  </acceptance_criteria>
  <done>Section 04 file authored with full paste-target JSON + reconcile prose + Linux/WSL footnote; committed atomically.</done>
</task>

</tasks>

<verification>
- Full Stop+Notification JSON byte-exact and copy-safe (no comments, no matcher)
- Reconcile prose D-08 present verbatim
- Linux/WSL footnote outside the fence
- No font-shrink override (decision deferred to Plan 06)
</verification>

<success_criteria>
HOOK-03 (wire step) satisfied: participants have one complete, valid JSON block to paste.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hooks/02-04-SUMMARY.md`.
</output>
