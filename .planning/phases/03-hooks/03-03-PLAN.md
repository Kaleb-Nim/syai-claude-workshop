---
phase: 03-hooks
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pages/03-hooks/03-how-hooks-work.md
autonomous: true
requirements: [HOOK-02, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 03 is a single slide teaching Stop + Notification events + minimal settings.json JSON shape"
    - "JSON shape is the byte-exact canonical D-06 Stop-only block (no `matcher`, no comments inside the fence)"
    - "Cross-OS notes render OUTSIDE the JSON fence in a dim helper div"
    - "Linux/WSL path is `/usr/share/sounds/alsa/Front_Center.wav` (NOT `.oga`)"
    - "Native Windows footnote is softened — frames WSL as practical recommendation for this hands-on, not as a Claude Code requirement"
    - "Other lifecycle events listed by name only in a single trailing dim line: PreToolUse, PostToolUse, UserPromptSubmit, SessionStart"
    - "Code block on this slide stays at or under 25 lines (D-11), rendered in fenced json fence; no custom Cmd Vue component"
  artifacts:
    - path: "pages/03-hooks/03-how-hooks-work.md"
      provides: "Section 03 — How hooks work (events + minimal JSON shape)"
      contains: "afplay /System/Library/Sounds/Glass.aiff"
  key_links:
    - from: "fenced ```json block"
      to: "~/.claude/settings.json (participant's local file at workshop time)"
      via: "copy-paste teaching artifact"
      pattern: '"type": "command"'
---

<objective>
Author Section 03 — How hooks work. Single slide. Stop + Notification events introduced (one line each), minimal Stop-only JSON in a fenced ```json block, cross-OS notes rendered as prose dim helpers OUTSIDE the fence, trailing "more exist" line naming the other lifecycle events. Satisfies HOOK-02.

Purpose: Teach the mechanical shape participants will paste in Section 04 — but with the minimal version so the JSON structure reads at one glance.
Output: One new file, single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hooks/02-CONTEXT.md
@.planning/phases/03-hooks/02-RESEARCH.md
@.planning/phases/03-hooks/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 03 (single slide — events + minimal JSON + cross-OS notes)</name>
  <files>pages/03-hooks/03-how-hooks-work.md</files>
  <read_first>
    - pages/01-audience-setup/06-win-statusline.md (Phase 1 ANALOG — single-slide demo with fenced blocks + dim prose)
    - .planning/phases/03-hooks/02-CONTEXT.md (D-05, D-06 — events emphasized, minimal Stop-only JSON, cross-OS notes OUTSIDE fence)
    - .planning/phases/03-hooks/02-RESEARCH.md (Pitfall 1 — softened Windows footnote; Pitfall 2 — no comments in JSON; Pitfall 4 — Linux .wav path correction; Pattern 4 — recommended whole-file content)
    - .planning/phases/03-hooks/02-PATTERNS.md (03-how-hooks-work.md section + Pattern B fenced code style + Pattern D dim helpers)
  </read_first>
  <action>
Create the NEW file `pages/03-hooks/03-how-hooks-work.md` with EXACTLY these contents. No frontmatter. The JSON inside the ```json fence is BYTE-EXACT (verified canonical per RESEARCH); do not add `"matcher"`, do not add comments, do not change indentation:

````markdown
# How hooks work

Two events do most of the work:

- **`Stop`** — fires when Claude finishes a turn
- **`Notification`** — fires when Claude needs your attention (permission, idle)

You wire them in `~/.claude/settings.json`:

```json
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "afplay /System/Library/Sounds/Glass.aiff"
          }
        ]
      }
    ]
  }
}
```

<div class="text-dim text-sm mt-4">

- Linux / WSL → swap `afplay` for `aplay /usr/share/sounds/alsa/Front_Center.wav`
- Native Windows → easiest path today is to run Claude Code inside WSL so `afplay`/`aplay` and shell paths work

</div>

<div class="text-dim text-xs mt-4">
  more exist: <code>PreToolUse</code>, <code>PostToolUse</code>, <code>UserPromptSubmit</code>, <code>SessionStart</code> — same shape
</div>
````

Element-by-element notes:
- **No frontmatter** — line 1 is `# How hooks work`.
- **Two bold event lines** with inline-coded names (`**\`Stop\`**`, `**\`Notification\`**`).
- **Fence language is `json`** (not `bash`) — `json` is registered in `slides.md` line 17 `langs:` list. Phase 1 RESEARCH confirms no headmatter edit needed.
- **JSON content is byte-exact** to CONTEXT D-06 (Stop only, no matcher, no comments). 14 lines inside the fence. **Do NOT add `// macOS` or `# Linux` comments inside this fence** (Pitfall 2 — `settings.json` rejects them; copy-paste must yield valid JSON).
- **Cross-OS notes** sit in a `<div class="text-dim text-sm mt-4">` block AFTER the closing ``` of the JSON fence. The blank lines INSIDE the `<div>` (before/after the markdown list) are required for the list to render as markdown inside the HTML wrapper.
- **Windows note is softened** (Pitfall 1) — exact wording: `Native Windows → easiest path today is to run Claude Code inside WSL so afplay/aplay and shell paths work`. Do NOT use the stale "bash/jq/paths break otherwise" phrasing.
- **Linux path** is `/usr/share/sounds/alsa/Front_Center.wav` (Pitfall 4 — never `.oga` with `aplay`).
- **"more exist" line** uses `text-dim text-xs mt-4` (Variant 2 + tiny). Event names inline-coded: `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `SessionStart`. Stop at SessionStart — do NOT extend the list mid-slide (RESEARCH State of the Art).
- **No exclamation marks** in body (QUAL-04).

Atomic commit (QUAL-02): `feat(phase-3): section 03 how hooks work (events + minimal settings.json)`.
  </action>
  <verify>
    <automated>test -f pages/03-hooks/03-how-hooks-work.md && grep -q '^# How hooks work$' pages/03-hooks/03-how-hooks-work.md && grep -q '"command": "afplay /System/Library/Sounds/Glass.aiff"' pages/03-hooks/03-how-hooks-work.md && ! grep -q '"matcher"' pages/03-hooks/03-how-hooks-work.md && grep -q '/usr/share/sounds/alsa/Front_Center.wav' pages/03-hooks/03-how-hooks-work.md && ! grep -q "\.oga" pages/03-hooks/03-how-hooks-work.md && ! grep -q "bash/jq/paths break" pages/03-hooks/03-how-hooks-work.md && grep -q "easiest path today is to run Claude Code inside WSL" pages/03-hooks/03-how-hooks-work.md && [ "$(awk '/^```json$/,/^```$/' pages/03-hooks/03-how-hooks-work.md | grep -cE '(^//|^#[^!])')" = "0" ] && [ "$(grep -c '!' pages/03-hooks/03-how-hooks-work.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-hooks/03-how-hooks-work.md` exists
    - Line 1 is `# How hooks work` (no frontmatter)
    - JSON fence contains the canonical Stop-only shape — `grep -c '"type": "command"' returns `1`
    - JSON fence contains `"command": "afplay /System/Library/Sounds/Glass.aiff"` exactly once
    - JSON fence does NOT contain `"matcher"` — `grep -c '"matcher"'` returns `0`
    - JSON fence contains NO `//` or `#`-prefixed comment lines (Pitfall 2): awk-extracted JSON region has zero comment-style lines
    - Linux path uses `/usr/share/sounds/alsa/Front_Center.wav` (Pitfall 4)
    - File does NOT contain `.oga` or `.ogg`
    - Windows footnote uses softened wording (`easiest path today is to run Claude Code inside WSL`) — NOT `bash/jq/paths break otherwise` (Pitfall 1)
    - "more exist" line names exactly: PreToolUse, PostToolUse, UserPromptSubmit, SessionStart (RESEARCH State of the Art)
    - `grep -c "!" pages/03-hooks/03-how-hooks-work.md` returns `0` (QUAL-04)
  </acceptance_criteria>
  <done>Section 03 file authored — minimal canonical JSON, prose cross-OS notes outside the fence, softened Windows wording, correct Linux .wav path; committed atomically.</done>
</task>

</tasks>

<verification>
- JSON inside ```json fence is the byte-exact canonical D-06 Stop-only block; no matcher; no comments
- Cross-OS notes live OUTSIDE the fence in a dim helper div
- Linux path is `/usr/share/sounds/alsa/Front_Center.wav` (not `.oga`)
- Windows wording softened per Pitfall 1
- Trailing "more exist" line cites only the 4 events from CONTEXT D-05
</verification>

<success_criteria>
HOOK-02 satisfied: mechanics taught with one canonical, copy-safe JSON shape and cross-OS escape hatches.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hooks/02-03-SUMMARY.md`.
</output>
