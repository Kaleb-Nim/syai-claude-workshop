---
phase: 03-hooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slides.md
  - pages/03-hooks.md
  - pages/03-hooks/01-cover.md
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "slides.md includes pages/03-hooks.md via an appended src: block"
    - "pages/03-hooks.md exists as a pure src:-only chapter orchestrator with 5 includes"
    - "pages/03-hooks/01-cover.md renders the chapter cover slide with `layout: center`"
    - "D-01: Phase 3 produces exactly 6 plans = 5 content sections (01-cover, 02-why-hooks, 03-how-hooks-work, 04-hands-on-build, 05-hands-on-fire) + 1 closing build plan"
    - "D-10: section files use Slidev per-slide `---` separator with minimal per-slide frontmatter; no `layout: section` slides at section boundaries within the chapter (only 01-cover gets `layout: center`)"
    - "D-13: tone discipline — confident, terse, no hype, no exclamation marks in body, no screenshots (enforced via `grep -c '!' <file> == 0` across every section file in Plans 02–05)"
  artifacts:
    - path: "slides.md"
      provides: "Deck root orchestrator with Phase 3 include appended"
      contains: "src: ./pages/03-hooks.md"
    - path: "pages/03-hooks.md"
      provides: "Chapter 3 orchestrator — 5 src: includes, no inline content"
      contains: "src: ./03-hooks/01-cover.md"
    - path: "pages/03-hooks/01-cover.md"
      provides: "Chapter 3 cover slide"
      contains: "Chapter 3 — Hooks"
  key_links:
    - from: "slides.md"
      to: "pages/03-hooks.md"
      via: "Slidev src: frontmatter"
      pattern: "src: ./pages/03-hooks.md"
    - from: "pages/03-hooks.md"
      to: "pages/03-hooks/01-cover.md"
      via: "nested src: include (relative to pages/)"
      pattern: "src: ./03-hooks/01-cover.md"
---

<objective>
Establish the Chapter 3 scaffolding so subsequent section plans (02–05) and the closer (06) have a working orchestrator chain. Three artifacts: (1) a single 3-line `src:` block appended to `slides.md` (no headmatter edits), (2) a brand-new `pages/03-hooks.md` chapter orchestrator with five `src:` includes, and (3) the Chapter 3 cover slide `pages/03-hooks/01-cover.md`. Mirrors Phase 1 Plans 01 + 02 exactly.

Purpose: Unblock Wave 2 (Plans 02-05 author the remaining section files in parallel).
Output: 1 modified file + 2 new files. Each in its own atomic commit per QUAL-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hooks/02-CONTEXT.md
@.planning/phases/03-hooks/02-RESEARCH.md
@.planning/phases/03-hooks/02-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 3 include to slides.md (append-only, no headmatter edits)</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (CURRENT — lines 1-30 must stay byte-identical; Phase 1 include block at lines 28-30 is the analog)
    - .planning/phases/03-hooks/02-PATTERNS.md (slides.md MODIFY section — lines 23-65 describe the append-only contract)
    - .planning/phases/03-hooks/02-RESEARCH.md (Pitfall 7 — slides.md headmatter preservation)
  </read_first>
  <action>
Append EXACTLY the following 3 lines at the END of `slides.md` (after the existing Phase 1 include block ending at line 30). Do NOT modify lines 1-30 — headmatter, cover slide, and Phase 1 include block stay byte-identical.

Append this block verbatim:

```markdown
---
src: ./pages/03-hooks.md
---
```

The file's current EOF is line 31 (a single trailing newline after the Phase 1 `---` at line 30). The new block sits immediately after, producing a final structure where the last 6 logical lines are:

```
---
src: ./pages/01-audience-setup.md
---
---
src: ./pages/03-hooks.md
---
```

Atomic commit (QUAL-02): `feat(phase-3): wire chapter 2 src include into slides.md`.
  </action>
  <verify>
    <automated>grep -c "src: ./pages/03-hooks.md" slides.md | grep -q "^1$" && head -n 30 slides.md | diff - <(git show HEAD:slides.md | head -n 30)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src: ./pages/03-hooks.md" slides.md` returns exactly `1`
    - `grep -c "src: ./pages/01-audience-setup.md" slides.md` returns exactly `1` (Phase 1 include preserved)
    - `head -n 30 slides.md` is byte-identical to `git show HEAD:slides.md | head -n 30` (lines 1-30 unchanged)
    - No `vite.config.ts` was created (CLAUDE.md constraint)
  </acceptance_criteria>
  <done>slides.md includes Phase 3 chapter via appended src: block; headmatter and Phase 1 include untouched; committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Create pages/03-hooks.md chapter orchestrator (pure src: includes)</name>
  <files>pages/03-hooks.md</files>
  <read_first>
    - pages/01-audience-setup.md (Phase 1 ANALOG — chapter orchestrator template, ~21 lines, pure src: includes)
    - .planning/phases/03-hooks/02-PATTERNS.md (pages/03-hooks.md section — lines 69-122 specify the full template)
    - .planning/phases/03-hooks/02-CONTEXT.md (D-01 — 5 section files in order)
  </read_first>
  <action>
Create the NEW file `pages/03-hooks.md` with EXACTLY these contents (no deck headmatter — first line is `---`, paths relative to `pages/` per Phase 1 Pitfall 3):

```markdown
---
src: ./03-hooks/01-cover.md
---
---
src: ./03-hooks/02-why-hooks.md
---
---
src: ./03-hooks/03-how-hooks-work.md
---
---
src: ./03-hooks/04-hands-on-build.md
---
---
src: ./03-hooks/05-hands-on-fire.md
---
```

End the file with a trailing newline after the final `---` (mirrors Phase 1 file).

Atomic commit (QUAL-02): `feat(phase-3): add chapter 2 orchestrator pages/03-hooks.md`.
  </action>
  <verify>
    <automated>test -f pages/03-hooks.md && grep -c "^src: \./03-hooks/" pages/03-hooks.md | grep -q "^5$" && head -n 1 pages/03-hooks.md | grep -q "^---$"</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-hooks.md` exists
    - `grep -c "^src: \./03-hooks/" pages/03-hooks.md` returns exactly `5`
    - First line is `---` (no deck headmatter)
    - Each of the 5 src: paths uses `./03-hooks/<file>.md` form (NOT `./pages/03-hooks/...`)
    - `grep -c "src: ./03-hooks/01-cover.md" pages/03-hooks.md` returns `1`
    - `grep -c "src: ./03-hooks/05-hands-on-fire.md" pages/03-hooks.md` returns `1`
  </acceptance_criteria>
  <done>Chapter orchestrator exists with 5 includes in spec order; relative paths correct; committed atomically.</done>
</task>

<task type="auto">
  <name>Task 3: Create pages/03-hooks/01-cover.md (chapter cover slide)</name>
  <files>pages/03-hooks/01-cover.md</files>
  <read_first>
    - pages/01-audience-setup/01-cover.md (Phase 1 ANALOG — 12 lines, the byte-exact template)
    - .planning/phases/03-hooks/02-PATTERNS.md (01-cover.md section — load-bearing conventions: layout, em-dash, middle-dot subtitle)
    - .planning/phases/03-hooks/02-CONTEXT.md (Claude's Discretion — exact cover one-liner is planner choice; tone: confident, terse, no `!`)
  </read_first>
  <action>
Create the new directory `pages/03-hooks/` (if not already created by Task 2's file write) and the NEW file `pages/03-hooks/01-cover.md` with EXACTLY these contents:

```markdown
---
layout: center
---

# Chapter 3 — Hooks

deterministic shell commands wired to Claude's lifecycle — one chime today, the pattern for the rest

<div class="text-sm opacity-60 mt-8">
  why hooks · how they work · one chime you'll wire live
</div>
```

Notes on each element:
- Frontmatter is exactly `layout: center` (3 lines, no other keys) — mirrors Phase 1 `01-cover.md`.
- H1 uses em-dash ` — ` between "Chapter 3" and "Hooks" (NOT a hyphen).
- One-line provocation under H1 is plain lowercase prose, no exclamation mark (QUAL-04).
- Dim subtitle div uses `text-sm opacity-60 mt-8` (Variant 1 in PATTERNS.md) and middle-dot ` · ` separators (NOT bullet/hyphen).
- File ends with newline after `</div>` closing line.

Atomic commit (QUAL-02): `feat(phase-3): add section 01 chapter cover`.
  </action>
  <verify>
    <automated>test -f pages/03-hooks/01-cover.md && grep -q "^layout: center$" pages/03-hooks/01-cover.md && grep -q "^# Chapter 3 — Hooks$" pages/03-hooks/01-cover.md && grep -q "opacity-60 mt-8" pages/03-hooks/01-cover.md && ! grep -q "!" pages/03-hooks/01-cover.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-hooks/01-cover.md` exists
    - Frontmatter contains `layout: center` and no other keys
    - H1 line is exactly `# Chapter 3 — Hooks` (em-dash U+2014, not `-`)
    - Dim subtitle div uses class `text-sm opacity-60 mt-8`
    - Subtitle contains middle-dot ` · ` separators (U+00B7), NOT hyphens
    - `grep -c "!" pages/03-hooks/01-cover.md` returns `0` (QUAL-04 — no exclamation marks in body)
  </acceptance_criteria>
  <done>Chapter 3 cover slide renders centered with the spec title, provocation, and dim subtitle; committed atomically.</done>
</task>

</tasks>

<verification>
- `slides.md` head (lines 1-30) unchanged; appended `src: ./pages/03-hooks.md` block exists once
- `pages/03-hooks.md` is a pure 5-include orchestrator with paths relative to `pages/`
- `pages/03-hooks/01-cover.md` renders the cover with `layout: center`, em-dash title, and middle-dot subtitle
- Three atomic commits scoped to slides.md / orchestrator / cover (QUAL-02)
</verification>

<success_criteria>
Scaffolding ready for parallel Wave 2 section authoring. `bunx slidev build` would still pass at this state (orchestrator's 4 other includes resolve to missing-file errors that Wave 2 plans will create — but the build is run only in Plan 06, after all section files land).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hooks/02-01-SUMMARY.md`.
</output>
