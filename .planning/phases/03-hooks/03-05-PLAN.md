---
phase: 03-hooks
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pages/03-hooks/05-hands-on-fire.md
autonomous: true
requirements: [HOOK-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 05 is a single slide with two bold-prefixed micro-steps and two ✓ Check blockquotes"
    - "Step 1 triggers Stop by running `claude` and asking a quick thing"
    - "Step 2 triggers Notification via a permission-prompt scenario (RESEARCH Pattern 6 — most deterministic trigger)"
    - "Each step is followed by a `> ✓ Check: …` blockquote with the literal Unicode ✓ glyph (U+2713)"
    - "Closing line is the D-09 sign-off line in a dim helper div"
  artifacts:
    - path: "pages/03-hooks/05-hands-on-fire.md"
      provides: "Section 05 — Hands-on fire (trigger Stop + Notification, hear two chimes, ✓ check each)"
      contains: "✓ Check"
  key_links:
    - from: "Step 2 prompt example"
      to: "Notification permission_prompt subtype"
      via: "tool-call attempt outside auto-allow list"
      pattern: "rm -rf|curl|git push"
---

<objective>
Author Section 05 — Hands-on fire. Single slide. Two micro-steps that trigger the Stop and Notification hooks wired in Section 04. Each step gets a `> ✓ Check:` blockquote. Closing dim line on the repeatability of the shape. Satisfies the HOOK-03 fire step.

Purpose: Give participants the audible payoff — two chimes, two events, same shape.
Output: One new file, single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hooks/02-CONTEXT.md
@.planning/phases/03-hooks/02-RESEARCH.md
@.planning/phases/03-hooks/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Section 05 (single slide — two steps + two ✓ Checks + closing line)</name>
  <files>pages/03-hooks/05-hands-on-fire.md</files>
  <read_first>
    - pages/01-audience-setup/06-win-statusline.md (Phase 1 ANALOG — ✓ Check wrapped variant; line 16-18)
    - pages/01-audience-setup/07-win-claude-md.md (Phase 1 ANALOG — ✓ Check single-line variant; line 35)
    - .planning/phases/03-hooks/02-CONTEXT.md (D-09 — two micro-steps, ✓ checks, closing line)
    - .planning/phases/03-hooks/02-RESEARCH.md (Pattern 6 — recommended whole-file content; Notification trigger choice = permission_prompt; A2/A3 — most deterministic for 30-second hands-on window)
    - .planning/phases/03-hooks/02-PATTERNS.md (05-hands-on-fire.md section; Pattern C ✓ Check; Pattern F bold inline step prefix)
  </read_first>
  <action>
Create the NEW file `pages/03-hooks/05-hands-on-fire.md` with EXACTLY these contents. No frontmatter. Use the literal Unicode `✓` glyph (U+2713), not `&check;` or `✓`. The Notification trigger is a permission-prompt scenario (per RESEARCH Pattern 6 — most deterministic; the Section 04 JSON omits matcher so the hook fires regardless of subtype).

```markdown
# Fire it

**Step 1 — Trigger `Stop`:** run `claude`, ask a quick thing, wait for it to finish.

> ✓ Check: you hear a chime when Claude finishes.

**Step 2 — Trigger `Notification`:** ask Claude to run something it needs permission for (e.g. `run rm -rf /tmp/foo`).

> ✓ Check: you hear a *different* chime when Claude asks for input.

<div class="text-dim mt-6">
  Two events, same shape. Add more later — that's the whole pattern.
</div>
```

Element-by-element notes:
- **No frontmatter** — line 1 is `# Fire it`.
- **H1** is intentionally short — two words. Mirrors Phase 1's clipped imperative style (`Win 1: …`, `Win 2: …`).
- **Bold inline step prefix** (Pattern F): `**Step 1 — Trigger \`Stop\`:**` — em-dash between "Step 1" and "Trigger", inline-coded event name, colon-space-then-prose continues on same line. Same for Step 2.
- **Step 1 prose**: `run \`claude\`, ask a quick thing, wait for it to finish.` (inline-coded `claude` command; terse).
- **✓ Check 1** (single-line variant): `> ✓ Check: you hear a chime when Claude finishes.`
- **Step 2 prose**: triggers `Notification` via a permission-prompt scenario. Example command intentionally chosen as `rm -rf /tmp/foo` because (a) it surfaces a permission prompt deterministically (`rm` is not in the default auto-allow list per RESEARCH A2), (b) `/tmp/foo` is a non-existent disposable path, (c) it reads as obviously safe in a workshop room. The `e.g.` framing makes clear it's one example, not a required command.
- **✓ Check 2** (with italic emphasis): `> ✓ Check: you hear a *different* chime when Claude asks for input.` Markdown `*different*` renders italic — matches RESEARCH Pattern 6.
- **Both ✓ Checks** use the literal `✓` glyph (U+2713). Prefix is exactly `> ✓ Check: ` (greater-than, space, check glyph, space, "Check:", space).
- **Closing dim line** uses Variant 2 bare `text-dim mt-6`: `Two events, same shape. Add more later — that's the whole pattern.` (verbatim D-09).
- **No exclamation marks** (QUAL-04). No screenshots. Plain markdown only.

Atomic commit (QUAL-02): `feat(phase-3): section 05 hands-on fire (trigger Stop + Notification, ✓ checks)`.
  </action>
  <verify>
    <automated>test -f pages/03-hooks/05-hands-on-fire.md && grep -q '^# Fire it$' pages/03-hooks/05-hands-on-fire.md && [ "$(grep -c '^> ✓ Check: ' pages/03-hooks/05-hands-on-fire.md)" = "2" ] && grep -Fxq "> ✓ Check: you hear a chime when Claude finishes." pages/03-hooks/05-hands-on-fire.md && grep -Fxq "> ✓ Check: you hear a *different* chime when Claude asks for input." pages/03-hooks/05-hands-on-fire.md && grep -q "\*\*Step 1 — Trigger \`Stop\`:\*\*" pages/03-hooks/05-hands-on-fire.md && grep -q "\*\*Step 2 — Trigger \`Notification\`:\*\*" pages/03-hooks/05-hands-on-fire.md && grep -Fq "Two events, same shape. Add more later — that's the whole pattern." pages/03-hooks/05-hands-on-fire.md && [ "$(grep -c '!' pages/03-hooks/05-hands-on-fire.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-hooks/05-hands-on-fire.md` exists
    - Line 1 is `# Fire it` (no frontmatter)
    - File contains exactly two `> ✓ Check: ` blockquote lines (literal U+2713 glyph)
    - First ✓ Check is verbatim: `> ✓ Check: you hear a chime when Claude finishes.`
    - Second ✓ Check is verbatim: `> ✓ Check: you hear a *different* chime when Claude asks for input.`
    - Step 1 bold prefix `**Step 1 — Trigger \`Stop\`:**` present
    - Step 2 bold prefix `**Step 2 — Trigger \`Notification\`:**` present
    - Step 2 prose mentions a permission scenario (e.g. `rm -rf /tmp/foo`) — Notification trigger per RESEARCH Pattern 6
    - Closing line `Two events, same shape. Add more later — that's the whole pattern.` appears verbatim
    - `grep -c "!" pages/03-hooks/05-hands-on-fire.md` returns `0` (QUAL-04)
    - No Vue components (`<Check>`, `<v-clicks>`, etc.) used
  </acceptance_criteria>
  <done>Section 05 file authored with two micro-steps + two ✓ Checks + closing line; committed atomically.</done>
</task>

</tasks>

<verification>
- Two ✓ Check blockquotes with literal Unicode ✓
- Step 2 triggers Notification via permission-prompt scenario
- Closing D-09 sign-off line present verbatim
- No exclamation marks, no screenshots, no custom components (QUAL-04)
</verification>

<success_criteria>
HOOK-03 (fire step) satisfied: participants trigger both events, hear two chimes, ✓ each. Chapter content complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hooks/02-05-SUMMARY.md`.
</output>
