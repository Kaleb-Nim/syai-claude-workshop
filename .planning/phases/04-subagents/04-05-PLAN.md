---
phase: 04-subagents
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/05-showcase.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Side-by-side 3-up grid showing frontmatter for python-pro, quant-analyst, market-researcher (D-06)"
    - "All three subagent frontmatters verbatim from VoltAgent registry (research-verified file URLs in 04-RESEARCH.md)"
    - "Tool/model contrast is visible — python-pro (Bash+Sonnet), quant-analyst (Bash+Opus), market-researcher (read-only+web+Sonnet)"
    - "Slide explicitly surfaces 'subagents aren't just for coding' (D-06 rationale)"
    - "Quant-analyst long description truncated for layout with footnote (research recommendation Path A)"
  artifacts:
    - path: "pages/04-subagents/05-showcase.md"
      provides: "3-up frontmatter showcase across diverse domains"
      contains: "name: python-pro"
---

<objective>
Author `pages/04-subagents/05-showcase.md` — Section 05 of Chapter 4. The proof-by-example slide: three real VoltAgent subagent frontmatters side-by-side (D-06 LOCKED domains). Uses the Phase 3 Section 03 `grid grid-cols-3` pattern with `<style scoped>` font-shrink. Quant-analyst description truncated to first sentence with a footnote (research Open Q4 / Assumption A5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/05-showcase.md with 3-up frontmatter grid (python-pro / quant-analyst / market-researcher)</name>
  <files>pages/04-subagents/05-showcase.md</files>
  <read_first>
    - pages/03-hooks/03-how-hooks-work.md (Phase 3 ANALOG — grid-cols-3 3-up code grid + style scoped font-shrink — copy structural shape)
    - .planning/phases/04-subagents/04-RESEARCH.md "Code Examples" Examples 2-4 (the three verbatim VoltAgent frontmatters — copy byte-for-byte; verified via curl 2026-05-13)
    - .planning/phases/04-subagents/04-RESEARCH.md "Architecture Patterns" Pattern 3 (the 3-up grid layout shape with example markup)
    - .planning/phases/04-subagents/04-RESEARCH.md "Assumptions Log" A5 (quant-analyst truncation acceptable; first-sentence truncation with footnote recommended)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-06 — broad-domain mix; subagents aren't just for coding)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/05-showcase.md` with EXACTLY these contents. Two slides: framing + 3-up grid. The grid uses yaml fences (Slidev's bundled Shiki handles YAML — confirmed by 04-RESEARCH.md "Supporting" table). Quant-analyst description is TRUNCATED to its first sentence (ending at the first period after "portfolios") plus an ellipsis — full description is in the source file linked in the footnote.

Use these exact contents (a single fenced markdown block follows — write its inner contents to the file as-is, do not nest fences):

````markdown
# Three subagents, three domains

The point of the file shape is that it doesn't care what the subagent does. Same template, very different jobs.

---

# Three real ones, side by side

<div class="grid grid-cols-3 gap-3 mt-2">

<div>

**python-pro**

```yaml
---
name: python-pro
description: "Use this agent when you need to build type-safe, production-ready Python code for web APIs, system utilities, or complex applications requiring modern async patterns and extensive type coverage."
tools: Read, Write, Edit, Bash, Glob, Grep
model: sonnet
---
```

</div>

<div>

**quant-analyst**

```yaml
---
name: quant-analyst
description: "Use this agent when you need to develop quantitative trading strategies, build financial models with rigorous mathematical foundations, or conduct advanced risk analytics for derivatives and portfolios..."
tools: Read, Write, Edit, Bash, Glob, Grep
model: opus
---
```

</div>

<div>

**market-researcher**

```yaml
---
name: market-researcher
description: "Use this agent when you need to analyze markets, understand consumer behavior, assess competitive landscapes, and size opportunities to inform business strategy and market entry decisions."
tools: Read, Grep, Glob, WebFetch, WebSearch
model: sonnet
---
```

</div>

</div>

<div class="text-dim text-xs mt-3">
  one SWE specialist, one quant, one researcher · <code>tools</code> and <code>model</code> shift per job — same shape
</div>

<div class="text-dim text-xs mt-1">
  <code>quant-analyst</code> description truncated for layout · full text + source files: github.com/VoltAgent/awesome-claude-code-subagents
</div>

<style scoped>
pre, code {
  font-size: 0.5rem !important;
  line-height: 1.15 !important;
}
.slidev-code {
  padding: 0.35em 0.55em !important;
}
h1 {
  margin-bottom: 0.3em !important;
}
</style>
````

Notes on load-bearing details:
- Slide 1 ("Three subagents, three domains"): one-line framing that primes D-06's "not just for coding" beat without spelling it; the grid does the work.
- Slide 2 ("Three real ones, side by side"): 3-up grid identical in structure to Phase 3 Section 03 (`grid grid-cols-3 gap-3 mt-2`). Each column has a bold name above the YAML fence.
- python-pro frontmatter VERBATIM from 04-RESEARCH.md Example 2 (lines 384-391). Description is one sentence, fits as-is.
- quant-analyst frontmatter: TRUNCATED description — keep first sentence ending with "portfolios" plus an ellipsis. Tools and model lines verbatim. `model: opus` (not sonnet — important contrast point).
- market-researcher frontmatter VERBATIM from 04-RESEARCH.md Example 4 (lines 410-417). Description fits as-is. Tools `Read, Grep, Glob, WebFetch, WebSearch` — note NO Bash/Write/Edit; this is the read-only+web contrast.
- First dim footnote surfaces the teaching point: tools+model shift per job, shape stays.
- Second dim footnote flags the quant-analyst truncation and points to the source repo (per A5 — reversible if user wants no truncation).
- `<style scoped>` font-shrink block byte-identical to Phase 3 Section 03's (font-size: 0.5rem, line-height: 1.15, padding: 0.35em 0.55em, h1 margin-bottom: 0.3em).
- No exclamation marks. No SDK references.

Atomic commit (QUAL-02): `feat(phase-4): add section 05 showcase grid`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/05-showcase.md && grep -q "name: python-pro" pages/04-subagents/05-showcase.md && grep -q "name: quant-analyst" pages/04-subagents/05-showcase.md && grep -q "name: market-researcher" pages/04-subagents/05-showcase.md && grep -q "model: opus" pages/04-subagents/05-showcase.md && grep -q "WebFetch, WebSearch" pages/04-subagents/05-showcase.md && grep -q "grid grid-cols-3 gap-3 mt-2" pages/04-subagents/05-showcase.md && grep -q "font-size: 0.5rem" pages/04-subagents/05-showcase.md && grep -q "truncated for layout" pages/04-subagents/05-showcase.md && grep -q "VoltAgent/awesome-claude-code-subagents" pages/04-subagents/05-showcase.md && [ "$(grep -v '^#' pages/04-subagents/05-showcase.md | grep -c '!')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/05-showcase.md` exists
    - All three frontmatter `name:` lines present verbatim: `name: python-pro`, `name: quant-analyst`, `name: market-researcher`
    - Model contrast visible: `model: opus` appears (quant-analyst only) AND `model: sonnet` appears
    - Tool-restriction contrast visible: market-researcher line `tools: Read, Grep, Glob, WebFetch, WebSearch` present (no Bash/Write/Edit)
    - 3-up grid wrapper present: `grid grid-cols-3 gap-3 mt-2`
    - Font-shrink `<style scoped>` block present: substring `font-size: 0.5rem` appears
    - Truncation footnote present: substring `truncated for layout`
    - Source citation present: `VoltAgent/awesome-claude-code-subagents`
    - `grep -v '^#' file | grep -c "!"` returns `0` (no exclamation marks in body)
    - No occurrence of `Agent SDK` or `query(` (D-09)
  </acceptance_criteria>
  <done>Section 05 ships the 3-up frontmatter comparison; tool/model/domain contrast all visible on one slide; QUAL-02 atomic commit.</done>
</task>

</tasks>

<verification>
- All three VoltAgent frontmatters verbatim (truncation only on quant-analyst description, flagged).
- Tool + model contrast lines present.
- Font-shrink style scoped block present.
- No SDK content; no `!` in body.
</verification>

<success_criteria>
Section 05 proves D-06's broad-domain-mix point: same template, three different jobs (SWE, quant, research) — one slide carries the load.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-05-SUMMARY.md`.
</output>
