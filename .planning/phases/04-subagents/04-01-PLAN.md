---
phase: 04-subagents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slides.md
  - pages/04-subagents.md
  - pages/04-subagents/01-cover.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "slides.md includes pages/04-subagents.md via an appended src: block, positioned after the Phase 3 include and BEFORE the Phase 99 resources include"
    - "pages/04-subagents.md exists as a pure src:-only chapter orchestrator with 7 includes in the locked D-01 order"
    - "pages/04-subagents/01-cover.md renders the chapter cover slide with `layout: center`, em-dash title, middle-dot subtitle"
    - "D-01: Phase 4 produces exactly 9 plans = 7 content sections + 1 chapter scaffolding (this plan) merged with Section 01 cover + 1 closing build/admin plan"
    - "D-09: phase is renamed 'Subagents' (NOT 'Subagents + Agent SDK'); no SDK content anywhere in Chapter 4"
    - "QUAL-04 tone: confident, terse, no exclamation marks, no screenshots"
  artifacts:
    - path: "slides.md"
      provides: "Deck root orchestrator with Phase 4 include appended before Phase 99"
      contains: "src: ./pages/04-subagents.md"
    - path: "pages/04-subagents.md"
      provides: "Chapter 4 orchestrator — 7 src: includes, no inline content"
      contains: "src: ./04-subagents/01-cover.md"
    - path: "pages/04-subagents/01-cover.md"
      provides: "Chapter 4 cover slide"
      contains: "Chapter 4 — Subagents"
  key_links:
    - from: "slides.md"
      to: "pages/04-subagents.md"
      via: "Slidev src: frontmatter (positioned BEFORE the 99-resources include)"
      pattern: "src: ./pages/04-subagents.md"
    - from: "pages/04-subagents.md"
      to: "pages/04-subagents/01-cover.md"
      via: "nested src: include (relative to pages/)"
      pattern: "src: ./04-subagents/01-cover.md"
---

<objective>
Establish the Chapter 4 scaffolding so Wave 2 section plans (02–08) can author the 6 remaining section files in parallel. Three artifacts:
1. Insert `src: ./pages/04-subagents.md` into `slides.md` immediately AFTER the existing Phase 3 include and BEFORE the Phase 99 resources include (Chapter 4 must precede Resources in deck order).
2. Create new `pages/04-subagents.md` chapter orchestrator with 7 `src:` includes in the D-01 locked order.
3. Create `pages/04-subagents/01-cover.md` chapter cover slide.

Mirrors Phase 3 Plan 01 byte-for-byte where possible, extended for the 7-section structure.

Purpose: Unblock Wave 2 (Plans 02–08 author the remaining section files in parallel — each owns its own file, no shared writes).
Output: 1 modified file + 2 new files. Atomic commits per QUAL-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Phase 4 include into slides.md BEFORE the Phase 99 resources include</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (CURRENT — lines 41-52 contain the four existing chapter includes; Phase 99 resources include sits at lines 50-52 and MUST end up last)
    - .planning/phases/04-subagents/04-CONTEXT.md (Integration Points section — slides.md needs the include AFTER Phase 3, BEFORE Phase 99)
    - .planning/phases/03-hooks/03-01-PLAN.md Task 1 (Phase 3 ANALOG — append-only pattern; this plan diverges because resources page now sits at the tail)
  </read_first>
  <action>
The current `slides.md` ends with these 4 chapter includes (lines 41-52):

```markdown
---
src: ./pages/01-audience-setup.md
---
---
src: ./pages/02-skills.md
---
---
src: ./pages/03-hooks.md
---
---
src: ./pages/99-resources.md
---
```

INSERT the Phase 4 include block between the Phase 3 include and the Phase 99 resources include. After the edit the last 14 logical lines must read EXACTLY:

```markdown
---
src: ./pages/01-audience-setup.md
---
---
src: ./pages/02-skills.md
---
---
src: ./pages/03-hooks.md
---
---
src: ./pages/04-subagents.md
---
---
src: ./pages/99-resources.md
---
```

Do NOT modify lines 1-40 (headmatter and cover slide stay byte-identical). Do NOT touch the four existing include blocks except to insert the new block in the correct position.

Atomic commit (QUAL-02): `feat(phase-4): wire chapter 4 src include into slides.md`.
  </action>
  <verify>
    <automated>grep -c "src: ./pages/04-subagents.md" slides.md | grep -q "^1$" && grep -n "src: ./pages/" slides.md | awk -F: '{print $1}' | paste -sd, - | grep -q "^[0-9]*,[0-9]*,[0-9]*,[0-9]*,[0-9]*$" && python3 -c "import re;s=open('slides.md').read();order=[m.group(1) for m in re.finditer(r'src: \./pages/([^.]+)\.md', s)];print(order);assert order==['01-audience-setup','02-skills','03-hooks','04-subagents','99-resources'],order"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src: ./pages/04-subagents.md" slides.md` returns exactly `1`
    - `grep -c "src: ./pages/99-resources.md" slides.md` returns exactly `1` (resources include preserved)
    - The order of chapter includes in `slides.md` is exactly: `01-audience-setup`, `02-skills`, `03-hooks`, `04-subagents`, `99-resources` (Phase 4 sits between hooks and resources)
    - `head -n 40 slides.md` is byte-identical to `git show HEAD:slides.md | head -n 40` (headmatter + cover slide unchanged)
    - No `vite.config.ts` created (CLAUDE.md constraint)
  </acceptance_criteria>
  <done>slides.md includes Phase 4 between Phase 3 and Phase 99; headmatter + cover untouched; committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Create pages/04-subagents.md chapter orchestrator (7 src: includes)</name>
  <files>pages/04-subagents.md</files>
  <read_first>
    - pages/03-hooks.md (Phase 3 ANALOG — pure src:-only orchestrator, 5 includes, relative paths)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-01 — locked 7 section filenames in order)
    - .planning/phases/04-subagents/04-RESEARCH.md ("Recommended Project Structure" — confirms the 7 filenames)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents.md` with EXACTLY these contents (no deck headmatter — first line is `---`, paths relative to `pages/` per the Phase 3 pattern):

```markdown
---
src: ./04-subagents/01-cover.md
---
---
src: ./04-subagents/02-skills-vs-subagents.md
---
---
src: ./04-subagents/03-why-subagents.md
---
---
src: ./04-subagents/04-how-subagents-work.md
---
---
src: ./04-subagents/05-showcase.md
---
---
src: ./04-subagents/06-hands-on-build.md
---
---
src: ./04-subagents/07-hands-on-fire.md
---
```

End the file with a trailing newline after the final `---`.

Atomic commit (QUAL-02): `feat(phase-4): add chapter 4 orchestrator pages/04-subagents.md`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents.md && grep -c "^src: \./04-subagents/" pages/04-subagents.md | grep -q "^7$" && head -n 1 pages/04-subagents.md | grep -q "^---$" && grep -q "01-cover.md" pages/04-subagents.md && grep -q "02-skills-vs-subagents.md" pages/04-subagents.md && grep -q "07-hands-on-fire.md" pages/04-subagents.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents.md` exists
    - `grep -c "^src: \./04-subagents/" pages/04-subagents.md` returns exactly `7`
    - First line is `---` (no deck headmatter)
    - Each of the 7 src: paths uses `./04-subagents/<file>.md` form (NOT `./pages/04-subagents/...`)
    - The 7 filenames in order are: `01-cover.md`, `02-skills-vs-subagents.md`, `03-why-subagents.md`, `04-how-subagents-work.md`, `05-showcase.md`, `06-hands-on-build.md`, `07-hands-on-fire.md`
  </acceptance_criteria>
  <done>Chapter orchestrator exists with 7 includes in D-01 spec order; relative paths correct; committed atomically.</done>
</task>

<task type="auto">
  <name>Task 3: Create pages/04-subagents/01-cover.md (chapter cover slide)</name>
  <files>pages/04-subagents/01-cover.md</files>
  <read_first>
    - pages/03-hooks/01-cover.md (Phase 3 ANALOG — 12 lines, the byte-exact template; em-dash title, middle-dot subtitle, `layout: center`)
    - .planning/phases/04-subagents/04-CONTEXT.md (Claude's Discretion — exact cover one-liner is planner choice; D-04 frames "context preservation"; D-06 frames "broad domain mix")
    - .planning/phases/04-subagents/04-RESEARCH.md (Summary — hands-on is `commit-message-writer`, showcase covers python-pro / quant-analyst / market-researcher)
  </read_first>
  <action>
Create the new directory `pages/04-subagents/` (the file write below will create it) and the NEW file `pages/04-subagents/01-cover.md` with EXACTLY these contents:

```markdown
---
layout: center
---

# Chapter 4 — Subagents

isolated workers Claude can delegate to — own context, own tools, one summary back to you

<div class="text-sm opacity-60 mt-8">
  skills vs subagents · why isolate context · build one live with /agents
</div>
```

Notes on each element (load-bearing per Phase 3 conventions):
- Frontmatter is exactly `layout: center` (3 lines, no other keys) — mirrors Phase 3 `01-cover.md`.
- H1 uses em-dash ` — ` between "Chapter 4" and "Subagents" (U+2014, NOT a hyphen).
- One-line provocation under H1 is plain lowercase prose, no exclamation mark (QUAL-04). Anchored on D-04 "context preservation" framing — surfaces the chapter's practical anchor without naming SDK.
- Dim subtitle div uses `text-sm opacity-60 mt-8` and middle-dot ` · ` separators (U+00B7), NOT bullets/hyphens. Three beats: framing (Section 02), why (Section 03), hands-on (Sections 06/07).
- File ends with newline after `</div>`.

Atomic commit (QUAL-02): `feat(phase-4): add section 01 chapter cover`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/01-cover.md && grep -q "^layout: center$" pages/04-subagents/01-cover.md && grep -q "^# Chapter 4 — Subagents$" pages/04-subagents/01-cover.md && grep -q "opacity-60 mt-8" pages/04-subagents/01-cover.md && grep -q " · " pages/04-subagents/01-cover.md && [ "$(grep -v '^#' pages/04-subagents/01-cover.md | grep -c '!')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/01-cover.md` exists
    - Frontmatter contains `layout: center` and no other keys
    - H1 line is exactly `# Chapter 4 — Subagents` (em-dash U+2014, not `-`)
    - Dim subtitle div uses class `text-sm opacity-60 mt-8`
    - Subtitle contains middle-dot ` · ` separators (U+00B7), NOT hyphens
    - `grep -v '^#' pages/04-subagents/01-cover.md | grep -c "!"` returns `0` (QUAL-04 — no exclamation marks in body; H1/header lines excluded from check to avoid markdown header collisions)
    - File contains NO mention of "SDK", "Agent SDK", or "query(" (D-09)
  </acceptance_criteria>
  <done>Chapter 4 cover slide renders centered with em-dash title, context-preservation provocation, and 3-beat dim subtitle; committed atomically.</done>
</task>

</tasks>

<verification>
- `slides.md` includes Phase 4 in the correct position (after Phase 3, before Phase 99); headmatter unchanged.
- `pages/04-subagents.md` is a pure 7-include orchestrator with paths relative to `pages/` in D-01 order.
- `pages/04-subagents/01-cover.md` renders the cover with `layout: center`, em-dash title, middle-dot subtitle, no exclamation marks, no SDK references.
- Three atomic commits scoped to slides.md / orchestrator / cover (QUAL-02).
</verification>

<success_criteria>
Scaffolding ready for parallel Wave 2 section authoring. `bunx slidev build` would fail at this state (orchestrator's other 6 includes resolve to missing-file errors that Wave 2 plans will create) — build is run only in Plan 09, after all section files land.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-01-SUMMARY.md`.
</output>
