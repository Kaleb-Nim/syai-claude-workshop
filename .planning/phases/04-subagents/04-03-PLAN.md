---
phase: 04-subagents
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/03-why-subagents.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 03 anchors on context preservation — the practical 'why you'd reach for one' (D-04)"
    - "The phrase 'isolated context window' appears at least once (per D-04 explicit beat)"
    - "The phrase 'context bloat' is raised explicitly (per D-04)"
    - "At least one concrete example of a context-heavy task is named (code review across many files, research synthesis, log analysis — researcher seeds in 04-RESEARCH.md)"
    - "Section is distinct from Section 02's mental-model framing — practical anchor, not cookbook contrast"
  artifacts:
    - path: "pages/04-subagents/03-why-subagents.md"
      provides: "Why subagents — context preservation framing"
      contains: "isolated context window"
---

<objective>
Author `pages/04-subagents/03-why-subagents.md` — Section 03 of Chapter 4. Carries the practical motivator (D-04): subagents exist because they get their own isolated context window, which prevents the main chat from filling up with diff dumps, log spew, and multi-file scans. Raises "context bloat" explicitly. Distinct beat from Section 02's mental-model framing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/03-why-subagents.md with context-preservation framing + concrete examples</name>
  <files>pages/04-subagents/03-why-subagents.md</files>
  <read_first>
    - pages/03-hooks/02-why-hooks.md (Phase 3 ANALOG — first two slides are framing prose, not quotes; same per-slide `---` separator pattern; this section follows that shape — no locked quotes here, framing is in workshop voice)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-04 — locked beat: isolated context window, raise context bloat explicitly)
    - .planning/phases/04-subagents/04-RESEARCH.md "Code Examples" Example 6 (commit-message-writer payoff — diff stays in subagent window, one-line message returns; useful concrete framing)
    - .planning/phases/04-subagents/04-RESEARCH.md "Summary" — confirms the context-preservation framing is the chapter's practical anchor
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/03-why-subagents.md` with EXACTLY these contents. Four slides, separated by `---`. Framing is in workshop voice (no verbatim quote constraint applies here — D-03 was Section 02 only):

```markdown
# Your main chat is finite

Every diff dump, every log spew, every multi-file scan piles into the same window. The window fills. Claude starts forgetting earlier turns. Output quality drops.

That's context bloat.

<div class="text-dim text-sm mt-6">
  the chat you're holding is the most expensive resource in the loop
</div>

---

# Subagents get their own window

A subagent runs in an isolated context window. Tool calls, intermediate reasoning, file contents — all of it lives inside the subagent, not your main chat.

Only the final summary returns to you.

---

# Where this pays off

- **Code review across many files** — the subagent reads 20 files; you see one review.
- **Research synthesis across many docs** — the subagent skims a folder of notes; you see the conclusion.
- **Log or diff analysis** — the subagent reads thousands of lines; you see the one-line takeaway.

<div class="text-dim text-sm mt-6">
  in every case, the heavy reading happens somewhere your main chat never has to load
</div>

---

# The trade

You give up live visibility into the subagent's intermediate steps. You get back a clean main chat and consistent quality across long sessions.

That's the deal. Section 04 is how you actually wire one.
```

Notes on load-bearing details:
- Slide 1 ("Your main chat is finite"): explicitly names "context bloat" (D-04 mandatory). Concrete failure mode (forgetting earlier turns, output quality drops) — not abstract.
- Slide 2 ("Subagents get their own window"): contains the verbatim phrase `isolated context window` (D-04 mandatory; also the substring participants saw verbatim in Section 02 Slide 3, deliberate echo).
- Slide 3 ("Where this pays off"): three concrete examples from 04-RESEARCH.md framing — code review, research synthesis, log/diff. The commit-message-writer hands-on in Sections 06/07 will demonstrate exactly the diff-analysis pattern, so the slide seeds that payoff without spoiling.
- Slide 4 ("The trade"): names the tradeoff explicitly. Closes with a one-line forward-reference to Section 04. Tone: terse, no marketing language.
- Per-slide `---` separators on their own lines.
- No exclamation marks anywhere in the body.
- No mention of SDK, Agent SDK, query() (D-09).

Atomic commit (QUAL-02): `feat(phase-4): add section 03 why-subagents context preservation`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/03-why-subagents.md && grep -q "isolated context window" pages/04-subagents/03-why-subagents.md && grep -q "context bloat" pages/04-subagents/03-why-subagents.md && grep -q "Code review across many files" pages/04-subagents/03-why-subagents.md && [ "$(grep -v '^#' pages/04-subagents/03-why-subagents.md | grep -c '!')" = "0" ] && ! grep -qi "SDK\|query(" pages/04-subagents/03-why-subagents.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/03-why-subagents.md` exists
    - Substring `isolated context window` appears at least once
    - Substring `context bloat` appears at least once
    - At least one concrete heavy-context example named (the verifier checks `Code review across many files` per the prescribed action text; research/log/diff examples also present)
    - File contains at least 3 `---` per-slide separators (4 slides total)
    - `grep -v '^#' file | grep -c "!"` returns `0` (no exclamation marks in body; H1s excluded)
    - No occurrence of `SDK` (case-insensitive) or `query(` (D-09)
  </acceptance_criteria>
  <done>Section 03 motivates subagents on the practical axis Section 02 left open — own window, clean main chat, named payoffs.</done>
</task>

</tasks>

<verification>
- File exists with the four required slides.
- Both D-04 mandatory phrases (`isolated context window`, `context bloat`) present.
- At least one concrete context-heavy example named.
- No exclamation marks in body; no SDK content.
</verification>

<success_criteria>
Section 03 carries the practical "why" for subagents distinct from Section 02's mental-model framing. Wires forward to Section 04 (how) without spoiling Sections 06/07 (build/fire).
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-03-SUMMARY.md`.
</output>
