---
phase: 04-subagents
plan: 08
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06, 04-07]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - pages/99-resources/01-resources.md
autonomous: false
requirements: [SUB-01, SUB-03, QUAL-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "`bunx slidev build` exits 0 with all 7 Phase 4 section files wired (QUAL-01)"
    - "Build artifact `dist/index.html` exists after the build"
    - "Presenter mode renders Chapter 4 from cover through Section 07 ✓ checks without console errors (human-verified)"
    - "Section 05 3-up grid fits within the slide viewport at presenter resolution (or has had Phase 3 font-shrink retrofitted)"
    - "ROADMAP.md: Phase 4 renamed from 'Subagents + Agent SDK' to 'Subagents' in all 3 occurrences (lines 25, 77, 109); Goal updated to drop SDK; SUB-02 dropped from requirements list (D-09 / CONTEXT.md Action Item 1)"
    - "REQUIREMENTS.md: SUB-02 marked closed-as-out-of-scope for v1.1 — not deferred, explicitly dropped (D-09 / CONTEXT.md Action Item 2)"
    - "STATE.md: current focus updated to reflect Phase 4 work + correct phase name (CONTEXT.md Action Item 3)"
    - "pages/99-resources/01-resources.md: dev.to article + VoltAgent repo + Claude Code subagents docs added under appropriate headings (RESEARCH Open Q3 — add URLs to resources page during this plan)"
  artifacts:
    - path: "dist/index.html"
      provides: "Static SPA build output (QUAL-01 build success indicator)"
      contains: "<!DOCTYPE html>"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with Phase 4 renamed and SUB-02 dropped"
      contains: "Phase 4: Subagents"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements with SUB-02 marked closed-as-out-of-scope"
      contains: "closed-as-out-of-scope"
  key_links:
    - from: "bunx slidev build"
      to: "dist/index.html"
      via: "Slidev → Vite SPA pipeline"
      pattern: "bunx slidev build"
---

<objective>
Close Chapter 4 with the QUAL-01 build + presenter spot-check gate AND execute the CONTEXT.md D-09 action items (rename phase, drop SUB-02, update STATE.md). Also append research-validated source URLs to `pages/99-resources/01-resources.md` (Open Question 3 — instructor-facing reference). Mirrors Phase 3 Plan 06 structure with admin updates folded in.

Five sub-tasks:
1. Update ROADMAP.md (rename Phase 4, drop SDK references, drop SUB-02 from requirements list).
2. Update REQUIREMENTS.md (mark SUB-02 closed-as-out-of-scope; mark SUB-01, SUB-03 complete).
3. Append source URLs to pages/99-resources/01-resources.md (RESEARCH Open Q3).
4. Run `bunx slidev build` and confirm exit 0 + dist/index.html.
5. Human presenter spot-check Chapter 4 + optional Section 05 font-shrink retrofit if overflow.
6. Update STATE.md at session end.

Because the spot-check is human-driven, this plan is `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
@.planning/phases/03-hooks/03-06-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md — rename Phase 4 (drop SDK), drop SUB-02 from requirements list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (CURRENT — lines 25, 77-83, 109 contain the three "Subagents + Agent SDK" occurrences and the Phase 4 details block)
    - .planning/phases/04-subagents/04-CONTEXT.md (Action Item 1 — exact rename + Goal/Success Criteria rewrite mandate; D-09 — SUB-02 dropped not deferred)
  </read_first>
  <action>
Apply EXACTLY these three textual changes to `.planning/ROADMAP.md`:

**Change 1 — line 25 (milestone phase list entry):**

Before:
```
- [ ] **Phase 4: Subagents + Agent SDK** — Teach subagents in Claude Code, bridge to the programmatic SDK, with one small hands-on task.
```

After:
```
- [ ] **Phase 4: Subagents** — Teach subagents in Claude Code: what they are, why isolated context matters, build one live with `/agents`.
```

**Change 2 — lines 77-83 (Phase 4 details block):**

Before:
```
### Phase 4: Subagents + Agent SDK
**Goal**: A reader opening `pages/04-subagents-sdk.md` understands subagents in Claude Code, sees the same primitive bridge to the programmatic Agent SDK, and walks through one small live hands-on task.
**Depends on**: Phase 3.
**File**: `pages/04-subagents-sdk.md`
**Requirements**: SUB-01, SUB-02, SUB-03
**Success Criteria**: Derived during `/gsd-plan-phase 4` after research. Generic shape: what-is-a-subagent (Task tool + `.claude/agents/*.md`) + bridge-to-SDK + one small live hands-on task.
**Plans**: TBD via `/gsd-plan-phase 4`.
```

After:
```
### Phase 4: Subagents
**Goal**: A reader opening `pages/04-subagents.md` understands subagents in Claude Code — what they are (`.claude/agents/<name>.md` file shape + Task/Agent tool delegation), why they exist (isolated context windows), and walks through one small live hands-on task building a subagent via the `/agents` slash command.
**Depends on**: Phase 3.
**File**: `pages/04-subagents.md`
**Requirements**: SUB-01, SUB-03
**Success Criteria**: 7 section files under `pages/04-subagents/` covering: cover, skills-vs-subagents framing (verbatim dev.to quotes), why-subagents (context preservation), how-subagents-work (file shape + invocation modes), 3-up showcase (python-pro / quant-analyst / market-researcher), hands-on build via `/agents` (commit-message-writer), hands-on fire (invoke + observe isolated context).
**Plans:** 8 plans
- [ ] 04-01-PLAN.md — scaffolding (slides.md include + pages/04-subagents.md orchestrator + section 01 cover)
- [ ] 04-02-PLAN.md — section 02 skills-vs-subagents (cookbook framing + 3 verbatim dev.to quotes)
- [ ] 04-03-PLAN.md — section 03 why-subagents (context preservation framing, D-04)
- [ ] 04-04-PLAN.md — section 04 how-subagents-work (file shape + VoltAgent template + invocation modes, SUB-01)
- [ ] 04-05-PLAN.md — section 05 showcase (3-up grid: python-pro / quant-analyst / market-researcher, D-06)
- [ ] 04-06-PLAN.md — section 06 hands-on-build via /agents (SUB-03 wire, D-07)
- [ ] 04-07-PLAN.md — section 07 hands-on-fire (invoke + observe isolated context, SUB-03 fire, D-08)
- [ ] 04-08-PLAN.md — ROADMAP/REQUIREMENTS/STATE + bunx slidev build + presenter spot-check (QUAL-01 + D-09 admin)
```

**Change 3 — line 109 (progress table row):**

Before:
```
| 4. Subagents + Agent SDK | v1.1 | 0/0 | Not started | — |
```

After:
```
| 4. Subagents | v1.1 | 0/8 | In progress | — |
```

After execution, completion state is up to the executor (mark plans as `[x]` and bump the count as they land). At Plan 08 close, leave the count showing actual progress.

Atomic commit (QUAL-02): `docs(phase-4): rename phase 4 'Subagents' + drop SUB-02 from roadmap (D-09)`.
  </action>
  <verify>
    <automated>! grep -q "Subagents + Agent SDK" .planning/ROADMAP.md && grep -q "Phase 4: Subagents$\|Phase 4: Subagents\b" .planning/ROADMAP.md && grep -q "\*\*Requirements\*\*: SUB-01, SUB-03$" .planning/ROADMAP.md && grep -q "04-01-PLAN.md" .planning/ROADMAP.md && grep -q "04-08-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Substring `Subagents + Agent SDK` no longer appears anywhere in ROADMAP.md
    - Phase 4 details block lists Requirements as exactly `SUB-01, SUB-03` (no SUB-02)
    - Phase 4 plans list contains entries for all 8 plans (04-01 through 04-08)
    - Goal text references `pages/04-subagents.md` (NOT `pages/04-subagents-sdk.md`)
    - Goal text contains no SDK references
  </acceptance_criteria>
  <done>ROADMAP.md reflects D-09 phase rename; SUB-02 absent from Phase 4 requirements; Plans list populated.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — mark SUB-02 closed-as-out-of-scope; mark SUB-01 + SUB-03 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (CURRENT — line 37 SUB-02 entry; line 106 traceability row; section header line 34 'Subagents + Agent SDK chapter')
    - .planning/phases/04-subagents/04-CONTEXT.md (Action Item 2 — closed-as-out-of-scope language; NOT deferred)
  </read_first>
  <action>
Apply EXACTLY these four textual changes to `.planning/REQUIREMENTS.md`:

**Change 1 — line 34 (section header):**

Before:
```
### Subagents + Agent SDK chapter (`pages/04-subagents-sdk.md`)
```

After:
```
### Subagents chapter (`pages/04-subagents.md`)
```

**Change 2 — line 36 (SUB-01) — mark complete:**

Before:
```
- [ ] **SUB-01**: Chapter explains subagents in Claude Code — Task tool, `.claude/agents/<name>.md` file shape, user-invoked vs Claude-invoked distinction
```

After (Plan 08 only flips the box AFTER the Section 04 file ships and is build-verified):
```
- [x] **SUB-01**: Chapter explains subagents in Claude Code — Task tool, `.claude/agents/<name>.md` file shape, user-invoked vs Claude-invoked distinction
```

**Change 3 — line 37 (SUB-02) — mark closed-as-out-of-scope:**

Before:
```
- [ ] **SUB-02**: Chapter bridges to the programmatic Agent SDK — same primitive outside the CLI in a short snippet
```

After:
```
- [~] **SUB-02**: ~~Chapter bridges to the programmatic Agent SDK — same primitive outside the CLI in a short snippet~~ — **closed-as-out-of-scope for v1.1** (dropped per Phase 4 CONTEXT.md D-09; not deferred — explicitly removed from milestone scope on 2026-05-13)
```

**Change 4 — line 38 (SUB-03) — mark complete:**

Before:
```
- [ ] **SUB-03**: Chapter includes one small live hands-on task. Specific worked example derived during `/gsd-plan-phase 4` research
```

After:
```
- [x] **SUB-03**: Chapter includes one small live hands-on task. Specific worked example derived during `/gsd-plan-phase 4` research (commit-message-writer via `/agents`)
```

**Change 5 — line 106 (traceability table row for SUB-02):**

Before:
```
| SUB-02 | Phase 4: Subagents + Agent SDK | — |
```

After:
```
| SUB-02 | Phase 4: Subagents | closed-as-out-of-scope (v1.1) |
```

**Change 6 — lines 105 and 107 (traceability rows for SUB-01, SUB-03 — rename phase):**

Before:
```
| SUB-01 | Phase 4: Subagents + Agent SDK | — |
```
After:
```
| SUB-01 | Phase 4: Subagents | — |
```

Before:
```
| SUB-03 | Phase 4: Subagents + Agent SDK | Specifics from research |
```
After:
```
| SUB-03 | Phase 4: Subagents | Specifics from research (commit-message-writer via /agents) |
```

Atomic commit (QUAL-02): `docs(phase-4): close SUB-02 as out-of-scope + mark SUB-01/SUB-03 complete`.

**Sequencing note:** Tasks 4-5 (build + presenter spot-check) must pass BEFORE the SUB-01/SUB-03 `[x]` flips become final. Apply Change 1 + Change 3 + Change 5 + Change 6 first; defer Change 2 (SUB-01) and Change 4 (SUB-03) to after Task 5 resumes with `approved` or `approved-with-retrofit`.
  </action>
  <verify>
    <automated>! grep -q "Subagents + Agent SDK" .planning/REQUIREMENTS.md && grep -q "closed-as-out-of-scope" .planning/REQUIREMENTS.md && grep -q "Subagents chapter" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Substring `Subagents + Agent SDK` no longer appears in REQUIREMENTS.md
    - SUB-02 line contains substring `closed-as-out-of-scope`
    - SUB-02 line uses strikethrough `~~...~~` around the original description text
    - Section header reads `### Subagents chapter (`pages/04-subagents.md`)`
    - After Task 5 approval: SUB-01 line begins with `- [x]` and SUB-03 line begins with `- [x]`
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects D-09 closure of SUB-02 and (post-approval) completion of SUB-01/SUB-03.</done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 4 source URLs to pages/99-resources/01-resources.md</name>
  <files>pages/99-resources/01-resources.md</files>
  <read_first>
    - pages/99-resources/01-resources.md (CURRENT — confirm two-column structure: "Deep dives" + "Repos & talks")
    - .planning/phases/04-subagents/04-RESEARCH.md "Sources" Primary section (the three URLs to surface: code.claude.com/docs/en/sub-agents, VoltAgent repo, dev.to article — dev.to already present)
    - .planning/phases/04-subagents/04-RESEARCH.md "Open Questions" Q3 (rec: add URLs to resources page during build plan)
  </read_first>
  <action>
The dev.to article is ALREADY listed in `pages/99-resources/01-resources.md` (under "Deep dives") — leave it untouched.

Add TWO new items:

1. To the **Deep dives** `<ul>` (after the existing dev.to entry), add a new `<li>` for the Claude Code subagents docs:

```html
    <li>
      <a href="https://code.claude.com/docs/en/sub-agents" target="_blank">
        Claude Code Subagents — official docs
      </a>
      <div class="text-dim text-xs">code.claude.com</div>
    </li>
```

2. To the **Repos & talks** `<ul>` (after the existing entries), add a new `<li>` for the VoltAgent registry:

```html
    <li>
      <a href="https://github.com/VoltAgent/awesome-claude-code-subagents" target="_blank">
        awesome-claude-code-subagents
      </a>
      <div class="text-dim text-xs">github.com/VoltAgent</div>
    </li>
```

Do NOT add the YouTube walkthrough URL (DNGxMX7ym44) — RESEARCH.md flags it as outdated UI flow; official docs above are the canonical reference.

Preserve indentation (4 spaces matching existing entries). The two new items extend their respective `<ul>` lists; no other content changes.

Atomic commit (QUAL-02): `docs(phase-4): add subagents docs + VoltAgent repo to resources page`.
  </action>
  <verify>
    <automated>grep -q "code.claude.com/docs/en/sub-agents" pages/99-resources/01-resources.md && grep -q "Claude Code Subagents — official docs" pages/99-resources/01-resources.md && grep -q "VoltAgent/awesome-claude-code-subagents" pages/99-resources/01-resources.md && grep -q "awesome-claude-code-subagents" pages/99-resources/01-resources.md && grep -q "dev.to / nunc" pages/99-resources/01-resources.md && ! grep -q "DNGxMX7ym44" pages/99-resources/01-resources.md</automated>
  </verify>
  <acceptance_criteria>
    - Substring `code.claude.com/docs/en/sub-agents` present (Claude Code subagents docs URL)
    - Substring `Claude Code Subagents — official docs` present (link text)
    - Substring `VoltAgent/awesome-claude-code-subagents` present (repo URL)
    - Substring `awesome-claude-code-subagents` present (link text)
    - Pre-existing dev.to entry preserved (substring `dev.to / nunc` still present)
    - YouTube `DNGxMX7ym44` ID NOT present (rejected per RESEARCH Open Q3 recommendation)
  </acceptance_criteria>
  <done>Resources page lists Claude Code subagents docs + VoltAgent repo; YouTube intentionally omitted.</done>
</task>

<task type="auto">
  <name>Task 4: Run bunx slidev build and confirm exit 0 + dist/index.html</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (QUAL-01 wording)
    - .planning/phases/03-hooks/03-06-PLAN.md (Phase 3 ANALOG — same failure-signature playbook applies)
    - CLAUDE.md (Bun-only — `bunx`, NEVER `npx`)
  </read_first>
  <action>
From the repo root, run the production build using Bun's bundled runner:

```bash
bunx slidev build
```

This must:
1. Exit with status `0`.
2. Produce `dist/index.html`.
3. Emit no Slidev / Vite / Shiki ERROR-level messages (warnings are tolerable; errors are not).

Common failure signatures (diagnose, then return to the offending plan — do NOT mask):
- `Imported markdown file not found: ./pages/04-subagents/0N-*.md` → orchestrator `src:` path mismatch (Plan 04-01 regression — paths must be `./04-subagents/...`, not `./pages/04-subagents/...`).
- Missing file under `pages/04-subagents/0N-*.md` → corresponding Wave 2 section plan failed; do not patch around it.
- Shiki lang error for `markdown` or `yaml` → unlikely (both in headmatter `langs:`), but verify `slides.md` lines 1-18 untouched.
- Build error inside Section 04's nested fence (the markdown-fenced template containing inner YAML frontmatter) → confirm Plan 04-04 used the prescribed quadruple-backtick or proper indentation pattern; if the outer markdown fence broke on the inner `---`, that's a Plan 04-04 regression — fix there.
- `vite.config.ts` warning → if a `vite.config.ts` was accidentally introduced, delete it (CLAUDE.md constraint).

Do NOT modify any source file from this task to make the build pass. Build failures here indicate a Wave 2 plan has a defect; surface it as a blocker and return to that plan.
  </action>
  <verify>
    <automated>bunx slidev build && test -f dist/index.html && grep -q "<!DOCTYPE html>" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `bunx slidev build` exits with status `0`
    - File `dist/index.html` exists after the build
    - `grep -q "<!DOCTYPE html>" dist/index.html` succeeds
    - Build output contains no `ERROR` lines (warnings OK)
    - No `vite.config.ts` was created or modified by this plan
  </acceptance_criteria>
  <done>Build passes; static SPA artifact exists at `dist/index.html`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Presenter spot-check Chapter 4 (human-verify)</name>
  <files>(read-only by default; conditional retrofit in Task 6 if Section 05 overflows)</files>
  <action>See `<how-to-verify>` below. The presenter runs the steps and resumes on the `<resume-signal>`.</action>
  <verify><automated>echo "human-verify checkpoint — see how-to-verify section"</automated></verify>
  <done>Presenter types `approved` (or `approved-with-retrofit` if Section 05 needs further font-shrink) after the per-section walkthrough completes without failures. Tasks 2 (SUB-01/SUB-03 final `[x]` flips) and 7 (STATE.md update) only proceed after this approval.</done>
  <what-built>
A complete Chapter 4 wired into the deck via `slides.md → pages/04-subagents.md → 7 section files`. Cover, skills-vs-subagents framing (cookbook quote + comparison table + 2 more verbatim quotes + Chapter 2 callback), why-subagents (context-bloat framing), how-subagents-work (file shape + VoltAgent template + Task/Agent invocation modes), 3-up showcase (python-pro / quant-analyst / market-researcher), hands-on build (`/agents` walkthrough), hands-on fire (invoke + two ✓ checks).
  </what-built>
  <how-to-verify>
1. Start the dev server in presenter mode:

   ```bash
   bunx slidev --open
   ```

   If port 3030 is occupied, use `bunx slidev --port 3031 --open`.

2. Navigate to Chapter 4's cover slide via slide menu (`o`) or arrow through prior chapters.

3. Step forward through every Chapter 4 slide. Confirm:

   - **Section 01 — Cover:** centered layout, title `Chapter 4 — Subagents` (em-dash). One-line provocation visible. Dim subtitle with middle-dot separators.

   - **Section 02 Slide 1 (Cookbook · sous chef):** H1 with middle-dot. Cross-callback to Chapter 2 in prose. Blockquote VERBATIM: `Skill = giving Claude a cookbook` / `Subagent = hiring a sous chef`. Dim citation `dev.to / nunc`.

   - **Section 02 Slide 2 (Quick version):** comparison table with header `| Aspect | Skills | Subagents |` and 4 rows from the article.

   - **Section 02 Slide 3 (Where context lives):** blockquote contains `Gets its own isolated context window`.

   - **Section 02 Slide 4 (Default to the lighter tool):** blockquote VERBATIM: `Start with a skill unless you specifically need subagent features. Skills are simpler, faster, and easier to maintain.`

   - **Section 03 Slide 1:** names `context bloat` explicitly. Slide 2 contains `isolated context window`. Slide 3 lists code review / research / log examples. Slide 4 states the tradeoff and forward-references Section 04.

   - **Section 04 Slide 1:** minimal `code-reviewer` example with frontmatter; mentions both `.claude/agents/` paths (project + user); calls `name` and `description` as the only required fields.

   - **Section 04 Slide 2:** VoltAgent template VERBATIM (`name: subagent-name`, `description: When this agent should be invoked`, `tools: Read, Write, Edit, Bash, Glob, Grep`, `model: sonnet`, plus `## Communication Protocol` and `## Development Workflow` headers). Dim footnote contains `VoltAgent-registry convention` and `Claude Code only requires`. `<style scoped>` font-shrink applied — template fits within viewport without overflow.

   - **Section 04 Slide 3:** two invocation modes named (`@subagent-name` user-invoked + `description:` Claude-invoked). Dim footnote contains `Task` → `Agent` rename note. `use proactively` mentioned.

   - **Section 05 Slide 1:** framing prose ("Three subagents, three domains").

   - **Section 05 Slide 2:** 3-up grid with all three names visible (`python-pro`, `quant-analyst`, `market-researcher`). `model: opus` visible on quant-analyst column. `WebFetch, WebSearch` visible on market-researcher column. Truncation footnote visible. **Visual overflow check:** all three frontmatter blocks fit within their grid columns at default presenter resolution. If any column clips → proceed to Task 6.

   - **Section 06 Slide 1:** `/agents` command shown; ordered list contains Library tab, Personal scope, Generate with Claude, paste-prompt blockquote, Bash+Read tools, Sonnet model.

   - **Section 06 Slide 2:** path `~/.claude/agents/commit-message-writer.md` visible.

   - **Section 07:** Step 0 shows `echo "test" >> README.md`. Step 1 references `/agents` and Library tab — first `> ✓ Check:` blockquote present. Step 2 shows `@commit-message-writer` (or `@agent-commit-message-writer`) — second `> ✓ Check:` blockquote present with substring `isolated context window`. Dim footnote names guaranteed vs heuristic. Closing dim line present.

4. Open browser devtools Console. No red errors. Warnings about HMR / Vue dev-mode OK.

5. Confirm `global-top.vue` progress bar advances through Chapter 4.

6. Confirm Chapters 1-3 still render cleanly (no regression from `slides.md` edit).

7. **NO SDK content check (D-09):** open the slide menu (`o`), scan all Chapter 4 slide thumbnails. Confirm NO slide contains the substring "SDK", "Agent SDK", or any `query(` snippet. If any appears, FAIL — return to the offending plan.

If ALL of the above pass with NO overflow on Section 05 → type `approved`. Skip Task 6.

If ALL pass EXCEPT Section 05 overflows → type `approved-with-retrofit`. Proceed to Task 6.

If ANY OTHER expectation fails, describe precisely (which section, which expectation). Return to the offending plan — DO NOT patch here.
  </how-to-verify>
  <resume-signal>Type "approved", "approved-with-retrofit", or describe failed expectations.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: (Conditional) Retrofit Section 05 grid font-shrink if presenter saw overflow</name>
  <files>pages/04-subagents/05-showcase.md</files>
  <read_first>
    - pages/04-subagents/05-showcase.md (CURRENT — `<style scoped>` block is at font-size: 0.5rem; retrofit shrinks further to 0.45rem)
    - pages/03-hooks/03-how-hooks-work.md (precedent — same `<style scoped>` shape; current Phase 3 value is 0.5rem)
    - .planning/phases/04-subagents/04-RESEARCH.md "Assumptions Log" A5 (truncation already applied; font-shrink is the secondary lever)
  </read_first>
  <action>
ONLY execute this task if Task 5 resumed with `approved-with-retrofit`. Otherwise skip.

Edit the existing `<style scoped>` block in `pages/04-subagents/05-showcase.md`:

Before:
```css
pre, code {
  font-size: 0.5rem !important;
  line-height: 1.15 !important;
}
```

After:
```css
pre, code {
  font-size: 0.45rem !important;
  line-height: 1.1 !important;
}
```

After editing: re-run `bunx slidev build` (must still exit 0), then re-step through Section 05 in presenter mode to confirm all three columns now fit.

Atomic commit: `style(phase-4): shrink section 05 grid further to fit presenter viewport`.
  </action>
  <verify>
    <automated>if grep -q "font-size: 0.45rem" pages/04-subagents/05-showcase.md; then bunx slidev build && test -f dist/index.html; else echo "skipped — no retrofit needed"; fi</automated>
  </verify>
  <acceptance_criteria>
    - IF Task 5 resumed with `approved-with-retrofit`: `pages/04-subagents/05-showcase.md` `<style scoped>` font-size is `0.45rem`; `bunx slidev build` re-passes
    - IF Task 5 resumed with `approved`: this task is skipped; no file modified
    - No other Section 05 content altered (frontmatter blocks, layout, footnotes unchanged)
  </acceptance_criteria>
  <done>Either no retrofit needed (approved), or retrofit applied and build re-verified; chapter is presentable.</done>
</task>

<task type="auto">
  <name>Task 7: Update STATE.md to reflect Phase 4 close + corrected phase name</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (CURRENT — frontmatter `progress` block lines 9-14; `Current focus` line 28; `Current Position` block 30-35; `Session Continuity` 77-81; `Recently Completed` 83-85)
    - .planning/phases/04-subagents/04-CONTEXT.md (Action Item 3 — update STATE.md at session end)
    - .planning/phases/03-hooks/03-06-PLAN.md (Phase 3 ANALOG — its SUMMARY documented the corresponding STATE updates)
  </read_first>
  <action>
ONLY execute after Task 5 resumes with `approved` or `approved-with-retrofit` (and Task 2 SUB-01/SUB-03 final `[x]` flips have been applied).

Apply these updates to `.planning/STATE.md`:

**Update 1 — frontmatter `progress` block (lines 9-14):**

Increment counts:
- `completed_phases`: 3 → 4
- `total_plans`: 21 → 29 (added 8 Phase 4 plans)
- `completed_plans`: 21 → 29
- `percent`: 60 → 80 (4/5 phases complete)

**Update 2 — `last_activity` and `stopped_at`:**

Update to: `Phase 04 (Subagents) complete — QUAL-01 build-verify approved` (or `approved-with-retrofit` if applicable). Update timestamps to current date.

**Update 3 — `Current focus` line (~line 28):**

Before: `**Current focus:** Phase 4 — Subagents + Agent SDK (next)`

After: `**Current focus:** Phase 5 — Outro + Milestone Gate (next)`

**Update 4 — `Current Position` block (~lines 30-35):**

Update to reflect Phase 04 close and Phase 05 as next. Reference D-09 SUB-02 drop in a one-line note.

**Update 5 — `Session Continuity` block (~lines 77-81):**

`Last session` → current date. `Stopped at` → `Phase 04 (Subagents) Plan 08 closed — QUAL-01 build-verify approved; SUB-02 closed-as-out-of-scope per D-09`. `Next action` → `/gsd-plan-phase 5 — decompose Phase 5 (Outro + Milestone Gate) into section plans.`

**Update 6 — `Recently Completed` block (~lines 83-85):**

Add a new top entry:

```
- **YYYY-MM-DD — Phase 04 (Subagents) Plan 08** — QUAL-01 build-verify + presenter spot-check. `bunx slidev build` exit 0; `dist/index.html` produced; Chapter 4 approved in presenter mode. D-09 action items executed: ROADMAP renamed Phase 4 → 'Subagents'; REQUIREMENTS marked SUB-02 closed-as-out-of-scope. SUMMARY: `.planning/phases/04-subagents/04-08-SUMMARY.md`. Requirements: SUB-01, SUB-03 marked complete; SUB-02 closed.
```

Use the actual close date.

Atomic commit (QUAL-02): `docs(state): close phase 4 (subagents) + advance focus to phase 5`.
  </action>
  <verify>
    <automated>grep -q "completed_phases: 4" .planning/STATE.md && grep -q "Phase 5 — Outro" .planning/STATE.md && grep -q "Phase 04 (Subagents) Plan 08" .planning/STATE.md && ! grep -q "Subagents + Agent SDK (next)" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `completed_phases: 4`
    - `Current focus` references Phase 5 (NOT Phase 4)
    - No leftover `Subagents + Agent SDK (next)` text
    - `Recently Completed` has a new top entry for Phase 04 Plan 08
    - `Session Continuity` `Next action` points to `/gsd-plan-phase 5`
  </acceptance_criteria>
  <done>STATE.md reflects Phase 04 close and Phase 05 readiness.</done>
</task>

</tasks>

<verification>
- ROADMAP, REQUIREMENTS, STATE all reflect D-09: phase renamed "Subagents", SUB-02 closed-as-out-of-scope, SUB-01/SUB-03 marked complete.
- Resources page lists subagents docs + VoltAgent repo (YouTube intentionally omitted).
- `bunx slidev build` exits 0; `dist/index.html` produced.
- Human presenter walkthrough confirms all 7 sections render per spec; no SDK content surfaces on any Chapter 4 slide.
- Conditional Section 05 font-shrink retrofit applied if needed; build re-verified.
- All atomic commits scoped per QUAL-02.
</verification>

<success_criteria>
QUAL-01 satisfied: Chapter 4 builds clean and renders clean. D-09 action items fully discharged (phase rename, SUB-02 closure, STATE update). Phase 04 of v1.1 is shippable; `/gsd-plan-phase 5` can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-08-SUMMARY.md`.
</output>
