---
phase: 04-subagents
plan: 06
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/06-hands-on-build.md
autonomous: true
requirements: [SUB-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Hands-on uses Claude Code's /agents interactive slash command — NOT manual mkdir + paste (D-07)"
    - "Walkthrough follows current docs (tabbed Running/Library UI), not the older YouTube flat-menu flow"
    - "Personal-scope vs Project-scope is called out explicitly; Personal recommended (Pitfall 3 mitigation)"
    - "Hands-on subagent is commit-message-writer (researcher recommendation; per A1 add `echo >> README.md` step zero or instructor verifies an uncommitted change exists)"
    - "No screenshots — walkthrough is a text-only ordered list (QUAL-04)"
  artifacts:
    - path: "pages/04-subagents/06-hands-on-build.md"
      provides: "/agents walkthrough — build commit-message-writer subagent live"
      contains: "/agents"
---

<objective>
Author `pages/04-subagents/06-hands-on-build.md` — Section 06 of Chapter 4. Wires the SUB-03 hands-on. Participants run `/agents`, walk the tabbed UI flow, and end with `~/.claude/agents/commit-message-writer.md` on disk. No manual file creation, no screenshots, text-only ordered list per QUAL-04. Anchors on the verified `/agents` walkthrough from 04-RESEARCH.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/06-hands-on-build.md — /agents walkthrough for commit-message-writer</name>
  <files>pages/04-subagents/06-hands-on-build.md</files>
  <read_first>
    - pages/03-hooks/04b-or-just-ask.md (Phase 3 ANALOG — hands-on build slide with a code fence + dim tips; same shape applies)
    - .planning/phases/04-subagents/04-RESEARCH.md "/agents Walkthrough — Verified Flow" section (the 13-step canonical sequence — base the slide's ordered list on these steps; condense to fit one slide)
    - .planning/phases/04-subagents/04-RESEARCH.md "Common Pitfalls" Pitfall 3 (Personal vs Project scope — Personal recommended)
    - .planning/phases/04-subagents/04-RESEARCH.md "Code Examples" Example 6 (commit-message-writer reference frontmatter — what the generated subagent should look like)
    - .planning/phases/04-subagents/04-RESEARCH.md "Assumptions Log" A1 (participants need an uncommitted change; surface as a step)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-07 — `/agents` LOCKED; D-08 hands-on subagent purpose)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/06-hands-on-build.md` with EXACTLY these contents. Two slides: the build flow itself, then the resulting on-disk artifact. Slide structure mirrors Phase 3 Section 04 (hands-on build) — fenced block + ordered prose + dim helper.

```markdown
# Build it: run /agents

Open Claude Code and run the slash command:

```
/agents
```

Then walk this flow:

1. The interface opens in two tabs — **Running** and **Library**. Switch to **Library**.
2. Pick **Create new agent**.
3. Choose **Personal** when prompted — your subagent lives in `~/.claude/agents/` and works in every project. Pick Project only if you want it scoped to one repo.
4. Pick **Generate with Claude** (the alternative is configure manually — we're not doing that today).
5. When asked what the agent does, paste:

   > A commit-message-writer that reads `git diff` and returns one conventional-commit line. No body, no footer, ≤72 chars.
6. Select tools: **Bash** and **Read** only. Deselect everything else.
7. Select model: **Sonnet**.
8. Pick a color. Memory: **None**. Save.

<div class="text-dim text-xs mt-3">
  Subagents created through <code>/agents</code> are loaded immediately — no session restart needed
</div>

---

# What just landed on disk

```
~/.claude/agents/commit-message-writer.md
```

Same shape as Section 04. Frontmatter Claude wrote for you, plus the system prompt it generated from your description.

<div class="text-dim text-sm mt-6">
  next slide: fire it · watch the diff stay inside the subagent's window
</div>
```

Notes on load-bearing details:
- Slide 1 ("Build it: run /agents"): the fenced ` ``` ` block holds just `/agents` (the command to type). The numbered list is the condensed walkthrough from 04-RESEARCH.md's 13-step canonical sequence — collapsed where research's steps 11-13 fold naturally (color/memory/save in step 8). Personal scope explicitly recommended (Pitfall 3). The "Generate with Claude" path is named (faster than manual config). The describe-prompt is given verbatim in a blockquote so participants can paste-and-go.
- Dim footnote calls out the "no session restart" advantage from Pitfall 5 — reinforces why `/agents` is the right path vs manual mkdir.
- Slide 2 ("What just landed on disk"): shows the resulting file path. Mentions same-shape-as-Section-04 (continuity). One-line forward-pointer to Section 07.
- NO screenshots (QUAL-04). The walkthrough is fully text-based.
- A1 assumption (uncommitted change exists): NOT addressed in this section — it belongs in Section 07's fire step. Section 06 only builds the subagent; uncommitted-change setup happens just before invocation.
- Tools: Bash + Read only — matches Example 6 reference frontmatter exactly.
- Model: Sonnet — matches Example 6.
- No exclamation marks in body.
- No SDK references.

Atomic commit (QUAL-02): `feat(phase-4): add section 06 hands-on build via /agents (SUB-03 wire)`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/06-hands-on-build.md && grep -q "/agents" pages/04-subagents/06-hands-on-build.md && grep -q "Library" pages/04-subagents/06-hands-on-build.md && grep -q "Personal" pages/04-subagents/06-hands-on-build.md && grep -q "Generate with Claude" pages/04-subagents/06-hands-on-build.md && grep -q "commit-message-writer" pages/04-subagents/06-hands-on-build.md && grep -q "Bash" pages/04-subagents/06-hands-on-build.md && grep -q "Sonnet" pages/04-subagents/06-hands-on-build.md && grep -q "~/.claude/agents/commit-message-writer.md" pages/04-subagents/06-hands-on-build.md && [ "$(grep -v '^#' pages/04-subagents/06-hands-on-build.md | grep -c '!')" = "0" ] && ! grep -qi "Agent SDK\|query(" pages/04-subagents/06-hands-on-build.md && ! grep -qi "screenshot\|\.png\|\.jpg" pages/04-subagents/06-hands-on-build.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/06-hands-on-build.md` exists
    - `/agents` command appears (the canonical entry point)
    - Tabbed UI named: substring `Library` appears
    - Personal-scope guidance explicit: substring `Personal` appears
    - Generate-with-Claude path named: substring `Generate with Claude`
    - Hands-on subagent identity: substring `commit-message-writer`
    - Tools/model lock: substrings `Bash` AND `Sonnet` both appear
    - Resulting path on disk: substring `~/.claude/agents/commit-message-writer.md`
    - QUAL-04: `grep -v '^#' file | grep -c "!"` returns `0`
    - QUAL-04: no `screenshot`, `.png`, `.jpg` references (case-insensitive)
    - D-09: no `Agent SDK` or `query(` substrings
  </acceptance_criteria>
  <done>Section 06 wires SUB-03's build step: text-only `/agents` walkthrough produces commit-message-writer on disk in Personal scope.</done>
</task>

</tasks>

<verification>
- /agents walkthrough is the canonical path (no manual mkdir).
- All key UI prompts named: Library, Personal, Generate with Claude, Bash+Read, Sonnet.
- Disk artifact path is explicit.
- No screenshots; no SDK content.
</verification>

<success_criteria>
SUB-03 build step delivered. Participants leave Section 06 with the subagent on disk and an understanding that `/agents` writes the file for them.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-06-SUMMARY.md`.
</output>
