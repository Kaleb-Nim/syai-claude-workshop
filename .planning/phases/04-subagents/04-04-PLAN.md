---
phase: 04-subagents
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/04-how-subagents-work.md
autonomous: true
requirements: [SUB-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 04 teaches `.claude/agents/<name>.md` file shape (SUB-01)"
    - "The standardized VoltAgent template appears as a fenced markdown block, verbatim per D-05"
    - "Both invocation modes are taught (SUB-01): user-invoked (`@subagent-name` / `/agents` UI) and Claude-invoked (Task/Agent tool via `description:` auto-delegation)"
    - "Section says 'Task / Agent tool' on first mention (Pitfall 2 — naming bridge for v2.1.63 rename)"
    - "A single dim-text footnote acknowledges the VoltAgent template divergence — Claude Code only requires `name` and `description` (D-05 mandate; RESEARCH.md Pitfall 1)"
  artifacts:
    - path: "pages/04-subagents/04-how-subagents-work.md"
      provides: "Subagent file shape, template, and invocation modes"
      contains: "name: subagent-name"
---

<objective>
Author `pages/04-subagents/04-how-subagents-work.md` — Section 04 of Chapter 4. The technical core of the chapter: SUB-01 fully delivered here. Three beats across slides: (1) file shape + locked VoltAgent template (D-05), (2) the two invocation modes (user-invoked + Claude-invoked) with the Task→Agent tool naming bridge, (3) the VoltAgent-divergence footnote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/04-how-subagents-work.md with file shape, template, invocation modes, and divergence footnote</name>
  <files>pages/04-subagents/04-how-subagents-work.md</files>
  <read_first>
    - pages/03-hooks/03-how-hooks-work.md (Phase 3 ANALOG — events + fenced JSON + dim footnotes + `<style scoped>` font-shrink; same shape applies for fenced markdown subagent template)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-05 — VoltAgent template locked verbatim; SUB-01 — both invocation modes required)
    - .planning/phases/04-subagents/04-RESEARCH.md "User Constraints" Section 04 (the verbatim VoltAgent template — copy byte-for-byte from lines 27-44)
    - .planning/phases/04-subagents/04-RESEARCH.md "Common Pitfalls" Pitfall 1 (divergence footnote wording) and Pitfall 2 (Task → Agent rename — say "Task / Agent tool" on first mention)
    - .planning/phases/04-subagents/04-RESEARCH.md "Code Examples" Example 5 (verbatim user-invocation patterns from official docs)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/04-how-subagents-work.md` with EXACTLY these contents. Three slides, separated by `---`. The middle slide carries the locked template and will likely need the Phase 3 `<style scoped>` font-shrink — include it preemptively (mirrors Phase 3 Section 03's grid-of-code pattern):

```markdown
# A subagent is a markdown file

Live under `.claude/agents/<name>.md` (project scope) or `~/.claude/agents/<name>.md` (user scope, available in every project). Two required frontmatter fields: `name` and `description`. The rest is optional.

```yaml
---
name: code-reviewer
description: Reviews code for quality and best practices
tools: Read, Glob, Grep
model: sonnet
---

You are a code reviewer. When invoked, analyze the code
and provide specific, actionable feedback.
```

<div class="text-dim text-xs mt-3">
  the minimal shape — straight from the Claude Code subagents docs
</div>

---

# The standard template

The VoltAgent registry uses a richer body shape — frontmatter, role line, then two convention sections:

```markdown
---
name: subagent-name
description: When this agent should be invoked
tools: Read, Write, Edit, Bash, Glob, Grep
model: sonnet
---

You are a [role description and expertise areas]...

[Agent-specific checklists, patterns, and guidelines]...

## Communication Protocol
Inter-agent communication specifications...

## Development Workflow
Structured implementation phases...
```

<div class="text-dim text-xs mt-3">
  the lower two sections (<code>## Communication Protocol</code>, <code>## Development Workflow</code>) are a VoltAgent-registry convention — Claude Code only requires <code>name</code> and <code>description</code>
</div>

<style scoped>
pre, code {
  font-size: 0.6rem !important;
  line-height: 1.2 !important;
}
.slidev-code {
  padding: 0.45em 0.7em !important;
}
h1 {
  margin-bottom: 0.3em !important;
}
</style>

---

# How it gets invoked

Two paths, both supported:

- **You invoke it** — type `@` and pick from the typeahead, or open `/agents` and run it from the UI. Guaranteed to run.
- **Claude invokes it** — Claude reads the `description:` field and reaches for the Task / Agent tool when the field matches your request. Heuristic, not guaranteed — include `use proactively` in `description:` to nudge it.

<div class="text-dim text-sm mt-6">
  <code>@subagent-name</code> is the manual handle · <code>description:</code> is the auto-delegation hook
</div>

<div class="text-dim text-xs mt-2">
  Claude Code v2.1.63 renamed the <strong>Task</strong> tool to <strong>Agent</strong> — old <code>Task(...)</code> references still work as aliases
</div>
```

Notes on load-bearing details:
- Slide 1 ("A subagent is a markdown file"): teaches the file shape with the OFFICIAL minimal Claude Code example from 04-RESEARCH.md Example 1 (verbatim). Names both scopes (project vs user) by path — per CONTEXT.md "one taught, the other listed by name" rule, both shown so participants know which is which. Calls out `name` and `description` as the only required fields (Pitfall 1 prevention).
- Slide 2 ("The standard template"): VoltAgent template VERBATIM from 04-RESEARCH.md `<user_constraints>` Section 04 (lines 27-44). Triple-fenced markdown block — the inner ` ``` ` blocks inside the template stay literal because the outer fence is markdown not yaml. **Important:** the inner template uses ` ```markdown ` fence, so the literal `---` lines of the template's frontmatter render as-is without breaking out of the fence. The dim footnote underneath is the EXACT divergence-flag wording from 04-RESEARCH.md Pitfall 1 ("VoltAgent-registry convention — Claude Code only requires `name` and `description`"). `<style scoped>` font-shrink block included preemptively to fit the long template (mirrors Phase 3 Section 03 pattern).
- Slide 3 ("How it gets invoked"): the two SUB-01 modes. User-invoked is explicit (`@`-mention + `/agents` UI — both surfaces named). Claude-invoked references the `description:` field as the auto-delegation hook AND surfaces the `use proactively` nudge from RESEARCH.md Pitfall 4. The "Task / Agent tool" naming bridge is at the bottom in a dim footnote per Pitfall 2.
- Per-slide `---` separators on their own lines (and there are three slide-separator `---` lines in addition to the YAML frontmatter `---` markers inside the fenced blocks — verifier accounts for both).
- No exclamation marks in body.
- No SDK references.

Atomic commit (QUAL-02): `feat(phase-4): add section 04 how subagents work (SUB-01)`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/04-how-subagents-work.md && grep -q "name: subagent-name" pages/04-subagents/04-how-subagents-work.md && grep -q "name: code-reviewer" pages/04-subagents/04-how-subagents-work.md && grep -q "## Communication Protocol" pages/04-subagents/04-how-subagents-work.md && grep -q "## Development Workflow" pages/04-subagents/04-how-subagents-work.md && grep -q "Task / Agent tool" pages/04-subagents/04-how-subagents-work.md && grep -q "VoltAgent-registry convention" pages/04-subagents/04-how-subagents-work.md && grep -q "Claude Code only requires" pages/04-subagents/04-how-subagents-work.md && grep -q "@subagent-name\|@\`-mention\|@</code>" pages/04-subagents/04-how-subagents-work.md && grep -q "\.claude/agents/" pages/04-subagents/04-how-subagents-work.md && [ "$(grep -v '^#' pages/04-subagents/04-how-subagents-work.md | grep -c '!')" = "0" ] && ! grep -qi "Agent SDK\|query(" pages/04-subagents/04-how-subagents-work.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/04-how-subagents-work.md` exists
    - VoltAgent template present verbatim: `name: subagent-name` AND `description: When this agent should be invoked` AND `tools: Read, Write, Edit, Bash, Glob, Grep` AND `model: sonnet`
    - Both VoltAgent body headers present: `## Communication Protocol` and `## Development Workflow`
    - Official minimal example present: `name: code-reviewer` and `description: Reviews code for quality and best practices`
    - File shape names both scope paths: `.claude/agents/` (project) and `~/.claude/agents/` (user)
    - Divergence footnote present: contains both `VoltAgent-registry convention` and `Claude Code only requires`
    - Both invocation modes named: `@subagent-name` (or `@`-mention as inline code) AND `description:` field references
    - Task→Agent naming bridge present: substring `Task / Agent tool` appears
    - `grep -v '^#' file | grep -c "!"` returns `0` (no exclamation marks in body)
    - No occurrence of `Agent SDK` or `query(` (D-09)
    - `<style scoped>` font-shrink block present (mirrors Phase 3 Section 03)
  </acceptance_criteria>
  <done>Section 04 delivers SUB-01 in full: file shape, locked VoltAgent template, both invocation modes, and the divergence footnote.</done>
</task>

</tasks>

<verification>
- All SUB-01 sub-criteria visible on the slide file (file shape + template + user-invoked + Claude-invoked).
- VoltAgent template byte-verbatim from CONTEXT.md D-05.
- Divergence footnote present per Pitfall 1.
- Task → Agent naming bridge present per Pitfall 2.
- No SDK content.
</verification>

<success_criteria>
SUB-01 fully discharged in this single section. Participants leave Section 04 knowing the file shape, the template, both invocation paths, and the VoltAgent-vs-official template distinction.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-04-SUMMARY.md`.
</output>
