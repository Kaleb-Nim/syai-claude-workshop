---
phase: 04-subagents
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/02-skills-vs-subagents.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 02 frames skills-vs-subagents via cookbook/sous-chef contrast (D-02) BEFORE Section 03's context-preservation anchor"
    - "All 3 LOCKED verbatim dev.to quotes from 04-RESEARCH.md appear verbatim (D-03 — no paraphrase)"
    - "A comparison table contrasts skills vs subagents on at least: activation, context, primary use (D-02 minimum columns)"
    - "Cross-chapter callback to Chapter 2 (Skills) appears explicitly (mirrors Phase 3 → Phase 1 callback pattern)"
    - "Source URL https://dev.to/nunc/claude-code-skills-vs-subagents-when-to-use-what-4d12 is cited inline as quote provenance"
  artifacts:
    - path: "pages/04-subagents/02-skills-vs-subagents.md"
      provides: "Skills-vs-subagents framing slides — comparison table + 3 verbatim quotes"
      contains: "Gets its own isolated context window"
  key_links:
    - from: "pages/04-subagents/02-skills-vs-subagents.md"
      to: "pages/02-skills.md"
      via: "in-prose callback ('back in Chapter 2 …')"
      pattern: "Chapter 2"
---

<objective>
Author `pages/04-subagents/02-skills-vs-subagents.md` — Section 02 of Chapter 4. Delivers the cookbook/sous-chef mental-model framing (D-02) BEFORE Section 03's practical "why" framing (D-04). Three verbatim dev.to quotes (D-03 LOCKED), a comparison table (the article's "Quick Version" table, verbatim per researcher recommendation Path A), and a Chapter 2 cross-callback. Mirrors Phase 3 Section 02's locked-quote pattern (3 determinism quotes in `pages/03-hooks/02-why-hooks.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/02-skills-vs-subagents.md with comparison table + 3 locked quotes + Chapter 2 callback</name>
  <files>pages/04-subagents/02-skills-vs-subagents.md</files>
  <read_first>
    - pages/03-hooks/02-why-hooks.md (Phase 3 ANALOG — 3 locked quote slides; same per-slide `---` separator + `> blockquote` shape)
    - pages/02-skills/02-why-skills.md (Chapter 2 callback target — confirms "procedural knowledge / cookbook" framing exists there)
    - .planning/phases/04-subagents/04-RESEARCH.md "Skills-vs-Subagents Quotes" section (the 3 LOCKED verbatim quotes — copy byte-for-byte from there)
    - .planning/phases/04-subagents/04-RESEARCH.md "Comparison-Table Source Data" section (the article's 4-row table — copy verbatim)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-02, D-03)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/02-skills-vs-subagents.md` with EXACTLY these contents. The file is one section file containing 5 Slidev slides separated by `---` per-slide separators. No outer frontmatter (per the Phase 3 pattern — section files inherit the chapter's default layout):

```markdown
# Cookbook · sous chef

Back in Chapter 2, a skill was a doc you wrote once that Claude pulled in when relevant. A subagent is structurally different — a whole separate worker Claude can hand a task to.

> Skill = giving Claude a cookbook
> Subagent = hiring a sous chef

<div class="text-dim text-xs mt-6">
  dev.to / nunc — <em>claude-code-skills-vs-subagents-when-to-use-what</em>
</div>

---

# The quick version

| Aspect | Skills | Subagents |
|--------|--------|-----------|
| What it is | Instructions that extend Claude's knowledge | A separate AI assistant with its own context |
| When Claude uses it | Automatically when relevant | Delegated for complex workflows |
| Best for | Utility functions, recipes, how-tos | Multi-step analysis, code review, research |
| Context | Shares your main chat context | Gets its own isolated context window |

<div class="text-dim text-xs mt-4">
  table verbatim from the dev.to article
</div>

---

# Where context lives

> Context — Skills: Shares your main chat context · Subagents: Gets its own isolated context window

<div class="text-dim text-sm mt-6">
  the structural difference — Section 03 unpacks why that matters
</div>

---

# Default to the lighter tool

> Start with a skill unless you specifically need subagent features. Skills are simpler, faster, and easier to maintain.

<div class="text-dim text-xs mt-6">
  all three quotes: dev.to / nunc — <em>claude-code-skills-vs-subagents-when-to-use-what</em>
</div>
```

Notes on load-bearing details:
- Slide 1 ("Cookbook · sous chef"): H1 uses middle-dot `·` (U+00B7). Cross-callback prose names "Chapter 2" explicitly. Quote 1 is the cookbook/sous-chef one-liner, VERBATIM from 04-RESEARCH.md lines 538-540 — two-line shape with the literal newline between cookbook and sous chef lines preserved inside the blockquote.
- Slide 2 ("The quick version"): Markdown table with EXACTLY the 4 rows from 04-RESEARCH.md "Comparison-Table Source Data". Column headers exactly `Aspect | Skills | Subagents`. No added rows (planner discretion was allowed but researcher recommended verbatim).
- Slide 3 ("Where context lives"): Blockquote uses researcher's Path A wording — the comparison table row verbatim with the middle-dot separators and `Context — Skills:` / `· Subagents:` prefix structure. This preserves verbatim discipline.
- Slide 4 ("Default to the lighter tool"): Quote 3 verbatim — exactly `Start with a skill unless you specifically need subagent features. Skills are simpler, faster, and easier to maintain.` (full stop included, two sentences).
- Per-slide separator is bare `---` on its own line (the Phase 3 pattern in `02-why-hooks.md`).
- Tone: no exclamation marks anywhere in the body; em-dashes ` — ` (U+2014) only.
- The blockquote bodies are SACRED — do not edit, paraphrase, or "tighten" them. They are verbatim verifiable.

Atomic commit (QUAL-02): `feat(phase-4): add section 02 skills-vs-subagents framing`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/02-skills-vs-subagents.md && grep -q "^> Skill = giving Claude a cookbook$" pages/04-subagents/02-skills-vs-subagents.md && grep -q "^> Subagent = hiring a sous chef$" pages/04-subagents/02-skills-vs-subagents.md && grep -q "Gets its own isolated context window" pages/04-subagents/02-skills-vs-subagents.md && grep -q "Start with a skill unless you specifically need subagent features" pages/04-subagents/02-skills-vs-subagents.md && grep -q "| Aspect | Skills | Subagents |" pages/04-subagents/02-skills-vs-subagents.md && grep -q "Chapter 2" pages/04-subagents/02-skills-vs-subagents.md && grep -q "dev.to / nunc" pages/04-subagents/02-skills-vs-subagents.md && [ "$(grep -v '^#' pages/04-subagents/02-skills-vs-subagents.md | grep -v '^>' | grep -c '!')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/02-skills-vs-subagents.md` exists
    - All 3 LOCKED verbatim quotes present:
      - `> Skill = giving Claude a cookbook` AND `> Subagent = hiring a sous chef` (Quote 1, two-line shape)
      - Substring `Gets its own isolated context window` appears (Quote 2)
      - Substring `Start with a skill unless you specifically need subagent features. Skills are simpler, faster, and easier to maintain.` appears verbatim (Quote 3)
    - Comparison table present with header row `| Aspect | Skills | Subagents |` and 4 data rows from 04-RESEARCH.md
    - Cross-callback substring `Chapter 2` appears in body prose
    - Source citation `dev.to / nunc` appears at least once
    - Per-slide separators present (the file contains at least 4 standalone `---` lines splitting 5 slides)
    - `grep -v '^#' file | grep -v '^>' | grep -c "!"` returns `0` (no exclamation marks outside H1s and blockquotes — and the blockquotes are verbatim, so they cannot contain any either)
    - No mention of "SDK", "Agent SDK", `query(` anywhere (D-09)
  </acceptance_criteria>
  <done>Section 02 ships with the cookbook framing, 3 verbatim quotes, the article's 4-row comparison table, and a Chapter 2 callback — content-true to dev.to article wording.</done>
</task>

</tasks>

<verification>
- File exists with all 3 locked verbatim quotes intact (no paraphrase).
- Comparison table is the article's 4 rows verbatim.
- Cross-chapter callback to Chapter 2 in the framing slide.
- Source citation present (dev.to / nunc).
- Tone discipline (no `!` in body) holds.
</verification>

<success_criteria>
Section 02 of Chapter 4 sets the mental model (cookbook/sous-chef) and the default heuristic (start with a skill) before Section 03 motivates the choice via context preservation. Commit lands as atomic per-section change.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-02-SUMMARY.md`.
</output>
