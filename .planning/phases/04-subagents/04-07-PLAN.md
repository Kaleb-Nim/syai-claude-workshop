---
phase: 04-subagents
plan: 07
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/04-subagents/07-hands-on-fire.md
autonomous: true
requirements: [SUB-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section invokes the freshly-created subagent and demonstrates isolated context (D-08)"
    - "Step 0 mitigates Assumption A1 — instructs participants to ensure they have an uncommitted change first"
    - "Uses @-mention (`@commit-message-writer` or `@agent-commit-message-writer`) — the GUARANTEED invocation path per Pitfall 4"
    - "Two ✓ Checks present (D-08): (1) subagent visible in `/agents` list, (2) invoked subagent returns a result while main chat stays clean"
    - "Slide explicitly notes the diff stays in the subagent's window (payoff for Section 03's context-preservation framing)"
  artifacts:
    - path: "pages/04-subagents/07-hands-on-fire.md"
      provides: "Fire-it slide — invoke subagent, observe isolated context, two ✓ checks"
      contains: "✓ Check"
---

<objective>
Author `pages/04-subagents/07-hands-on-fire.md` — Section 07 of Chapter 4. Closes SUB-03's fire step: participants invoke `@commit-message-writer` and SEE that the diff stays in the subagent window while only the commit message returns to main chat. Two ✓ checks (D-08). Mirrors Phase 3 Section 05's "Fire it" shape (`pages/03-hooks/05-hands-on-fire.md`) — two bold-prefixed steps, two ✓ Check blockquotes, closing dim line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-subagents/04-CONTEXT.md
@.planning/phases/04-subagents/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/04-subagents/07-hands-on-fire.md — invoke commit-message-writer + observe isolated context</name>
  <files>pages/04-subagents/07-hands-on-fire.md</files>
  <read_first>
    - pages/03-hooks/05-hands-on-fire.md (Phase 3 ANALOG — exact structural template: bold-prefixed steps, ✓ Check blockquotes with literal ✓ U+2713 glyph, closing dim line)
    - .planning/phases/04-subagents/04-RESEARCH.md "Common Pitfalls" Pitfall 4 (auto-delegation NOT guaranteed → use @-mention)
    - .planning/phases/04-subagents/04-RESEARCH.md "Code Examples" Example 5 (verbatim invocation patterns from official docs)
    - .planning/phases/04-subagents/04-RESEARCH.md "Assumptions Log" A1 (Step 0 — ensure uncommitted change exists)
    - .planning/phases/04-subagents/04-CONTEXT.md (D-08 — two ✓ checks; isolated context payoff)
  </read_first>
  <action>
Create the NEW file `pages/04-subagents/07-hands-on-fire.md` with EXACTLY these contents. Single slide (or two if the content overflows — Phase 3 Section 05 is one slide so default to one). Uses literal `✓` Unicode glyph (U+2713) — copy directly from Phase 3 Section 05.

```markdown
# Fire it

**Step 0 — Make sure you have an uncommitted change:**

```
echo "test" >> README.md
```

**Step 1 — Confirm the subagent is registered:** run `/agents` again, switch to the **Library** tab.

> ✓ Check: `commit-message-writer` is listed.

**Step 2 — Invoke it explicitly:** in your main Claude Code chat, type `@` and pick `commit-message-writer` from the typeahead (or type `@agent-commit-message-writer` directly). Send.

> ✓ Check: Claude returns one conventional-commit line. The `git diff` output never appears in your main chat — that's the isolated context window from Section 03, working.

<div class="text-dim mt-6">
  Auto-delegation via <code>description:</code> is heuristic. <code>@</code>-mention guarantees the subagent runs.
</div>

<div class="text-dim text-sm mt-2">
  One subagent, one window, one summary back. Add more later — same pattern.
</div>
```

Notes on load-bearing details:
- Step 0 explicitly mitigates A1 by giving a one-line shell command that produces an uncommitted change in any repo (workshop repo itself works). The command `echo "test" >> README.md` is intentionally idempotent-ish — running it twice just makes the diff bigger; never errors.
- Step 1 reuses `/agents` from Section 06 to verify the on-disk artifact is loaded. This satisfies D-08's ✓ Check #1.
- Step 2 uses `@`-mention (Pitfall 4 mitigation) — guarantees delegation, no live-demo risk. Both syntactic forms named: the typeahead UX (`@` + pick) and the explicit `@agent-commit-message-writer` form (from Example 5 verbatim docs).
- Second ✓ Check explicitly names the payoff: `git diff` output does NOT appear in main chat. This is the structural payoff for Section 03's framing — closes the loop.
- Dim footnote names the Pitfall 4 lesson: auto-delegation is heuristic, `@`-mention is guaranteed. Sized smaller (the closing line is text-sm; this caveat is plain mt-6 — slightly more prominent).
- Closing dim line mirrors Phase 3 Section 05's closing rhythm: "Two events, same shape. Add more later — that's the whole pattern." Here: "One subagent, one window, one summary back. Add more later — same pattern."
- Literal `✓` glyph (U+2713) — same character used in Phase 3 Section 05.
- No exclamation marks anywhere in the body.
- No SDK references.

Atomic commit (QUAL-02): `feat(phase-4): add section 07 hands-on fire (SUB-03 fire)`.
  </action>
  <verify>
    <automated>test -f pages/04-subagents/07-hands-on-fire.md && grep -q "Step 0" pages/04-subagents/07-hands-on-fire.md && grep -q "echo \"test\" >> README.md" pages/04-subagents/07-hands-on-fire.md && grep -q "@commit-message-writer\|@agent-commit-message-writer" pages/04-subagents/07-hands-on-fire.md && [ "$(grep -c '✓ Check' pages/04-subagents/07-hands-on-fire.md)" = "2" ] && grep -q "/agents" pages/04-subagents/07-hands-on-fire.md && grep -q "Library" pages/04-subagents/07-hands-on-fire.md && grep -q "isolated context window" pages/04-subagents/07-hands-on-fire.md && grep -q "guarantees" pages/04-subagents/07-hands-on-fire.md && [ "$(grep -v '^#' pages/04-subagents/07-hands-on-fire.md | grep -c '!')" = "0" ] && ! grep -qi "Agent SDK\|query(" pages/04-subagents/07-hands-on-fire.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/04-subagents/07-hands-on-fire.md` exists
    - Step 0 (A1 mitigation) present: substring `Step 0` AND the literal command `echo "test" >> README.md`
    - @-mention invocation present: substring `@commit-message-writer` or `@agent-commit-message-writer`
    - Exactly TWO `✓ Check` blockquotes (D-08 — two checks, literal U+2713 glyph)
    - Step 1 references `/agents` and Library tab
    - Isolated-context payoff named: substring `isolated context window` (echoes Section 03's verbatim phrase)
    - Pitfall 4 lesson surfaced: substring `guarantees`
    - QUAL-04: `grep -v '^#' file | grep -c "!"` returns `0`
    - D-09: no `Agent SDK` or `query(` substrings
  </acceptance_criteria>
  <done>Section 07 closes SUB-03: participants invoke commit-message-writer, see both ✓ checks, and the slide names the isolated-context payoff explicitly.</done>
</task>

</tasks>

<verification>
- Step 0 (A1 mitigation), Step 1 (registry check + ✓), Step 2 (invoke + ✓).
- @-mention used for guarantee (Pitfall 4).
- Isolated-context payoff named (Section 03 callback).
- No `!` in body; no SDK content.
</verification>

<success_criteria>
SUB-03 fire step delivered with both ✓ checks. Section 03's framing pays off — participants SEE the diff stay in the subagent window.
</success_criteria>

<output>
After completion, create `.planning/phases/04-subagents/04-07-SUMMARY.md`.
</output>
