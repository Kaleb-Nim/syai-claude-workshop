---
phase: 02-skills
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - pages/02-skills/02-why-skills.md
autonomous: true
requirements: [SKL-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "pages/02-skills/02-why-skills.md contains exactly 5 slides separated by `---` rulers"
    - "Slide 1 contains an explicit Phase 3 → Phase 2 bridge contrasting hook (you wire the when) vs skill (Claude decides the when)"
    - "Slide 2 frames skills as reusable procedural knowledge with a one-line concrete example"
    - "Slide 3 contains LOCKED QUOTE 1 verbatim (no rewording)"
    - "Slide 4 contains LOCKED QUOTE 2 verbatim (no rewording)"
    - "Slide 5 contains LOCKED QUOTE 3 verbatim (no rewording)"
    - "No exclamation marks in slide prose (QUAL-04)"
  artifacts:
    - path: "pages/02-skills/02-why-skills.md"
      provides: "Section 02 — 5-slide framing + 3 verbatim quotes (SKL-01 framing half)"
      contains: "Skills are procedural knowledge"
  key_links:
    - from: "pages/02-skills.md"
      to: "pages/02-skills/02-why-skills.md"
      via: "src: include resolution"
      pattern: "src: ./02-skills/02-why-skills.md"
---

<objective>
Author Section 02 of Chapter 2 — the "why skills" framing section. 5 slides total, locked shape per D-02:

- Slide 1: Phase 3 → Phase 2 bridge (hook = you wire the "when"; skill = Claude decides the "when").
- Slide 2: Why skills matter — procedural knowledge framing + one-line concrete example.
- Slide 3: LOCKED QUOTE 1 (verbatim) — what skills are in essence.
- Slide 4: LOCKED QUOTE 2 (verbatim) — why this scales.
- Slide 5: LOCKED QUOTE 3 (verbatim) — the self-invocation moment.

The 3 quotes are user-locked content (D-03). They MUST appear byte-exact. Verification greps the literal quote text.

Covers SKL-01 framing half. SKL-01 mechanism half (file shape, frontmatter, location) is Plan 03-04's responsibility.

Optional `<v-clicks>` on Slide 3-5 quote reveals if pacing benefits (per D-13). Default: omit unless pacing analysis suggests otherwise — keep slides clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-skills/03-CONTEXT.md
@.planning/phases/02-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/02-skills/02-why-skills.md (5 slides — bridge + framing + 3 locked quotes)</name>
  <files>pages/02-skills/02-why-skills.md</files>
  <read_first>
    - pages/03-hooks/02-why-hooks.md (Phase 3 ANALOG — 5-slide section using `---` rulers + 3 locked quotes as blockquotes; this is the byte-exact template for slide structure)
    - .planning/phases/02-skills/03-CONTEXT.md (D-02 — Section 02 shape; D-03 — VERBATIM QUOTES; D-04 — Phase 3 bridge required)
    - .planning/phases/02-skills/03-RESEARCH.md (Pattern 3 — multi-slide section; Pattern 4 — locked-quote slide form)
  </read_first>
  <action>
Create the NEW file `pages/02-skills/02-why-skills.md` with EXACTLY these contents:

```markdown
# Hooks and skills are opposite ends

In Chapter 3 you wired a hook — **you** told Claude exactly when to run something. A deterministic IF/THEN.

A skill is the opposite end. **Claude** decides when to load it, based on what you ask.

<div class="text-dim text-sm mt-6">
  hook = you wire the "when" · skill = Claude decides the "when"
</div>

---

# Why this matters

A skill is reusable procedural knowledge — a "how we do XYZ here" — packaged in a format Claude can discover and load itself.

You write a `pr-review` skill once. Next time someone says "review this PR," Claude loads it without being told.

---

# What skills are, in essence

> Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself.

---

# Why this scales

> CLAUDE.md is always loaded. A skill is loaded when needed. That distinction is why a 50-skill library doesn't slow Claude down, but a 5,000-line CLAUDE.md does.

---

# The self-invocation moment

> You didn't type a slash command. You didn't remind Claude of the format. The skill activated itself.

<div class="text-dim text-sm mt-6">
  that's what you'll prove for yourself in two slides
</div>
```

Notes on each element:
- File has NO top-level frontmatter — first line is the first H1. Per-slide frontmatter not needed; default layout is fine.
- Slides separated by `---` on its own line with blank lines above and below (Slidev convention; matches Phase 3 `02-why-hooks.md`).
- Slide 1 H1 = `Hooks and skills are opposite ends`. Phase 3 bridge in plain prose with bolded **you** / **Claude** for the contrast. Dim helper below distills the contrast into one line.
- Slide 2 H1 = `Why this matters`. Procedural-knowledge framing in plain prose. Concrete example uses `pr-review` (per D-02 illustrative). No exclamation marks.
- Slide 3 H1 = `What skills are, in essence`. Blockquote contains LOCKED QUOTE 1 verbatim:

  *"Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself."*

  Rendered as `> Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself.` (single blockquote line, no surrounding double-quotes — the markdown `>` is the quote indicator).

- Slide 4 H1 = `Why this scales`. Blockquote contains LOCKED QUOTE 2 verbatim:

  *"CLAUDE.md is always loaded. A skill is loaded when needed. That distinction is why a 50-skill library doesn't slow Claude down, but a 5,000-line CLAUDE.md does."*

- Slide 5 H1 = `The self-invocation moment`. Blockquote contains LOCKED QUOTE 3 verbatim:

  *"You didn't type a slash command. You didn't remind Claude of the format. The skill activated itself."*

  Dim teaser below points forward to the hands-on payoff.

- Em-dashes inside the quotes are U+2014 (` — `) — copy them from the locked text above, NOT a typed hyphen.
- The apostrophe characters in the quotes are straight `'` (U+0027), matching the locked source verbatim. Do NOT autocorrect to curly quotes.
- No exclamation marks anywhere (QUAL-04).
- Optional `<v-clicks>` wrapper on Slides 3-5 reveal is permitted (D-13) but NOT applied by default — keep slides clean unless live pacing review says otherwise.

Atomic commit (QUAL-02): `feat(phase-2): section 02 why-skills`.
  </action>
  <verify>
    <automated>test -f pages/02-skills/02-why-skills.md && [ "$(grep -c '^---$' pages/02-skills/02-why-skills.md)" = "4" ] && grep -q "^# Hooks and skills are opposite ends$" pages/02-skills/02-why-skills.md && grep -q "^# Why this matters$" pages/02-skills/02-why-skills.md && grep -q "^# What skills are, in essence$" pages/02-skills/02-why-skills.md && grep -q "^# Why this scales$" pages/02-skills/02-why-skills.md && grep -q "^# The self-invocation moment$" pages/02-skills/02-why-skills.md && grep -qF "Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself." pages/02-skills/02-why-skills.md && grep -qF "CLAUDE.md is always loaded. A skill is loaded when needed. That distinction is why a 50-skill library doesn't slow Claude down, but a 5,000-line CLAUDE.md does." pages/02-skills/02-why-skills.md && grep -qF "You didn't type a slash command. You didn't remind Claude of the format. The skill activated itself." pages/02-skills/02-why-skills.md && [ "$(grep -c '!' pages/02-skills/02-why-skills.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/02-skills/02-why-skills.md` exists
    - Exactly 4 slide separators (`^---$`) — yielding 5 slides
    - All 5 H1 lines present in order: `# Hooks and skills are opposite ends`, `# Why this matters`, `# What skills are, in essence`, `# Why this scales`, `# The self-invocation moment`
    - LOCKED QUOTE 1 byte-exact (single line, em-dashes preserved): `Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself.`
    - LOCKED QUOTE 2 byte-exact: `CLAUDE.md is always loaded. A skill is loaded when needed. That distinction is why a 50-skill library doesn't slow Claude down, but a 5,000-line CLAUDE.md does.`
    - LOCKED QUOTE 3 byte-exact: `You didn't type a slash command. You didn't remind Claude of the format. The skill activated itself.`
    - Slide 1 contains the bridge phrase `hook = you wire the "when"` AND `skill = Claude decides the "when"`
    - Slide 2 contains the word `pr-review` (concrete example present)
    - `grep -c '!' pages/02-skills/02-why-skills.md` returns `0`
    - No `<v-clicks>` wrapper unless added by deliberate decision (default: omit)
  </acceptance_criteria>
  <done>Section 02 ships with the 5-slide framing + 3 verbatim quotes; committed atomically.</done>
</task>

</tasks>

<verification>
- 5 slides separated by 4 `---` rulers
- All 3 LOCKED QUOTES grep-verifiable byte-exact
- Phase 3 bridge present on Slide 1
- Zero exclamation marks
- Atomic commit `feat(phase-2): section 02 why-skills` (QUAL-02)
</verification>

<success_criteria>
SKL-01 framing half delivered. Participants understand: skills are model-invoked (vs hooks user-wired), they're reusable procedural knowledge, they scale via lazy loading, and the wow moment is auto-invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills/03-03-SUMMARY.md`.
</output>
