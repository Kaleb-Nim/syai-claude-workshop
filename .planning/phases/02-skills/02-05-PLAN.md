---
phase: 02-skills
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - pages/02-skills/04-hands-on-build.md
autonomous: true
requirements: [SKL-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "pages/02-skills/04-hands-on-build.md is a single slide with two code blocks: a ```bash``` block for mkdir, then a ```markdown``` block containing the EXACT D-06 SKILL.md verbatim"
    - "A 'same SKILL.md as the last slide — paste it as-is' reconcile line sits above the SKILL.md fence (reduces participant friction)"
    - "Closing line below the paste blocks uses the SOFTENED hot-reload wording per research: 'Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart claude.'"
    - "No exclamation marks in slide prose (QUAL-04). Brain-rot vocabulary inside SKILL.md body is exempt per D-14."
  artifacts:
    - path: "pages/02-skills/04-hands-on-build.md"
      provides: "Section 04 — hands-on build (SKL-03 wire half)"
      contains: "mkdir -p ~/.claude/skills/pirate-mode"
  key_links:
    - from: "pages/02-skills.md"
      to: "pages/02-skills/04-hands-on-build.md"
      via: "src: include resolution"
      pattern: "src: ./02-skills/04-hands-on-build.md"
---

<objective>
Author Section 04 of Chapter 2 — the hands-on build half. Single slide (recommended; split to two slides only if dev-server overflow check during execution forces it). Two paste blocks:

- Step 1: ```bash``` block — `mkdir -p ~/.claude/skills/pirate-mode`
- Step 2: ```markdown``` block — the EXACT D-06 SKILL.md (frontmatter + body, byte-identical to Plan 03-04 Slide 2).

A one-line "this is the same SKILL.md from the last slide — paste it as-is" reconcile note sits between the two blocks to remove the "is this different?" friction (D-08 pattern, mirrors Phase 3 D-08 reconcile note).

Closing line uses the SOFTENED restart wording. Research confirmed hot-reload is now official (`[VERIFIED: code.claude.com changelog]`), so D-09's "softens to..." clause applies: *"Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart `claude`."* (NOT a mandatory pre-step.)

Covers SKL-03 wire half. SKL-03 fire half is Plan 03-06.

The SKILL.md content is repeated VERBATIM here — that's deliberate (D-08). Plan 03-04 shows it; Plan 03-05 re-shows it inside the paste callout. Both fences MUST have identical content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-skills/03-CONTEXT.md
@.planning/phases/02-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/02-skills/04-hands-on-build.md (single-slide two-block paste with softened restart caveat)</name>
  <files>pages/02-skills/04-hands-on-build.md</files>
  <read_first>
    - pages/03-hooks/04-hands-on-build.md (Phase 3 ANALOG — hands-on build with reconcile-note pattern and dim footnote)
    - pages/01-audience-setup/05-claude-files-primer.md (ANALOG — `<style scoped>` font-shrink retrofit pattern if overflow surfaces in Plan 03-07)
    - .planning/phases/02-skills/03-CONTEXT.md (D-06 EXACT SKILL.md content; D-08 two-step paste shape; D-09 softened hot-reload caveat)
    - .planning/phases/02-skills/03-RESEARCH.md (Pattern 7 Option A — recommended single-slide form; State of the Art — hot-reload is official)
  </read_first>
  <action>
Create the NEW file `pages/02-skills/04-hands-on-build.md` with EXACTLY these contents:

```markdown
# Build the skill

Step 1 — create the folder:

```bash
mkdir -p ~/.claude/skills/pirate-mode
```

Step 2 — open `~/.claude/skills/pirate-mode/SKILL.md` and paste:

```markdown
---
name: pirate-mode
description: Use when the user asks for pirate-mode, asks to explain
  something in pirate, or says "arrr." Responds in 2026 brain-rot pirate
  voice — opens with "Ahoy" and signs every reply "— per pirate-mode."
---

# Pirate Mode

Respond in pirate-speak laced with 2026 internet brain-rot vocabulary
(skibidi, rizz, no cap, sigma, ohio, gyat, lowkey). Open every response
with "Ahoy ye sigma" and close with "— per pirate-mode."
```

<div class="text-dim text-sm mt-4">
  This is the same SKILL.md from the last slide — paste it as-is. Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart <code>claude</code>.
</div>
```

CRITICAL — the ```markdown``` fenced block contains the EXACT D-06 SKILL.md, byte-identical to Plan 03-04 Slide 2. Every line, every word, every wrapping break. Do NOT reflow. Do NOT add/remove blank lines. The em-dash inside the description value (` — `) is U+2014.

CRITICAL — the closing dim helper merges two concerns into one paragraph:
1. The "same SKILL.md from the last slide — paste it as-is" reconcile note (D-08).
2. The SOFTENED hot-reload caveat (D-09, research-verified): *"Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart `claude`."*

Both must be present in that order, inside the single `<div class="text-dim text-sm mt-4">`. Inline-code `claude` via `<code>claude</code>` (HTML tag, because the surrounding container is raw HTML).

Notes:
- File has NO top-level frontmatter. First line is the H1 `# Build the skill`.
- No `---` separator at the bottom — this is a single-slide file.
- Shell block uses ```bash``` (already in slides.md shiki.langs).
- SKILL.md block uses ```markdown``` (already in slides.md shiki.langs).
- The `Step 1` and `Step 2` prose lines are plain markdown — NOT bolded with `**`. Phase 3's hands-on uses prose-style step labels, not bold. Match that.
- Inline code in the prose around the SKILL.md fence uses backticks: `` `~/.claude/skills/pirate-mode/SKILL.md` ``.
- No exclamation marks in slide prose (QUAL-04). Brain-rot vocabulary inside the ```markdown``` fence is content payload (exempt per D-14).
- If the slide overflows during the build spot-check (Plan 03-07), Plan 03-07 has a conditional retrofit task that appends the Phase 1 `<style scoped>` font-shrink — do NOT pre-emptively add it here.

Atomic commit (QUAL-02): `feat(phase-2): section 04 hands-on-build`.
  </action>
  <verify>
    <automated>test -f pages/02-skills/04-hands-on-build.md && grep -q "^# Build the skill$" pages/02-skills/04-hands-on-build.md && [ "$(grep -c '^---$' pages/02-skills/04-hands-on-build.md)" = "0" ] && grep -qF "mkdir -p ~/.claude/skills/pirate-mode" pages/02-skills/04-hands-on-build.md && grep -q "^name: pirate-mode$" pages/02-skills/04-hands-on-build.md && grep -qF 'description: Use when the user asks for pirate-mode, asks to explain' pages/02-skills/04-hands-on-build.md && grep -qF 'something in pirate, or says "arrr." Responds in 2026 brain-rot pirate' pages/02-skills/04-hands-on-build.md && grep -qF 'voice — opens with "Ahoy" and signs every reply "— per pirate-mode."' pages/02-skills/04-hands-on-build.md && grep -q "^# Pirate Mode$" pages/02-skills/04-hands-on-build.md && grep -qF '(skibidi, rizz, no cap, sigma, ohio, gyat, lowkey).' pages/02-skills/04-hands-on-build.md && grep -qF 'with "Ahoy ye sigma" and close with "— per pirate-mode."' pages/02-skills/04-hands-on-build.md && grep -qF "This is the same SKILL.md from the last slide — paste it as-is." pages/02-skills/04-hands-on-build.md && grep -qF "Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart" pages/02-skills/04-hands-on-build.md && grep -qF "<code>claude</code>" pages/02-skills/04-hands-on-build.md && [ "$(grep -v '^#' pages/02-skills/04-hands-on-build.md | grep -c '!')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/02-skills/04-hands-on-build.md` exists
    - Single-slide file: zero `^---$` slide separators
    - H1 is exactly `# Build the skill`
    - ```bash``` block contains `mkdir -p ~/.claude/skills/pirate-mode` verbatim
    - ```markdown``` block contains the EXACT D-06 SKILL.md, verified by 6 separate grep -F assertions covering frontmatter `name`, all three `description` lines, `# Pirate Mode` H2, brain-rot vocabulary list line, and the closing signature line
    - Reconcile note `This is the same SKILL.md from the last slide — paste it as-is.` present verbatim
    - Softened hot-reload caveat `Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart` present verbatim, with `<code>claude</code>` inline
    - Non-heading lines have zero `!` characters (the H1 starts with `#` so is excluded; SKILL.md body contains no `!`)
  </acceptance_criteria>
  <done>Section 04 ships with single-slide two-block paste + softened hot-reload caveat; committed atomically.</done>
</task>

</tasks>

<verification>
- Single-slide file (zero slide separators)
- D-06 SKILL.md body byte-exact (6 grep -F assertions)
- mkdir block present
- Reconcile note present
- Softened hot-reload caveat present (research-verified wording)
- Zero exclamation marks in non-heading prose
- Atomic commit `feat(phase-2): section 04 hands-on-build` (QUAL-02)
</verification>

<success_criteria>
SKL-03 wire half delivered. Participants have created `~/.claude/skills/pirate-mode/SKILL.md` on disk and know to restart `claude` only if the next slide's auto-invocation doesn't fire.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills/03-05-SUMMARY.md`.
</output>
