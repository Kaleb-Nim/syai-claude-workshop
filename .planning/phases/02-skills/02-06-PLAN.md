---
phase: 02-skills
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - pages/02-skills/05-hands-on-fire.md
autonomous: true
requirements: [SKL-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "pages/02-skills/05-hands-on-fire.md is a single slide with two micro-steps and two `> ✓ Check:` blockquotes"
    - "Step 1 trigger prompt is `Explain hooks in pirate-mode.` (matches the locked D-06 description)"
    - "Step 2 control prompt is a neutral technical question (`What's a git rebase?`) with no pirate vocabulary"
    - "✓ Check 1 contains the locked text proving auto-invocation (signature open + close + 'never typed a slash command')"
    - "✓ Check 2 contains the locked text proving non-invocation (no Ahoy, no — per pirate-mode, description didn't match)"
    - "Closing line: 'Two prompts, one skill. Claude chose when to use it — that's the whole point.'"
    - "No exclamation marks in slide prose (QUAL-04)"
  artifacts:
    - path: "pages/02-skills/05-hands-on-fire.md"
      provides: "Section 05 — hands-on fire (SKL-03 fire half)"
      contains: "Ahoy ye sigma"
  key_links:
    - from: "pages/02-skills.md"
      to: "pages/02-skills/05-hands-on-fire.md"
      via: "src: include resolution"
      pattern: "src: ./02-skills/05-hands-on-fire.md"
---

<objective>
Author Section 05 of Chapter 2 — the hands-on fire half. Single slide, two micro-steps inline:

- Step 1: Trigger auto-invocation with `Explain hooks in pirate-mode.` → ✓ Check on the locked signature ("Ahoy ye sigma" open + "— per pirate-mode" close + "never typed a slash command").
- Step 2: Control prompt `What's a git rebase?` → ✓ Check confirms no auto-invocation (no Ahoy, no signature, description didn't match).
- Closing line: *"Two prompts, one skill. Claude chose when to use it — that's the whole point."*

Both ✓ Checks use the plain `> ✓ Check:` blockquote pattern verified in Phase 1 (`pages/01-audience-setup/06-win-statusline.md` line 16). No custom Vue component. The literal Unicode `✓` glyph (U+2713) is used.

Covers SKL-03 fire half. This is the wow moment — Claude self-invokes a skill the participant just authored, then deliberately doesn't on a control prompt. The distinctive signature ("Ahoy ye sigma" + "— per pirate-mode") makes ✓ Check 1 unambiguous: Claude wouldn't unprompted produce that signature.

Per research Pitfall 2: Step 2 control prompt MUST NOT contain `pirate`, `arrr`, or any piratical vocabulary. `git rebase` is verified neutral.

Per research A1 (load-bearing assumption): the locked description triggers reliably on "explain hooks in pirate-mode" and does NOT trigger on "what is a git rebase." Plan 03-07 includes a dry-run task to confirm A1 before the build gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-skills/03-CONTEXT.md
@.planning/phases/02-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/02-skills/05-hands-on-fire.md (single-slide two-step fire with two ✓ Checks)</name>
  <files>pages/02-skills/05-hands-on-fire.md</files>
  <read_first>
    - pages/01-audience-setup/06-win-statusline.md (Phase 1 ANALOG — plain `> ✓ Check:` blockquote pattern verbatim, lines 16-18)
    - pages/01-audience-setup/07-win-claude-md.md (Phase 1 ANALOG — second ✓ Check pattern occurrence)
    - .planning/phases/02-skills/03-CONTEXT.md (D-10 — exact ✓ Check texts and closing line)
    - .planning/phases/02-skills/03-RESEARCH.md (Pattern 6 — locked ✓ Check rendering; Pitfall 2 — Step 2 must avoid pirate vocabulary; A1 — assumption load)
  </read_first>
  <action>
Create the NEW file `pages/02-skills/05-hands-on-fire.md` with EXACTLY these contents:

```markdown
# Fire it

Step 1 — run `claude` and ask, in plain English:

```text
Explain hooks in pirate-mode.
```

> ✓ Check: Claude's response opens with "Ahoy ye sigma" and ends with "— per pirate-mode." You never typed a slash command.

Step 2 — ask Claude a normal, unrelated question:

```text
What's a git rebase?
```

> ✓ Check: no "Ahoy," no "— per pirate-mode" — the skill didn't fire because nothing in your prompt matched its description.

<div class="text-dim text-sm mt-6">
  Two prompts, one skill. Claude chose when to use it — that's the whole point.
</div>
```

CRITICAL — the two ✓ Check blockquotes are LOCKED content (D-10). Each begins with `> ✓ Check:` (literal U+2713 `✓` glyph), single-line blockquote, with the EXACT body text from D-10:

- ✓ Check 1: `Claude's response opens with "Ahoy ye sigma" and ends with "— per pirate-mode." You never typed a slash command.`
- ✓ Check 2: `no "Ahoy," no "— per pirate-mode" — the skill didn't fire because nothing in your prompt matched its description.`

The em-dash in ✓ Check 2 (` — `) is U+2014.

CRITICAL — closing line is LOCKED (D-10): `Two prompts, one skill. Claude chose when to use it — that's the whole point.` Rendered inside `<div class="text-dim text-sm mt-6">`.

Notes:
- File has NO top-level frontmatter. First line is the H1 `# Fire it`.
- Single-slide file — no `---` separators.
- Step prose uses plain markdown `Step 1 — ...` / `Step 2 — ...` (em-dash, no bold) matching Phase 3 hands-on style.
- Prompt examples use ```text``` fenced blocks (Shiki accepts `text` by default; no langs change needed) — keeps them visually distinct from running code while staying copyable. This matches Phase 1's prompt-callout style.
- The `claude` mention in Step 1 prose uses backticks for inline code.
- No exclamation marks anywhere in the slide (QUAL-04). The trigger prompt ends with a period, not `!`.
- No `<v-clicks>` wrapper (D-13 — hands-on sections don't use it).
- Step 2's control question `What's a git rebase?` is verified neutral per research Pitfall 2. The apostrophe is straight `'` (U+0027). The `?` at the end is NOT an exclamation mark.

Atomic commit (QUAL-02): `feat(phase-2): section 05 hands-on-fire`.
  </action>
  <verify>
    <automated>test -f pages/02-skills/05-hands-on-fire.md && grep -q "^# Fire it$" pages/02-skills/05-hands-on-fire.md && [ "$(grep -c '^---$' pages/02-skills/05-hands-on-fire.md)" = "0" ] && grep -qF "Explain hooks in pirate-mode." pages/02-skills/05-hands-on-fire.md && grep -qF "What's a git rebase?" pages/02-skills/05-hands-on-fire.md && grep -qF '> ✓ Check: Claude'"'"'s response opens with "Ahoy ye sigma" and ends with "— per pirate-mode." You never typed a slash command.' pages/02-skills/05-hands-on-fire.md && grep -qF '> ✓ Check: no "Ahoy," no "— per pirate-mode" — the skill didn'"'"'t fire because nothing in your prompt matched its description.' pages/02-skills/05-hands-on-fire.md && grep -qF "Two prompts, one skill. Claude chose when to use it — that's the whole point." pages/02-skills/05-hands-on-fire.md && [ "$(grep -v '^#' pages/02-skills/05-hands-on-fire.md | grep -c '!')" = "0" ] && [ "$(grep -c '✓ Check:' pages/02-skills/05-hands-on-fire.md)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/02-skills/05-hands-on-fire.md` exists
    - Single-slide file: zero `^---$` slide separators
    - H1 is exactly `# Fire it`
    - Trigger prompt `Explain hooks in pirate-mode.` present verbatim
    - Control prompt `What's a git rebase?` present verbatim (no pirate vocabulary — neutral topic)
    - Exactly 2 occurrences of `✓ Check:` (literal U+2713 glyph)
    - ✓ Check 1 body byte-exact: `Claude's response opens with "Ahoy ye sigma" and ends with "— per pirate-mode." You never typed a slash command.`
    - ✓ Check 2 body byte-exact: `no "Ahoy," no "— per pirate-mode" — the skill didn't fire because nothing in your prompt matched its description.`
    - Closing line byte-exact: `Two prompts, one skill. Claude chose when to use it — that's the whole point.`
    - Closing line wrapped in `<div class="text-dim text-sm mt-6">`
    - Non-heading lines have zero `!` characters
    - Step 2 control prompt does NOT contain `pirate`, `arrr`, `Ahoy` (research Pitfall 2 — no piratical vocabulary in control)
  </acceptance_criteria>
  <done>Section 05 ships with two micro-steps + two ✓ Check blockquotes + closing line; committed atomically.</done>
</task>

</tasks>

<verification>
- Single-slide file (zero slide separators)
- Two `✓ Check:` blockquotes present
- Both ✓ Check bodies + closing line byte-exact (grep -F assertions)
- Step 2 control prompt verified neutral
- Zero exclamation marks in non-heading prose
- Atomic commit `feat(phase-2): section 05 hands-on-fire` (QUAL-02)
</verification>

<success_criteria>
SKL-03 fire half delivered. Participants experience auto-invocation (Step 1 ✓) and the deliberate non-trigger control (Step 2 ✓). The chapter closes on the model-invoked distinction, not a new concept.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills/03-06-SUMMARY.md`.
</output>
