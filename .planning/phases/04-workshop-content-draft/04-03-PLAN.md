---
phase: 04-workshop-content-draft
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - pages/02-subagents-sdk.md
autonomous: true
requirements: [CONT-04]
must_haves:
  truths:
    - "D-08: chapter follows shape title → why care → mental model → live walkthrough → exercise → recap"
    - "D-17: first slide uses `layout: section`"
    - "D-19: chapter covers when to spawn a subagent, the Task tool, `.claude/agents/<name>.md` shape with frontmatter, user-vs-Claude-invoked subagents, plus a brief @anthropic-ai/claude-agent-sdk snippet (≤15 lines) showing programmatic invocation OUTSIDE Claude Code"
    - "D-19: exercise = write `.claude/agents/code-reviewer.md` and invoke via Task tool"
    - "D-11/D-21: chapter contains ≥1 `✓ Check:` callout with copy-paste-precise expected output"
    - "D-10: every fenced code block ≤25 lines (SDK snippet ≤15 per D-19/Claude's-discretion)"
    - "D-16: real Claude Code APIs — `.claude/agents/*.md`, Task tool, @anthropic-ai/claude-agent-sdk; mark `<!-- VERIFY -->` for unsure fields"
    - "D-22: no screenshots"
    - "D-23: confident terse no-hype tone"
  artifacts:
    - path: "pages/02-subagents-sdk.md"
      provides: "Subagents & Agent SDK chapter content (~30 min)"
      contains: "Task"
  key_links:
    - from: "slides.md"
      to: "pages/02-subagents-sdk.md"
      via: "src: directive (created in 04-01)"
      pattern: "src:\\s*\\./pages/02-subagents-sdk\\.md"
---

<objective>
Draft the Subagents & Agent SDK chapter — `pages/02-subagents-sdk.md` — per D-19. ~30 min hands-on. The chapter teaches subagents from inside Claude Code (Task tool + `.claude/agents/*.md`), then bridges to the Agent SDK (the same primitive, programmatic, ≤15-line snippet).

Purpose: Second anchor topic per D-05 — "force-multiplier" frame. Builds on hooks ("Claude Code is programmable") by showing Claude can also spawn focused workers. The SDK bridge plants the seed that the same primitive runs outside the CLI.

Output: One markdown file with ~14-18 slides following D-08 shape exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-workshop-content-draft/04-CONTEXT.md
@.planning/phases/02-theme-visual-identity/02-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft pages/02-subagents-sdk.md following D-08 shape and D-19 substance</name>
  <files>pages/02-subagents-sdk.md</files>
  <read_first>
    - .planning/phases/04-workshop-content-draft/04-CONTEXT.md (D-08, D-09..D-12, D-16, D-17, D-19, D-21, D-22, D-23)
  </read_first>
  <action>
Create `pages/02-subagents-sdk.md` with the slide-by-slide structure below. Use Slidev's per-slide `---` separator. Slide 1 has `layout: section` per D-17.

The chapter has TWO halves: (1) subagents inside Claude Code, (2) the SDK bridge. The exercise (D-19) lives at the boundary — write a `.claude/agents/code-reviewer.md` and invoke it via Task. The SDK snippet comes after the exercise, as a "and here's the same primitive in your own code" payoff.

---

**Slide 1 — Section title** (`layout: section`)
```markdown
# Subagents & the Agent SDK

spawn focused workers; invoke from your own code
```

**Slide 2 — Why care (D-08 step 2)**
```markdown
# Why subagents

- **Independent context.** A subagent doesn't pollute the parent's context window with research it did.
- **Parallelism.** Three subagents can investigate three branches of a problem at once.
- **Specialization.** Different system prompts, different tool sets, different focus.

When you find yourself thinking "I need a fresh Claude for this" — that's a subagent.
```

**Slide 3 — Mental model: the Task tool**
```markdown
# The Task tool

Inside Claude Code, **Claude itself can spawn subagents** by calling the `Task` tool.

You don't call Task. Claude does — when its system prompt or your prompt says it should.

```
parent Claude → Task → fresh subagent → returns final message → parent continues
```

The subagent has its own context, its own tool budget, and its own system prompt (from the agent definition file).
```

**Slide 4 — Mental model: the agent definition file (D-19)**
```markdown
# `.claude/agents/<name>.md`

A subagent is a markdown file with frontmatter. The frontmatter declares the agent's
identity; the body becomes its system prompt.

```markdown
---
name: code-reviewer
description: Reviews diffs for bugs, style, and missed edge cases.
tools: [Read, Grep, Bash]
---

You are a strict but fair code reviewer.
Read the diff. Flag concrete issues with file:line references.
Skip style nits unless they materially affect readability.
```

`tools` is the allowlist — the subagent only sees these. Omit to inherit the parent's tool set. <!-- VERIFY: exact frontmatter keys (name, description, tools) -->
```

**Slide 5 — User-invoked vs Claude-invoked (D-19)**
```markdown
# Two ways in

- **Claude-invoked.** You ask the parent to do something; it decides a subagent is the right move and calls Task with the agent name.
- **User-invoked.** You explicitly say *"use the code-reviewer agent on this diff"* — the parent forwards.

Same primitive, different trigger. The agent file doesn't know or care which path called it.
```

**Slide 6 — Live walkthrough: list what's there**
```bash
# Many CC installs ship with example agents. List yours:
ls -la .claude/agents/ 2>/dev/null
ls -la ~/.claude/agents/ 2>/dev/null

# Project-local agents in .claude/agents/ override user-wide agents in ~/.claude/agents/.
```

```markdown
✓ Check: at least one of those directories exists. If not, you'll create one in the exercise.
```

**Slide 7 — Live walkthrough: anatomy of a real agent**
```bash
# Pick an existing agent and read it. If you don't have one, skip to Slide 8.
ls ~/.claude/agents/ | head -1
# example output: planner.md
cat ~/.claude/agents/planner.md | head -20
```

```markdown
Notice the shape: frontmatter (`name`, `description`, optional `tools`) + a body that reads as
a system prompt. That's the entire contract.
```

**Slide 8 — Exercise (D-19, D-08 step 5) — write code-reviewer.md**
```markdown
# Exercise (8 min) — write `code-reviewer`

1. Create `.claude/agents/code-reviewer.md` in this project:

```markdown
---
name: code-reviewer
description: Reviews recent changes for bugs, style, and missed edge cases.
tools: [Read, Grep, Bash]
---

You are a strict but fair code reviewer.
When invoked, run `git diff` to see recent changes.
Flag concrete issues with file:line references.
Skip style nits unless they materially affect readability.
End with a one-line verdict: APPROVE or REQUEST CHANGES.
```

2. In Claude Code, ask: *"use the code-reviewer agent on the most recent commit"*.
3. ✓ Check: Claude calls the Task tool with `subagent_type: "code-reviewer"` and you see a review that ends with `APPROVE` or `REQUEST CHANGES`.
```

**Slide 9 — Bridge: same primitive, outside Claude Code**
```markdown
# The SDK bridge

The Agent SDK exposes the same primitive — query a Claude agent — from your own code.
Same model, same tool semantics, no CLI wrapper.

`@anthropic-ai/claude-agent-sdk` on npm. TypeScript-first. Streams.
```

**Slide 10 — Live walkthrough: minimal SDK call (D-19, ≤15 lines)**
```ts
import { query } from "@anthropic-ai/claude-agent-sdk";

const result = query({
  prompt: "List the markdown files in this repo and summarize each in one line.",
});

for await (const message of result) {
  if (message.type === "result") {
    console.log(message.result);
  }
}
```

```markdown
That's the whole "spawn a Claude" surface. <!-- VERIFY: exact message.type discriminant for final result -->
```

**Slide 11 — When SDK, when CLI**
```markdown
# Which one when

- **CLI / inside Claude Code.** Interactive. You're at a terminal, riffing.
- **SDK.** Programmatic. Cron job, CI hook, your own app. No terminal in the loop.

The agent definition file format is the same on both sides. Write it once, invoke from either.
```

**Slide 12 — Recap + pointers (D-08 step 6)**
```markdown
# Subagents recap

- Subagents = fresh Claude with its own context, system prompt, tool set.
- Invoke from inside CC via the Task tool (Claude- or user-triggered).
- Define in `.claude/agents/<name>.md` with `name`, `description`, optional `tools`.
- Same primitive lives in `@anthropic-ai/claude-agent-sdk` for programmatic use.

Next: how to package your own commands and skills.
```

---

**Constraints:**
- D-10: every fenced code block ≤25 lines. The SDK snippet (slide 10) ≤15 lines per D-19.
- D-16: real APIs only. `.claude/agents/*.md`, Task tool, `@anthropic-ai/claude-agent-sdk` are real. Field names in agent frontmatter and SDK message shapes get `<!-- VERIFY -->` if not 100% confirmed.
- D-22: no screenshots.
- D-23: confident terse no-hype.
- D-12: no `<v-clicks>` (content is reference-shaped).
- D-17: slide 1 `layout: section` only.
- No inline hex literals.
- Each slide separated by `---` on its own line.
- 12-18 slides total.

**Important syntax note:** When showing a markdown code block (e.g. an agent definition that itself contains frontmatter), use **fenced blocks with explicit language**: ` ```markdown ` opening, ` ``` ` closing. Slidev/Shiki handle nested triple-backticks via this pattern. Do NOT use 4-backtick fences — Shiki may not parse them. If a code block needs to contain triple-backticks (e.g. showing how to write a markdown file with code in it), use indentation + lang annotation as a fallback. Most slides here just show one fence at a time, so no nesting needed.
  </action>
  <verify>
    <automated>
      test -f pages/02-subagents-sdk.md &&
      head -5 pages/02-subagents-sdk.md | grep -q '^layout: section$' &&
      # D-19 substance
      grep -q 'Task' pages/02-subagents-sdk.md &&
      grep -q '\.claude/agents' pages/02-subagents-sdk.md &&
      grep -q 'code-reviewer' pages/02-subagents-sdk.md &&
      grep -q '@anthropic-ai/claude-agent-sdk' pages/02-subagents-sdk.md &&
      # at least one ✓ Check:
      [ "$(grep -c '✓ Check:' pages/02-subagents-sdk.md)" -ge 1 ] &&
      # D-08 sections
      grep -qE '^# Why subagents' pages/02-subagents-sdk.md &&
      grep -qE '^# Exercise' pages/02-subagents-sdk.md &&
      grep -qE '^# Subagents recap|^# Recap' pages/02-subagents-sdk.md &&
      # no screenshots / no hype
      ! grep -qiE '!{2,}|amazing|awesome|incredible|<img |!\[[^]]+\]\(' pages/02-subagents-sdk.md &&
      # no inline hex
      ! grep -E '#[0-9a-fA-F]{6}' pages/02-subagents-sdk.md &&
      # ≥12 slides (slide separators)
      [ "$(grep -c '^---$' pages/02-subagents-sdk.md)" -ge 12 ] &&
      # every fenced code block ≤25 lines; SDK snippet (the one starting with import from claude-agent-sdk) ≤15
      awk '/^```/{c++; if(c%2==1){n=0; lang=$0} else {if(n>25){print "OVERLONG block ending at line " NR; exit 1}}; next} c%2==1{n++}' pages/02-subagents-sdk.md &&
      # find the SDK snippet block specifically and assert ≤15 lines
      python3 -c "
import re, sys
text = open('pages/02-subagents-sdk.md').read()
# Find ts code blocks containing the SDK import
blocks = re.findall(r'\`\`\`ts\n(.*?)\n\`\`\`', text, re.DOTALL)
for b in blocks:
    if 'claude-agent-sdk' in b:
        n = b.count(chr(10)) + 1
        if n > 15:
            print(f'SDK snippet too long: {n} lines'); sys.exit(1)
print('ok')
"
    </automated>
  </verify>
  <done>pages/02-subagents-sdk.md exists, has section-layout title, covers Task tool + agent file shape + user-vs-Claude-invoked + ≤15-line SDK snippet, includes the code-reviewer exercise with ✓ Check, and a recap. All code blocks ≤25 lines, SDK snippet ≤15.</done>
</task>

</tasks>

<verification>
The chapter delivers D-19's full substance in ~30 min. Wave 3 verifies build/presenter/PDF.
</verification>

<success_criteria>
- pages/02-subagents-sdk.md exists, ≥12 slides
- Task tool, `.claude/agents/`, `@anthropic-ai/claude-agent-sdk` all named
- Exercise slide writes `code-reviewer.md` and invokes via Task
- SDK snippet present and ≤15 lines
- ≥1 `✓ Check:` callout
- No screenshots, no inline hex, no hype
</success_criteria>

<output>
`.planning/phases/04-workshop-content-draft/04-03-SUMMARY.md` with: slide count, SDK snippet line count, all `<!-- VERIFY -->` markers and what each flags.
</output>
</content>
</invoke>