---
phase: 02-theme-visual-identity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - slides.md
autonomous: true
requirements: [THEM-01, THEM-03, THEM-04]
must_haves:
  truths:
    - "Deck loads with a dark color scheme (colorSchema: dark in headmatter)."
    - "Headings use Inter sans, code uses JetBrains Mono — declared via Slidev fonts: headmatter."
    - "Shiki highlighter is pinned to a single dark theme that pairs with #1A1714."
    - "Shiki language list is explicitly pinned (ts, tsx, bash, json, md, yaml minimum)."
    - "The 3 placeholder slides remain unchanged in content (only headmatter expands)."
  artifacts:
    - path: "slides.md"
      provides: "Slidev headmatter declaring colorSchema, fonts, highlighter, shiki.langs"
      contains: "colorSchema: dark"
  key_links:
    - from: "slides.md headmatter"
      to: "Slidev build pipeline"
      via: "frontmatter keys (colorSchema, fonts, highlighter, shiki)"
      pattern: "colorSchema:\\s*dark"
---

<objective>
Expand the existing `slides.md` headmatter with the locked theme contract: dark color schema, Inter + JetBrains Mono fonts via Slidev `fonts:`, Shiki highlighter pinned to `vitesse-dark`, and an explicit Shiki language list. The 3 existing placeholder slides MUST stay byte-identical below the headmatter — Phase 2 only adds frontmatter keys.

Purpose: THEM-01 (dark mode), THEM-03 (typography), and the headmatter half of THEM-04 (Shiki theme + pinned langs). This plan establishes the framework-level theme contract before any UnoCSS tokens or CSS overrides are added.

Output: Updated `slides.md` whose headmatter declares `colorSchema`, `fonts`, `highlighter`, and `shiki.langs`. Slide bodies unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-theme-visual-identity/02-CONTEXT.md
@CLAUDE.md
@slides.md

<interfaces>
<!-- Current slides.md headmatter (Phase 1 baseline). The new keys are ADDED to this block. -->
<!-- Slide bodies (everything below the closing --- of the first frontmatter) MUST NOT change. -->

Current headmatter (lines 1-10 of slides.md):
```yaml
---
title: syai-claude-workshop
info: |
  Advanced Claude Code workshop scaffold.
  Phase 1 placeholder deck.
class: text-center
drawings:
  persist: false
mdc: true
---
```

Slidev frontmatter keys to add (per https://sli.dev/custom/):
- `colorSchema: dark`
- `fonts:` block (sans + mono via Google Fonts; Slidev fetches at build time)
- `highlighter: shiki`
- `shiki:` block with `theme:` and `langs:`

Reference shape from Slidev docs:
```yaml
fonts:
  sans: 'Inter'
  mono: 'JetBrains Mono'
highlighter: shiki
shiki:
  theme: 'vitesse-dark'
  langs: ['ts', 'tsx', 'bash', 'json', 'md', 'yaml']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme contract to slides.md headmatter</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (entire current file — confirm slide bodies before editing)
    - .planning/phases/02-theme-visual-identity/02-CONTEXT.md (D-09, D-10, D-12, D-13)
    - CLAUDE.md (Theme Recommendation section — confirms `colorSchema: 'dark'` and `fonts.mono` guidance)
  </read_first>
  <action>
Edit ONLY the opening YAML frontmatter block of `slides.md` (lines 1–10 in the current file, between the first two `---` lines). Preserve every existing key (`title`, `info`, `class`, `drawings`, `mdc`). Append the four new keys below `mdc: true`. Do NOT touch any line below line 10 — the 3 placeholder slides stay byte-identical.

The final headmatter block MUST be exactly:

```yaml
---
title: syai-claude-workshop
info: |
  Advanced Claude Code workshop scaffold.
  Phase 1 placeholder deck.
class: text-center
drawings:
  persist: false
mdc: true
colorSchema: dark
fonts:
  sans: 'Inter'
  mono: 'JetBrains Mono'
highlighter: shiki
shiki:
  theme: 'vitesse-dark'
  langs: ['ts', 'tsx', 'bash', 'json', 'md', 'yaml']
---
```

Notes on the locked values:
- `colorSchema: dark` (per D-01 + THEM-01 — unquoted; Slidev accepts both quoted and unquoted, unquoted matches existing key style in this file).
- `fonts.sans: 'Inter'` and `fonts.mono: 'JetBrains Mono'` per D-09/D-10. Slidev defaults to Google Fonts at build time (`fonts.local: false`); accept that default per D-09 — do NOT add `fonts.local`, do NOT add `fonts.fallbacks`, do NOT add weight overrides (D-11: don't fight the framework).
- `highlighter: shiki` is the Slidev default but pinned explicitly so future config changes don't drift (D-12).
- `shiki.theme: 'vitesse-dark'` per D-12 recommendation. Do NOT pick `github-dark-default` or `one-dark-pro` unless contrast verification in 02-04 fails — this plan ships vitesse-dark.
- `shiki.langs` array uses the exact 6-item list from D-13 (no `vue` per D-13 — defer to Phase 3+).

Do NOT:
- Modify any line below the closing `---` on what is currently line 10 (after edits, it will be line 17).
- Reorder existing keys.
- Quote `dark` (Slidev accepts unquoted; matches `mdc: true` style).
- Add `themeConfig.primary` here (that's per-theme; the rust accent flows through UnoCSS in 02-02, not theme config).
- Add `lineNumbers` (D-06: off by default; per-slide opt-in remains functional without a global key).

After editing, confirm with `bunx slidev build` that the build still exits 0.
  </action>
  <verify>
    <automated>cd /Users/kalebnim/Documents/GitHub/syai-claude-workshop && grep -q '^colorSchema: dark$' slides.md && grep -q "^  sans: 'Inter'$" slides.md && grep -q "^  mono: 'JetBrains Mono'$" slides.md && grep -q '^highlighter: shiki$' slides.md && grep -q "^  theme: 'vitesse-dark'$" slides.md && grep -q "ts.*tsx.*bash.*json.*md.*yaml" slides.md && bunx slidev build</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '^colorSchema: dark$' slides.md` returns `1`.
- `grep -c '^highlighter: shiki$' slides.md` returns `1`.
- `grep -c "sans: 'Inter'" slides.md` returns `1`.
- `grep -c "mono: 'JetBrains Mono'" slides.md` returns `1`.
- `grep -c "theme: 'vitesse-dark'" slides.md` returns `1`.
- `slides.md` contains the literal substring `langs: ['ts', 'tsx', 'bash', 'json', 'md', 'yaml']`.
- The slide-body content (everything after the first closing `---`) is byte-identical to the pre-edit version. Verify by extracting from the line containing `# syai-claude-workshop` to end-of-file and diffing against the Phase 1 content shown in `01-01-SUMMARY.md` / current git HEAD `slides.md`. `git diff slides.md -- :!slides.md` is not the right tool; instead run: `git show HEAD:slides.md | tail -n +11 | diff - <(tail -n +18 slides.md)` returns empty (zero diff).
- `bunx slidev build` exits 0 and produces `dist/index.html`.
- No `vue` appears in the `shiki.langs` array (`grep -E "langs:.*vue" slides.md` returns no matches).
  </acceptance_criteria>
  <done>slides.md headmatter declares the locked theme contract (colorSchema, fonts, highlighter, shiki.theme, shiki.langs) per D-09/D-10/D-12/D-13; slide bodies unchanged; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → CDN (Google Fonts) | Slidev's font pipeline fetches Inter and JetBrains Mono from Google Fonts at build time. Untrusted CSS/font payload crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `fonts:` Google Fonts fetch | accept | D-09 explicitly accepts the network dependency; v2 self-hosting deferred. No PII leaks (font request is anonymous). |
| T-02-02 | Tampering | `slides.md` headmatter | mitigate | Headmatter is committed to git; review-on-PR catches changes. No runtime mutation path. |
</threat_model>

<verification>
- `bunx slidev build` exits 0.
- Headmatter contains all five required keys (colorSchema, fonts.sans, fonts.mono, highlighter, shiki.theme, shiki.langs).
- Slide bodies unchanged from Phase 1 baseline.
</verification>

<success_criteria>
slides.md headmatter encodes the dark/Inter/JetBrains-Mono/vitesse-dark contract; the existing 3 placeholder slides remain intact; Slidev build still produces a clean `dist/`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-theme-visual-identity/02-01-SUMMARY.md` per the standard summary template.
</output>
