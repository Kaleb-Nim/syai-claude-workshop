---
phase: 03-skills
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - pages/03-skills/03-how-skills-work.md
autonomous: true
requirements: [SKL-01, SKL-02, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "pages/03-skills/03-how-skills-work.md contains exactly 3 slides separated by `---` rulers"
    - "Slide 1 shows the directory tree `~/.claude/skills/pirate-mode/ └── SKILL.md` + user-vs-project one-line note"
    - "Slide 2 shows the EXACT D-06 SKILL.md content (frontmatter + body) in a fenced ```markdown``` block"
    - "Slide 3 renders the 3-tier progressive disclosure visual as a fenced markdown table with Discovery / Activation / Execution + token costs + trailing 'more fields exist' line"
    - "No exclamation marks in slide prose (QUAL-04)"
  artifacts:
    - path: "pages/03-skills/03-how-skills-work.md"
      provides: "Section 03 — file shape + minimal frontmatter + 3-tier progressive disclosure (SKL-01 mechanism half + SKL-02)"
      contains: "name: pirate-mode"
  key_links:
    - from: "pages/03-skills.md"
      to: "pages/03-skills/03-how-skills-work.md"
      via: "src: include resolution"
      pattern: "src: ./03-skills/03-how-skills-work.md"
---

<objective>
Author Section 03 of Chapter 3 — "how skills work" mechanism teaching. 3 slides:

- Slide 1: File shape (directory tree) + user-vs-project one-line note (D-05).
- Slide 2: Minimal valid SKILL.md (`name` + `description` + body) in a fenced markdown block (D-06 EXACT content — this is the same SKILL.md participants paste in Section 04).
- Slide 3: 3-tier progressive disclosure as a fenced markdown table — Discovery → Activation → Execution with token costs (D-07) + trailing "more fields exist" line.

Covers SKL-01 mechanism half (file shape, location, frontmatter) + SKL-02 (progressive disclosure mental model).

The SKILL.md body on Slide 2 is deliberately repeated verbatim in Plan 03-05 (Section 04) — D-08 calls for "this is the same SKILL.md from the last slide — paste it as-is."

Per research's primary recommendation: render the 3-tier visual as a fenced markdown table (NOT a two-cols layout, NOT a hand-drawn ASCII diagram) — table renders cleanest in `vitesse-dark` with JetBrains Mono and matches the Phase 1 compact-block precedent.

Shiki language fence: use ```markdown``` for the SKILL.md block — `md` is already in `slides.md` shiki.langs (verified). Frontmatter renders cleanly with markdown highlighting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-skills/03-CONTEXT.md
@.planning/phases/03-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/03-skills/03-how-skills-work.md (3 slides — file shape + SKILL.md + 3-tier table)</name>
  <files>pages/03-skills/03-how-skills-work.md</files>
  <read_first>
    - pages/02-hooks/03-how-hooks-work.md (Phase 2 ANALOG — multi-slide mechanism section with code fences and dim helpers)
    - pages/01-audience-setup/05-claude-files-primer.md (ANALOG — directory tree rendering pattern + scoped style block as overflow fallback)
    - .planning/phases/03-skills/03-CONTEXT.md (D-05 file shape; D-06 EXACT SKILL.md; D-07 3-tier model)
    - .planning/phases/03-skills/03-RESEARCH.md (Pattern 5 — recommended 3-tier table form verbatim; Code Examples — exact pirate-mode SKILL.md)
    - slides.md (line 17 — shiki.langs confirms `md` is registered, so ```markdown``` fence will highlight correctly)
  </read_first>
  <action>
Create the NEW file `pages/03-skills/03-how-skills-work.md` with EXACTLY these contents:

```markdown
# Where skills live

```text
~/.claude/skills/pirate-mode/
  └── SKILL.md
```

<div class="text-dim text-sm mt-6">
  User-level today. Project-level (<code>.claude/skills/</code> inside a repo) versions with the codebase — same shape.
</div>

---

# The smallest valid SKILL.md

```markdown
---
name: pirate-mode
description: Use when the user asks for pirate-mode, asks to explain
  something in pirate, or says "arrr." Responds in 2026 brain-rot pirate
  voice — opens with "Ahoy" and signs every reply "— per pirate-mode."
---

# Pirate Mode

Respond in pirate-speak laced with 2026 internet brain-rot vocabulary
(skibidi, rizz, no cap, sigma, ohio, gyat, lowkey). Open every response
with "Ahoy ye sigma" and close with "— per pirate-mode."
```

<div class="text-dim text-sm mt-4">
  <code>description</code> has two jobs: tell humans what the skill does, and tell Claude when to load it.
</div>

---

# How Claude loads a skill

| Tier | What loads | Token cost |
|------|------------|------------|
| **Discovery** | `name` + `description` only | ~100 tokens / skill |
| **Activation** | Full `SKILL.md` body | ~1K–5K tokens |
| **Execution** | Scripts, reference files, templates | only what the task needs |

<div class="text-dim text-sm mt-6">
  more frontmatter fields exist (<code>allowed-tools</code>, <code>disable-model-invocation</code>, <code>paths</code>…) — <code>name</code> + <code>description</code> is enough today.
</div>
```

CRITICAL — Slide 2 SKILL.md content is LOCKED (D-06). The fenced ```markdown block must contain this EXACT body — every word, every line break, every character of the description (which wraps across 3 lines), the H1 `# Pirate Mode`, and the 3-line body. The wrapping inside the `description:` YAML value uses YAML's "fold via continuation" (leading whitespace on continuation lines is part of the value semantically but renders as single-spaced). Do NOT reflow.

CRITICAL — Slide 3 table is LOCKED in shape (D-07). The 3 tiers + their token-cost cells + trailing "more fields exist" line are non-negotiable. Bold the tier names in the first column using `**Discovery**` etc. Use inline code (backticks) on the field names in the trailing line: `<code>allowed-tools</code>`, `<code>disable-model-invocation</code>`, `<code>paths</code>` (HTML `<code>` tags since they live inside a `<div>` — backticks won't render inside raw HTML).

Notes:
- File has NO top-level frontmatter. First line is the first H1.
- Slides separated by `---` on its own line with blank lines above/below.
- Slide 1 directory tree uses ```text``` fence (Shiki accepts `text` by default; no langs edit needed). The U+2514 `└` and U+2500 `─` characters render correctly in JetBrains Mono.
- Slide 2 SKILL.md uses ```markdown``` fence (`md` is in slides.md shiki.langs at line 17 — verified).
- The em-dash inside the description value (` — `) is U+2014 — copy from D-06 verbatim. Curly quotes are NOT used; straight `"` (U+0022) and `'` (U+0027) only.
- No exclamation marks in slide prose (QUAL-04). Brain-rot vocabulary inside the SKILL.md body is content of the demo skill, not slide prose — exempt per D-14.

Atomic commit (QUAL-02): `feat(phase-3): section 03 how-skills-work`.
  </action>
  <verify>
    <automated>test -f pages/03-skills/03-how-skills-work.md && [ "$(grep -c '^---$' pages/03-skills/03-how-skills-work.md)" = "2" ] && grep -q "^# Where skills live$" pages/03-skills/03-how-skills-work.md && grep -q "^# The smallest valid SKILL.md$" pages/03-skills/03-how-skills-work.md && grep -q "^# How Claude loads a skill$" pages/03-skills/03-how-skills-work.md && grep -q "~/.claude/skills/pirate-mode/" pages/03-skills/03-how-skills-work.md && grep -qF "└── SKILL.md" pages/03-skills/03-how-skills-work.md && grep -q "^name: pirate-mode$" pages/03-skills/03-how-skills-work.md && grep -qF 'description: Use when the user asks for pirate-mode, asks to explain' pages/03-skills/03-how-skills-work.md && grep -qF 'something in pirate, or says "arrr." Responds in 2026 brain-rot pirate' pages/03-skills/03-how-skills-work.md && grep -qF 'voice — opens with "Ahoy" and signs every reply "— per pirate-mode."' pages/03-skills/03-how-skills-work.md && grep -q "^# Pirate Mode$" pages/03-skills/03-how-skills-work.md && grep -qF '(skibidi, rizz, no cap, sigma, ohio, gyat, lowkey).' pages/03-skills/03-how-skills-work.md && grep -qF 'Open every response' pages/03-skills/03-how-skills-work.md && grep -qF 'with "Ahoy ye sigma" and close with "— per pirate-mode."' pages/03-skills/03-how-skills-work.md && grep -q "\*\*Discovery\*\*" pages/03-skills/03-how-skills-work.md && grep -q "\*\*Activation\*\*" pages/03-skills/03-how-skills-work.md && grep -q "\*\*Execution\*\*" pages/03-skills/03-how-skills-work.md && grep -q "~100 tokens / skill" pages/03-skills/03-how-skills-work.md && grep -qF "<code>allowed-tools</code>" pages/03-skills/03-how-skills-work.md && grep -qF "<code>disable-model-invocation</code>" pages/03-skills/03-how-skills-work.md && grep -qF "<code>paths</code>" pages/03-skills/03-how-skills-work.md && [ "$(grep -v '^#' pages/03-skills/03-how-skills-work.md | grep -c '!')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-skills/03-how-skills-work.md` exists
    - Exactly 2 slide separators (`^---$`) — yielding 3 slides
    - All 3 H1 lines present in order: `# Where skills live`, `# The smallest valid SKILL.md`, `# How Claude loads a skill`
    - Slide 1 directory tree contains `~/.claude/skills/pirate-mode/` AND `└── SKILL.md` (U+2514 + U+2500 ASCII art)
    - Slide 1 user-vs-project note byte-exact: `User-level today. Project-level (<code>.claude/skills/</code> inside a repo) versions with the codebase — same shape.`
    - Slide 2 SKILL.md frontmatter contains `name: pirate-mode`
    - Slide 2 SKILL.md description spans the EXACT three locked-text lines: `Use when the user asks for pirate-mode, asks to explain`, `something in pirate, or says "arrr." Responds in 2026 brain-rot pirate`, `voice — opens with "Ahoy" and signs every reply "— per pirate-mode."`
    - Slide 2 SKILL.md body contains `# Pirate Mode` H2 (rendered) AND the brain-rot vocabulary list `(skibidi, rizz, no cap, sigma, ohio, gyat, lowkey).`
    - Slide 2 SKILL.md body ends with `with "Ahoy ye sigma" and close with "— per pirate-mode."`
    - Slide 3 table has bolded tier names `**Discovery**`, `**Activation**`, `**Execution**`
    - Slide 3 table contains `~100 tokens / skill` cell
    - Slide 3 trailing line names all three deferred fields via `<code>` tags: `allowed-tools`, `disable-model-invocation`, `paths`
    - Non-comment lines have zero `!` characters (excludes the H1 lines starting with `#`); note: the SKILL.md body inside the ```markdown``` fence is content payload, not slide prose, and contains no `!` anyway
  </acceptance_criteria>
  <done>Section 03 ships with file shape + SKILL.md + 3-tier table; committed atomically.</done>
</task>

</tasks>

<verification>
- 3 slides separated by 2 `---` rulers
- D-06 SKILL.md body byte-exact (verified by 8 separate grep -F assertions on each load-bearing line)
- 3-tier table renders with all three tiers, token costs, and trailing fields line
- User-vs-project one-line note present on Slide 1
- Zero exclamation marks in non-heading prose
- Atomic commit `feat(phase-3): section 03 how-skills-work` (QUAL-02)
</verification>

<success_criteria>
SKL-01 mechanism half + SKL-02 mental model delivered. Participants now know: where the file lives, what's inside it (just two frontmatter fields + a body), and why this format scales (3-tier lazy loading at ~100 tokens/skill at discovery). They're primed to paste it in Section 04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills/03-04-SUMMARY.md`.
</output>
