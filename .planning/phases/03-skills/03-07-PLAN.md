---
phase: 03-skills
plan: 07
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
files_modified: []
autonomous: false
requirements: [QUAL-01, QUAL-04]
must_haves:
  truths:
    - "Dry-run (instructor-side, BEFORE the build): `claude` session with `~/.claude/skills/pirate-mode/SKILL.md` installed responds to `Explain hooks in pirate-mode.` with the locked signature (Ahoy ye sigma open + — per pirate-mode close). If the instructor doesn't have pirate-mode installed locally, this task degrades gracefully — log skip and continue."
    - "`bunx slidev build` exits 0 with all 5 Phase 3 section files wired"
    - "Build artifact `dist/index.html` exists after the build"
    - "Presenter mode renders Chapter 3 from cover through Section 05 closing line without console errors (human-verified)"
    - "All 3 LOCKED QUOTES on Section 02 slides 3-5 render byte-exact in presenter mode"
    - "Section 04 SKILL.md block is legible at presenter resolution OR has been retrofitted with the Phase 1 `<style scoped>` font-shrink"
    - "Section 05 two `> ✓ Check:` blockquotes render with the literal ✓ glyph in dark theme"
  artifacts:
    - path: "dist/index.html"
      provides: "Static SPA build output (build success indicator)"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "bunx slidev build"
      to: "dist/index.html"
      via: "Slidev → Vite SPA pipeline"
      pattern: "bunx slidev build"
---

<objective>
Close Chapter 3 with the QUAL-01 build + presenter spot-check gate. Mirrors Phase 1 Plan 01-08 and Phase 2 Plan 02-06 shapes, with one addition: per research's A1 dry-run gate recommendation, a manual `autonomous: false` dry-run runs FIRST. The dry-run validates that the locked `pirate-mode` description triggers reliably on the Section 05 prompt and does NOT trigger on the control — the load-bearing assumption for the chapter's wow payoff.

Three tasks:
1. Dry-run (autonomous: false, gracefully skippable if instructor lacks pirate-mode locally). Validates A1.
2. `bunx slidev build` (autonomous: true). Must exit 0 + produce `dist/index.html`.
3. Presenter spot-check (autonomous: false). Walks all 5 sections, verifies the 3 locked quotes + SKILL.md + 3-tier table + ✓ Check blockquotes.

Plan 06's autonomous build task does NOT depend on the dry-run — if the dry-run is skipped, the build still runs. The dry-run only blocks if it RUNS and FAILS (in which case D-06 needs revisiting in `/gsd-discuss-phase`).

Conditional retrofit: if Section 04's SKILL.md block visually overflows at presenter resolution, the presenter spot-check resumes with `approved-with-retrofit` and an optional 4th task applies the Phase 1 `<style scoped>` font-shrink pattern. This mirrors Phase 2 Plan 02-06 Task 3.

Because spot-check + dry-run are human-driven, this plan is `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-skills/03-CONTEXT.md
@.planning/phases/03-skills/03-RESEARCH.md
@.planning/phases/01-audience-setup/01-08-PLAN.md
@.planning/phases/02-hooks/02-06-PLAN.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 1: Dry-run — validate A1 (pirate-mode auto-invocation)</name>
  <files>(read-only — no source files modified)</files>
  <read_first>
    - .planning/phases/03-skills/03-RESEARCH.md (Assumption A1 — load-bearing; section "Validation Architecture" — manual dry-run is the only available verification surface)
    - .planning/phases/03-skills/03-CONTEXT.md (D-06 locked description; D-10 trigger prompts)
    - pages/03-skills/05-hands-on-fire.md (the prompts and ✓ Check texts the presenter will reproduce)
  </read_first>
  <action>See `<how-to-verify>`. Presenter runs the dry-run on their own machine and resumes on the `<resume-signal>`. If the instructor has no local `pirate-mode` skill installed (it's a participant artifact, not in the repo), the task degrades gracefully: resume with `skipped — no local pirate-mode` and continue to Task 2.</action>
  <verify><automated>echo "human-verify dry-run — see how-to-verify section (non-blocking — skippable if no local pirate-mode)"</automated></verify>
  <done>Presenter resumes with `dry-run-passed`, `dry-run-failed`, or `skipped — no local pirate-mode`. Only `dry-run-failed` blocks — that path requires returning to `/gsd-discuss-phase` to revisit D-06.</done>
  <what-built>
The chapter's Section 04 + Section 05 sequence reproduces what participants will do live. This dry-run is the only pre-build verification of A1 — that the locked description triggers reliably on the locked prompt and stays silent on the control.
  </what-built>
  <how-to-verify>
1. If you do NOT have `~/.claude/skills/pirate-mode/SKILL.md` on your local machine, skip this task by resuming with `skipped — no local pirate-mode`. (The skill is a participant exercise artifact, not a repo file. Skipping is a valid outcome and does NOT block the build.)

2. If you DO have it (you built it during a personal walkthrough), proceed:

   a. Confirm the file exists and matches the D-06 content:

      ```bash
      cat ~/.claude/skills/pirate-mode/SKILL.md
      ```

      Expected: contains `name: pirate-mode`, the full locked description spanning 3 lines, `# Pirate Mode` H1, and the body ending in `with "Ahoy ye sigma" and close with "— per pirate-mode."`. If the file is missing or different, either (a) install the D-06 content first then re-run, or (b) skip per option 1.

   b. Start a fresh `claude` session (exit any existing session first to ensure clean state):

      ```bash
      claude
      ```

   c. Type the Section 05 Step 1 trigger prompt EXACTLY:

      ```text
      Explain hooks in pirate-mode.
      ```

   d. Observe the response. PASS criteria:
      - Response opens with `Ahoy ye sigma` (or extremely close — `Ahoy` + brain-rot variant on `ye sigma`).
      - Response ends with `— per pirate-mode.` (em-dash, exact closing signature).
      - You did NOT type `/pirate-mode` or any slash command. Claude self-invoked.

      FAIL criteria: response is generic / non-pirate / lacks the closing signature.

   e. In the same session, type the Section 05 Step 2 control prompt EXACTLY:

      ```text
      What's a git rebase?
      ```

   f. Observe. PASS criteria:
      - Response is a normal explanation of `git rebase`.
      - No "Ahoy," no "— per pirate-mode," no pirate vocabulary anywhere.

      FAIL criteria: response contains pirate signature (spurious trigger — description is too broad).

3. If Step (d) passes AND Step (f) passes → resume with `dry-run-passed`. Proceed to Task 2.

4. If EITHER Step (d) or Step (f) fails → resume with `dry-run-failed` and describe which check failed and the actual response. This indicates D-06's description wording needs to be revisited — return to `/gsd-discuss-phase 3` because D-06 is locked content.

5. If you cannot run a `claude` session for any reason → resume with `skipped — no local pirate-mode`. The build task (Task 2) proceeds regardless.
  </how-to-verify>
  <resume-signal>Type `dry-run-passed`, `dry-run-failed: <description>`, or `skipped — no local pirate-mode`.</resume-signal>
  <acceptance_criteria>
    - If dry-run runs: Step 1 prompt produces locked signature (Ahoy ye sigma open + — per pirate-mode close); Step 2 control produces no pirate signature
    - If skipped: presenter explicitly noted `skipped — no local pirate-mode` and task does not block subsequent tasks
    - If failed: presenter described which check failed; this halts the phase and routes back to discuss-phase
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run bunx slidev build and confirm exit 0 + dist/index.html</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (QUAL-01 wording)
    - .planning/phases/02-hooks/02-06-PLAN.md (Phase 2 build plan — Task 1 failure-signature playbook applies verbatim)
    - .planning/phases/01-audience-setup/01-08-PLAN.md (Phase 1 ANALOG)
    - .planning/phases/01-audience-setup/01-08-SUMMARY.md (port-3030 stale-process gotcha)
    - CLAUDE.md (Bun-only — `bunx`, NEVER `npx`)
  </read_first>
  <action>
From the repo root, run the production build:

```bash
bunx slidev build
```

Must:
1. Exit with status `0`.
2. Produce `dist/index.html`.
3. Emit no Slidev / Vite / Shiki ERROR-level messages (warnings tolerable).

Common failure signatures (diagnose, then return to the offending plan — do NOT mask):
- `Imported markdown file not found: ./pages/03-skills/0N-*.md` → orchestrator `src:` path mismatch (Plan 03-01 regression — paths must be relative to `pages/`).
- Missing `pages/03-skills/0N-*.md` → corresponding Wave 2 plan failed.
- Shiki lang error for ` ```markdown ` → unlikely (`md` is in `slides.md` shiki.langs at line 17), but if seen confirm `slides.md` head matter (lines 1-18) was not edited (Plan 03-01 regression).
- `vite.config.ts` warning → if accidentally introduced, delete it (CLAUDE.md constraint, Slidev issue #2043).

Do NOT modify any source file from this task to make the build pass. Build failures indicate a Wave 2 plan defect; surface as a blocker.
  </action>
  <verify>
    <automated>bunx slidev build && test -f dist/index.html && grep -q "<!DOCTYPE html>" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `bunx slidev build` exits with status `0`
    - File `dist/index.html` exists after the build
    - `grep -q "<!DOCTYPE html>" dist/index.html` succeeds
    - Build output contains no `ERROR` lines (warnings OK)
    - No `vite.config.ts` was created or modified by this plan
    - `slides.md` head matter (lines 1-18) byte-identical to start of Phase 3
  </acceptance_criteria>
  <done>Build passes; static SPA artifact exists at `dist/index.html`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Presenter spot-check Chapter 3 (human-verify)</name>
  <files>(read-only — no source files modified by default; conditional retrofit in Task 4 if overflow seen)</files>
  <read_first>
    - .planning/phases/03-skills/03-CONTEXT.md (D-02..D-10 — the locked content spot-check verifies against)
    - .planning/phases/03-skills/03-RESEARCH.md (Pattern 5 — 3-tier table form; Pattern 6 — ✓ Check rendering)
    - pages/03-skills/02-why-skills.md, pages/03-skills/03-how-skills-work.md, pages/03-skills/04-hands-on-build.md, pages/03-skills/05-hands-on-fire.md (the actual content to verify against)
  </read_first>
  <action>See `<how-to-verify>`. Presenter walks Chapter 3 and resumes on the `<resume-signal>`.</action>
  <verify><automated>echo "human-verify checkpoint — see how-to-verify section"</automated></verify>
  <done>Presenter types `approved` (or `approved-with-retrofit` if Section 04 needs Pattern G font-shrink) after the per-section walkthrough completes without failures.</done>
  <what-built>
A complete Chapter 3 wired via `slides.md → pages/03-skills.md → 5 section files`. Cover, why-skills (Phase 2 bridge + procedural-knowledge framing + 3 locked verbatim quotes), how-skills-work (file shape + SKILL.md + 3-tier table), hands-on build (mkdir + SKILL.md paste + softened hot-reload caveat), hands-on fire (two micro-steps + two ✓ Checks + closing line).
  </what-built>
  <how-to-verify>
1. Start the dev server in presenter mode:

   ```bash
   bunx slidev --open
   ```

   If port 3030 is occupied (Phase 1 gotcha), kill the stale process or use `bunx slidev --port 3031 --open`.

2. Navigate to Chapter 3's cover. Use `o` for overview, or arrow-key past Chapters 1 + 2.

3. Step through every Chapter 3 slide. Confirm:

   - **Section 01 — Cover:** centered layout. Title `Chapter 3 — Skills` with em-dash. Provocation visible. Dim subtitle `what skills are · how they're loaded · one skill you'll wire live` with middle-dot separators.

   - **Section 02 — Slide 1 (Phase 2 bridge):** H1 `Hooks and skills are opposite ends`. Bolded **you** / **Claude** contrast visible. Dim helper below reads `hook = you wire the "when" · skill = Claude decides the "when"`.

   - **Section 02 — Slide 2 (why this matters):** H1 `Why this matters`. Procedural-knowledge framing. `pr-review` example mentioned.

   - **Section 02 — Slide 3 (Quote 1):** H1 `What skills are, in essence`. Blockquote contains VERBATIM: `Skills are procedural knowledge — a reusable 'how we do XYZ here' — packaged in a format Claude can discover and load itself.`

   - **Section 02 — Slide 4 (Quote 2):** H1 `Why this scales`. Blockquote contains VERBATIM: `CLAUDE.md is always loaded. A skill is loaded when needed. That distinction is why a 50-skill library doesn't slow Claude down, but a 5,000-line CLAUDE.md does.`

   - **Section 02 — Slide 5 (Quote 3):** H1 `The self-invocation moment`. Blockquote contains VERBATIM: `You didn't type a slash command. You didn't remind Claude of the format. The skill activated itself.`

   - **Section 03 — Slide 1 (file shape):** H1 `Where skills live`. ```text``` block renders `~/.claude/skills/pirate-mode/` with `└── SKILL.md` indented. Dim note below mentions user-level AND project-level (`.claude/skills/`) — same shape.

   - **Section 03 — Slide 2 (SKILL.md):** H1 `The smallest valid SKILL.md`. Fenced ```markdown``` block highlights the YAML frontmatter cleanly (vitesse-dark + JetBrains Mono). Contains `name: pirate-mode`, the 3-line `description` value, `# Pirate Mode` H1, the brain-rot vocab list, and the closing signature line. Dim note below mentions `description` has two jobs.

   - **Section 03 — Slide 3 (3-tier table):** H1 `How Claude loads a skill`. Fenced markdown table renders with 3 rows: **Discovery** (`name` + `description` only, ~100 tokens / skill), **Activation** (full SKILL.md body, ~1K–5K tokens), **Execution** (Scripts, reference files, templates, only what the task needs). Dim trailing line names `allowed-tools`, `disable-model-invocation`, `paths`.

   - **Section 04 — Hands-on build:** H1 `Build the skill`. ```bash``` block: `mkdir -p ~/.claude/skills/pirate-mode`. ```markdown``` block: the SAME SKILL.md from Section 03 Slide 2 (byte-identical). Dim footnote: reconcile note `This is the same SKILL.md from the last slide — paste it as-is.` PLUS softened hot-reload caveat `Skills hot-reload — if it doesn't fire on the first try in the next slide, exit and restart claude.` **Visual overflow check:** the entire slide fits within the presenter viewport without scrolling/clipping. If it overflows → proceed to Task 4.

   - **Section 05 — Hands-on fire:** H1 `Fire it`. Two ```text``` prompt blocks. Two `> ✓ Check:` blockquotes with literal ✓ glyph. ✓ Check 1 text: `Claude's response opens with "Ahoy ye sigma" and ends with "— per pirate-mode." You never typed a slash command.` ✓ Check 2 text: `no "Ahoy," no "— per pirate-mode" — the skill didn't fire because nothing in your prompt matched its description.` Closing dim line: `Two prompts, one skill. Claude chose when to use it — that's the whole point.`

4. Open browser devtools Console. Confirm: no red errors during the Chapter 3 walkthrough. Warnings about HMR / Vue dev-mode are acceptable.

5. Confirm `global-top.vue` progress bar continues to advance.

6. Confirm Chapters 1 + 2 still render cleanly — navigate back to verify no regression.

7. Confirm tone discipline (QUAL-04): step through Chapter 3 once more and visually scan for any `!` in slide prose. Brain-rot vocabulary in the SKILL.md code fence is OK (demo payload, D-14 exception). Any `!` in slide prose → fails QUAL-04.

If ALL of the above pass with NO overflow on Section 04 → type `approved`. Skip Task 4.

If ALL pass EXCEPT Section 04 overflows → type `approved-with-retrofit`. Proceed to Task 4.

If ANY OTHER expectation fails, describe precisely (which section, which expectation, what rendered). Return to the offending plan — DO NOT patch here.
  </how-to-verify>
  <resume-signal>Type `approved`, `approved-with-retrofit`, or describe failed expectations.</resume-signal>
  <acceptance_criteria>
    - All 5 sections render in order without console errors
    - All 3 LOCKED QUOTES (Section 02 slides 3-5) render byte-exact (presenter visually verifies)
    - SKILL.md on Section 03 Slide 2 and Section 04 paste block are byte-identical
    - 3-tier table renders with all rows + trailing fields line
    - Section 05 two `> ✓ Check:` blockquotes both render with the literal ✓ glyph
    - No exclamation marks in slide prose (visual scan)
    - Chapters 1 + 2 still render cleanly (no regression)
    - Presenter resumes with `approved` or `approved-with-retrofit`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: (Conditional) Retrofit Section 04 with `<style scoped>` font-shrink if presenter saw overflow</name>
  <files>pages/03-skills/04-hands-on-build.md</files>
  <read_first>
    - pages/01-audience-setup/05-claude-files-primer.md (Phase 1 ANALOG — the `<style scoped>` block to mirror byte-exactly)
    - .planning/phases/03-skills/03-RESEARCH.md (Pattern 7 Option B — overflow fallback)
    - pages/03-skills/04-hands-on-build.md (CURRENT — verify no `<style>` block already present)
  </read_first>
  <action>
ONLY execute this task if Task 3 resumed with `approved-with-retrofit`. Otherwise skip.

Append the following block at the END of `pages/03-skills/04-hands-on-build.md` (after the closing `</div>` of the existing dim helper):

```html
<style scoped>
pre, code {
  font-size: 0.7rem !important;
  line-height: 1.15 !important;
}
.slidev-code {
  padding: 0.6em 0.9em !important;
}
h1 {
  margin-bottom: 0.4em !important;
}
</style>
```

This is byte-identical to the Phase 1 `05-claude-files-primer.md` font-shrink block.

After appending: re-run `bunx slidev build` (must still exit 0), then re-step through Section 04 in presenter mode to confirm fit.

Atomic commit: `style(phase-3): shrink section 04 SKILL.md to fit presenter viewport`.
  </action>
  <verify>
    <automated>if grep -q "<style scoped>" pages/03-skills/04-hands-on-build.md; then bunx slidev build && test -f dist/index.html && grep -q "font-size: 0.7rem" pages/03-skills/04-hands-on-build.md; else echo "skipped — no retrofit needed"; fi</automated>
  </verify>
  <acceptance_criteria>
    - IF Task 3 resumed with `approved-with-retrofit`: `pages/03-skills/04-hands-on-build.md` ends with the `<style scoped>` font-shrink block; `bunx slidev build` re-passes
    - IF Task 3 resumed with `approved`: this task is skipped; no file modified
    - No other Section 04 content is altered (mkdir block, SKILL.md fence, dim helper all unchanged)
  </acceptance_criteria>
  <done>Either no retrofit needed (approved), or retrofit applied and build re-verified.</done>
</task>

</tasks>

<verification>
- Dry-run validates A1 (pirate-mode auto-invocation) OR is gracefully skipped
- `bunx slidev build` exit 0
- `dist/index.html` exists
- Human presenter walkthrough passes the per-section checklist
- Chapters 1 + 2 still render clean (no regression)
- All 3 LOCKED QUOTES + SKILL.md + 3-tier table + 2 ✓ Checks verified byte-exact in presenter mode
- Optional Section 04 font-shrink retrofit applied if overflow seen
</verification>

<success_criteria>
QUAL-01 satisfied: Chapter 3 builds clean and renders clean in presenter mode. A1 confirmed (or graceful skip noted). Phase 3 of v1.1 is shippable; Phase 4 (Subagents + Agent SDK) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills/03-07-SUMMARY.md`.
</output>
