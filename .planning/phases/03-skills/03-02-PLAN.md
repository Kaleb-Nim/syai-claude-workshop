---
phase: 03-skills
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - pages/03-skills/01-cover.md
autonomous: true
requirements: [SKL-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "pages/03-skills/01-cover.md renders the Chapter 3 cover slide with `layout: center`"
    - "H1 reads `Chapter 3 — Skills` (em-dash U+2014)"
    - "Dim subtitle uses middle-dot ` · ` separators and `text-sm opacity-60 mt-8` classes"
    - "No exclamation marks in body prose (QUAL-04)"
  artifacts:
    - path: "pages/03-skills/01-cover.md"
      provides: "Chapter 3 cover slide"
      contains: "Chapter 3 — Skills"
  key_links:
    - from: "pages/03-skills.md"
      to: "pages/03-skills/01-cover.md"
      via: "src: include resolution"
      pattern: "src: ./03-skills/01-cover.md"
---

<objective>
Author the Chapter 3 cover slide. Single slide, `layout: center`, one tight provocation under the H1, dim breadcrumb subtitle. Mirrors `pages/02-hooks/01-cover.md` byte-for-byte in shape — only the words change.

The chapter sets up the model-invoked-vs-user-invoked wow framing that Section 02 unpacks. The cover should hint at "Claude decides when to load it" without spoiling Section 02's quote reveals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-skills/03-CONTEXT.md
@.planning/phases/03-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/03-skills/01-cover.md (chapter cover slide)</name>
  <files>pages/03-skills/01-cover.md</files>
  <read_first>
    - pages/02-hooks/01-cover.md (Phase 2 ANALOG — byte-exact template, 12 lines)
    - pages/01-audience-setup/01-cover.md (Phase 1 ANALOG — same shape, validates the pattern)
    - .planning/phases/03-skills/03-CONTEXT.md (Claude's Discretion — exact cover one-liner is planner choice; tone: confident, terse, no `!`)
    - .planning/phases/03-skills/03-RESEARCH.md (Pattern 2 — recommended chapter cover form including the suggested provocation wording)
  </read_first>
  <action>
Create the new directory `pages/03-skills/` (mkdir if not present) and the NEW file `pages/03-skills/01-cover.md` with EXACTLY these contents:

```markdown
---
layout: center
---

# Chapter 3 — Skills

procedural knowledge Claude discovers and loads itself — one tiny skill today, the pattern for the rest

<div class="text-sm opacity-60 mt-8">
  what skills are · how they're loaded · one skill you'll wire live
</div>
```

Notes on each element:
- Frontmatter is exactly `layout: center` (3 lines, no other keys) — mirrors Phase 2 `01-cover.md`.
- H1 uses em-dash ` — ` (U+2014) between "Chapter 3" and "Skills" (NOT a hyphen).
- Provocation line is lowercase prose, no exclamation marks, no period at the end (matches Phase 1 + Phase 2 cover style).
- Dim subtitle div uses class `text-sm opacity-60 mt-8` and middle-dot ` · ` (U+00B7) separators between the three breadcrumb items.
- File ends with newline after `</div>`.

Atomic commit (QUAL-02): `feat(phase-3): section 01 chapter cover`.
  </action>
  <verify>
    <automated>test -f pages/03-skills/01-cover.md && grep -q "^layout: center$" pages/03-skills/01-cover.md && grep -q "^# Chapter 3 — Skills$" pages/03-skills/01-cover.md && grep -q "opacity-60 mt-8" pages/03-skills/01-cover.md && grep -q "what skills are · how they're loaded · one skill you'll wire live" pages/03-skills/01-cover.md && [ "$(grep -c '!' pages/03-skills/01-cover.md)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-skills/01-cover.md` exists
    - Frontmatter contains `layout: center` and no other keys
    - H1 line is exactly `# Chapter 3 — Skills` (em-dash U+2014, not `-`)
    - Provocation line `procedural knowledge Claude discovers and loads itself — one tiny skill today, the pattern for the rest` appears verbatim
    - Dim subtitle div uses class `text-sm opacity-60 mt-8`
    - Subtitle contains middle-dot ` · ` separators (U+00B7), NOT hyphens
    - `grep -c "!" pages/03-skills/01-cover.md` returns `0` (QUAL-04 — no exclamation marks)
  </acceptance_criteria>
  <done>Chapter 3 cover slide renders centered with the spec title, provocation, and dim subtitle; committed atomically.</done>
</task>

</tasks>

<verification>
- File exists, frontmatter correct, H1 uses em-dash, subtitle uses middle-dot separators, zero `!` characters
- Atomic commit scoped `feat(phase-3): section 01 chapter cover` (QUAL-02)
</verification>

<success_criteria>
Section 01 cover slide ships; Wave 2 can continue on the other 4 section files in parallel.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills/03-02-SUMMARY.md`.
</output>
