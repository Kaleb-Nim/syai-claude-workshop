---
phase: 03-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slides.md
  - pages/03-skills.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "slides.md includes pages/03-skills.md via an appended src: block after the existing Phase 1 + Phase 2 includes"
    - "pages/03-skills.md exists as a pure src:-only chapter orchestrator with 5 includes (01-cover, 02-why-skills, 03-how-skills-work, 04-hands-on-build, 05-hands-on-fire)"
    - "D-01: Phase 3 produces section files for 5 content sections + 1 closing build plan (7 plans total — wiring is its own atomic plan, separate from cover authoring)"
    - "slides.md head matter (lines 1-18) is byte-identical to pre-plan state"
  artifacts:
    - path: "slides.md"
      provides: "Deck root orchestrator with Phase 3 include appended"
      contains: "src: ./pages/03-skills.md"
    - path: "pages/03-skills.md"
      provides: "Chapter 3 orchestrator — 5 src: includes, no inline content"
      contains: "src: ./03-skills/01-cover.md"
  key_links:
    - from: "slides.md"
      to: "pages/03-skills.md"
      via: "Slidev src: frontmatter"
      pattern: "src: ./pages/03-skills.md"
    - from: "pages/03-skills.md"
      to: "pages/03-skills/01-cover.md"
      via: "nested src: include (relative to pages/)"
      pattern: "src: ./03-skills/01-cover.md"
---

<objective>
Wire the Phase 3 chapter orchestrator into the deck. Two artifacts: (1) a single 3-line `src:` block appended to `slides.md` AFTER the existing Phase 1 + Phase 2 includes (no head-matter edits), and (2) the new `pages/03-skills.md` chapter orchestrator with five `src:` includes. Mirrors Phase 2 Plan 01-01 exactly minus the cover-slide task (cover authoring is split into Plan 03-02 for cleaner atomic commits — see comment in this plan's rationale).

Why split wiring from cover authoring (vs Phase 2's combined Plan 01-01): wiring is a distinct integration task (touches `slides.md` head-adjacent + creates an orchestrator file) versus authoring a content slide (chapter cover provocation). Two commits = cleaner blast-radius if either needs revert. Recommended over the merged shape.

Purpose: Unblock Wave 2 (Plans 03-02..03-06 author the 5 section files in parallel).
Output: 1 modified file + 1 new file. Each in its own atomic commit per QUAL-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-skills/03-CONTEXT.md
@.planning/phases/03-skills/03-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 3 include to slides.md (append-only, no head-matter edits)</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (CURRENT — lines 1-34 must be preserved; head matter lines 1-18, cover lines 20-26, Phase 1 include lines 28-30, Phase 2 include lines 31-33)
    - pages/02-hooks.md (ANALOG — chapter orchestrator with 5 src: includes)
    - .planning/phases/02-hooks/02-01-PLAN.md (ANALOG — Phase 2 Plan 01 Task 1, append-only pattern verbatim)
    - .planning/phases/03-skills/03-CONTEXT.md (D-01 — append AFTER Phase 1 + Phase 2 includes; no head-matter changes)
  </read_first>
  <action>
Append EXACTLY the following 3 lines at the END of `slides.md` (after the existing Phase 2 include block ending at line 33). Do NOT modify lines 1-33 — head matter, cover slide, Phase 1 include, and Phase 2 include all stay byte-identical.

Append this block verbatim:

```markdown
---
src: ./pages/03-skills.md
---
```

The expected final tail of `slides.md` (last 9 logical lines) will be:

```
---
src: ./pages/01-audience-setup.md
---
---
src: ./pages/02-hooks.md
---
---
src: ./pages/03-skills.md
---
```

Atomic commit (QUAL-02): `feat(phase-3): wire chapter 3 src include into slides.md`.
  </action>
  <verify>
    <automated>grep -c "src: \./pages/03-skills\.md" slides.md | grep -q "^1$" && grep -c "src: \./pages/02-hooks\.md" slides.md | grep -q "^1$" && grep -c "src: \./pages/01-audience-setup\.md" slides.md | grep -q "^1$" && head -n 18 slides.md | diff - <(git show HEAD:slides.md | head -n 18)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src: \./pages/03-skills\.md" slides.md` returns exactly `1`
    - `grep -c "src: \./pages/02-hooks\.md" slides.md` returns exactly `1` (Phase 2 include preserved)
    - `grep -c "src: \./pages/01-audience-setup\.md" slides.md` returns exactly `1` (Phase 1 include preserved)
    - `head -n 18 slides.md` is byte-identical to `git show HEAD:slides.md | head -n 18` (head matter unchanged)
    - No `vite.config.ts` was created (CLAUDE.md constraint)
    - Phase 3 include block appears AFTER the Phase 2 include block (verify via `grep -n` line ordering)
  </acceptance_criteria>
  <done>slides.md includes Phase 3 chapter via appended src: block; head matter and prior includes untouched; committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Create pages/03-skills.md chapter orchestrator (pure src: includes)</name>
  <files>pages/03-skills.md</files>
  <read_first>
    - pages/02-hooks.md (Phase 2 ANALOG — chapter orchestrator template, 16 lines, pure src: includes, paths relative to pages/)
    - pages/01-audience-setup.md (Phase 1 ANALOG — same pattern)
    - .planning/phases/03-skills/03-CONTEXT.md (D-01 — 5 section files in order)
    - .planning/phases/03-skills/03-RESEARCH.md (Pattern 1 — chapter orchestrator section)
  </read_first>
  <action>
Create the NEW file `pages/03-skills.md` with EXACTLY these contents (no deck head matter — first line is `---`, paths relative to `pages/`):

```markdown
---
src: ./03-skills/01-cover.md
---
---
src: ./03-skills/02-why-skills.md
---
---
src: ./03-skills/03-how-skills-work.md
---
---
src: ./03-skills/04-hands-on-build.md
---
---
src: ./03-skills/05-hands-on-fire.md
---
```

End the file with a trailing newline after the final `---` (mirrors Phase 2 file at `pages/02-hooks.md`).

Atomic commit (QUAL-02): `feat(phase-3): add chapter 3 orchestrator pages/03-skills.md`.
  </action>
  <verify>
    <automated>test -f pages/03-skills.md && grep -c "^src: \./03-skills/" pages/03-skills.md | grep -q "^5$" && head -n 1 pages/03-skills.md | grep -q "^---$"</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/03-skills.md` exists
    - `grep -c "^src: \./03-skills/" pages/03-skills.md` returns exactly `5`
    - First line is `---` (no deck head matter)
    - Each of the 5 src: paths uses `./03-skills/<file>.md` form (NOT `./pages/03-skills/...`)
    - `grep -c "src: \./03-skills/01-cover\.md" pages/03-skills.md` returns `1`
    - `grep -c "src: \./03-skills/02-why-skills\.md" pages/03-skills.md` returns `1`
    - `grep -c "src: \./03-skills/03-how-skills-work\.md" pages/03-skills.md` returns `1`
    - `grep -c "src: \./03-skills/04-hands-on-build\.md" pages/03-skills.md` returns `1`
    - `grep -c "src: \./03-skills/05-hands-on-fire\.md" pages/03-skills.md` returns `1`
  </acceptance_criteria>
  <done>Chapter 3 orchestrator exists with 5 includes in spec order; relative paths correct; committed atomically.</done>
</task>

</tasks>

<verification>
- `slides.md` head matter (lines 1-18) unchanged; appended `src: ./pages/03-skills.md` block exists once after the Phase 2 include
- `pages/03-skills.md` is a pure 5-include orchestrator with paths relative to `pages/`
- Two atomic commits scoped to slides.md / orchestrator (QUAL-02)
</verification>

<success_criteria>
Scaffolding ready for parallel Wave 2 section authoring. `bunx slidev build` would currently fail (the 5 referenced section files do not yet exist) — that's expected. Plan 03-07 runs the build only after Wave 2 lands all 5 section files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills/03-01-SUMMARY.md`.
</output>
