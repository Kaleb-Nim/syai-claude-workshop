---
phase: 01-audience-setup
plan: 07
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/06-win-claude-md.md
autonomous: true
requirements: [SET-01, SET-02, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 06 delivers Win 2 — participants add a single rule to ~/.claude/CLAUDE.md"
    - "Rule wording is exactly: When in plan mode, always ask clarifying questions before you start planning."
    - "Slide 2 explains why it sticks using RESEARCH-corrected wording (delivered as a message attached to your conversation, NOT 'system prompt')"
    - "Closing ✓ Check uses grep -c (NOT cat) and doubles as the SET-02 sync-check (CONTEXT D-13)"
  artifacts:
    - path: "pages/01-audience-setup/06-win-claude-md.md"
      provides: "Win 2 + chapter sync-check (SET-01 + SET-02)"
      contains: "When in plan mode, always ask clarifying questions before you start planning."
  key_links:
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/06-win-claude-md.md"
      via: "src include"
      pattern: "src: \\./01-audience-setup/06-win-claude-md\\.md"
---

<objective>
Author Section 06 — Win 2 + chapter sync-check. TWO slides in one file separated by an inline `---`. Slide 1 has participants add a single rule line to `~/.claude/CLAUDE.md`. Slide 2 explains why it sticks (loaded at the start of every session as a message attached to the conversation — NOT "system prompt"). The slide-2 ✓ Check uses `grep -c` to verify the rule is present, and this same check IS the SET-02 chapter sync-check per CONTEXT D-13.

This is a CONTEXT-D-06 WORDING OVERRIDE for the "why it sticks" framing: D-06 said "CLAUDE.md is loaded into every session's system prompt"; current Claude Code docs disclaim that explicitly (RESEARCH Pitfall 4). Use the RESEARCH-corrected wording.

Satisfies SET-01 (Win 2 of 2) and SET-02 (chapter sync-check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/06-win-claude-md.md (two-slide; RESEARCH wording overrides)</name>
  <files>pages/01-audience-setup/06-win-claude-md.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Pitfall 4 — "message attached to conversation, not system prompt"; Pitfall 5 — grep -c not cat; Pattern 2 two-slide example; "Inline rule line in a section file (Win 2)" code example)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`06-win-claude-md.md` section — two-slide pattern, verbatim)
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-06 verbatim rule wording; D-13 SET-02 fold-in; D-17 tone)
    - .planning/REQUIREMENTS.md (SET-01, SET-02)
  </read_first>
  <action>
Write `pages/01-audience-setup/06-win-claude-md.md` with EXACTLY the following content. TWO slides separated by an inline `---` slide divider on its own line. No frontmatter on either slide (default layout for both).

Slide 1 contains the verbatim rule line from CONTEXT D-06, fenced as ` ```text ` (it is prose, not a shell command — RESEARCH "Inline rule line" code example). Slide 2 explains why the rule sticks using RESEARCH Pitfall 4 corrected wording, and ends with a markdown-blockquote ✓ Check using `grep -c` (RESEARCH Pitfall 5).

The Slide 2 ✓ Check is ALSO the SET-02 chapter sync-check (CONTEXT D-13) — there is no separate sync-check slide.

Verbatim file contents:

````markdown
# Win 2: Make Claude listen across sessions

Add this one line to `~/.claude/CLAUDE.md`:

```text
When in plan mode, always ask clarifying questions before you start planning.
```

Save and close.

---

# Why it sticks

Claude Code loads `~/.claude/CLAUDE.md` at the start of every session —
as a message attached to your conversation, not as enforced config.
Behavioral rules in there land in front of every prompt you send,
with no per-session ceremony.

> ✓ Check: `grep -c 'When in plan mode' ~/.claude/CLAUDE.md` returns `1`.
````

Rules (LOAD-BEARING):
- The exact rule line `When in plan mode, always ask clarifying questions before you start planning.` MUST appear inside a ` ```text ` fenced block (NOT ` ```bash `).
- The phrase `system prompt` MUST NOT appear anywhere in this file (RESEARCH Pitfall 4 — D-06 wording correction).
- The phrase `message attached to your conversation` MUST appear in Slide 2 body.
- The ✓ Check uses `grep -c 'When in plan mode' ~/.claude/CLAUDE.md` and expects `1` — NOT `cat` (RESEARCH Pitfall 5).
- Exactly TWO H1 headings in the file (one per slide).
- Exactly ONE `^---$` line in the file (the inter-slide divider; no opening frontmatter on either slide).
- No exclamation marks in body prose.
- No `<Cmd>`, no `<img>`, no `<screenshot>`.
- Each fenced block ≤ 25 lines (the only fenced block contains 1 line of content).
  </action>
  <verify>
    <automated>bash -c 'F=pages/01-audience-setup/06-win-claude-md.md; test -f "$F" && grep -qx "# Win 2: Make Claude listen across sessions" "$F" && grep -qx "# Why it sticks" "$F" && grep -qx "When in plan mode, always ask clarifying questions before you start planning." "$F" && grep -qx "\`\`\`text" "$F" && ! grep -qE "system prompt" "$F" && grep -qE "message attached to your conversation" "$F" && grep -qE "^> ✓ Check: .grep -c .When in plan mode" "$F" && ! grep -qE "^> .*cat ~/\.claude/CLAUDE\.md" "$F" && [ "$(grep -c "^# " "$F")" = "2" ] && [ "$(grep -c "^---$" "$F")" = "1" ] && ! grep -q "!" "$F" && ! grep -qE "<img|<Cmd|<screenshot" "$F"'</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/01-audience-setup/06-win-claude-md.md` exists
    - Exact H1 `# Win 2: Make Claude listen across sessions` present
    - Exact H1 `# Why it sticks` present
    - Exact line `When in plan mode, always ask clarifying questions before you start planning.` present
    - File contains a ` ```text ` opening fence on its own line
    - `grep -E "system prompt" …` returns no match (Pitfall 4 — D-06 wording override)
    - `grep -E "message attached to your conversation" …` returns a match (Pitfall 4 corrected wording)
    - ✓ Check line present: `> ✓ Check: \`grep -c 'When in plan mode' ~/.claude/CLAUDE.md\` returns \`1\`.`
    - `grep -E "^> .*cat ~/\.claude/CLAUDE\.md" …` returns no match (Pitfall 5 — use grep -c, not cat)
    - `grep -c "^# " …` returns `2` (one H1 per slide)
    - `grep -c "^---$" …` returns `1` (one slide divider; no frontmatter)
    - `grep -c "!" …` returns `0` (QUAL-04 — no ASCII exclamation marks)
    - `grep -cE "<img|<Cmd|<screenshot" …` returns `0`
    - File line count ≤ 22
  </acceptance_criteria>
  <done>Section 06 file delivers Win 2 with verbatim rule wording and the SET-02 sync-check folded into Slide 2's ✓ Check.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit for section 06</name>
  <files>pages/01-audience-setup/06-win-claude-md.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03)
  </read_first>
  <action>
```bash
git add pages/01-audience-setup/06-win-claude-md.md
git commit -m "feat(phase-1): section 06 win-claude-md"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 06 win-claude-md" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/06-win-claude-md.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 06 win-claude-md`
    - Commit changes exactly that one file
  </acceptance_criteria>
  <done>Section 06 committed atomically.</done>
</task>

</tasks>

<verification>
- Rule line present inside `text` fence
- `system prompt` absent (Pitfall 4)
- `message attached to your conversation` present
- ✓ Check uses `grep -c`, not `cat` (Pitfall 5)
- Two H1s, one inter-slide `---`
- No exclamation marks
- One atomic commit
</verification>

<success_criteria>
SET-01 Win 2 delivered: participant's `~/.claude/CLAUDE.md` now contains the plan-mode rule. SET-02 satisfied: the slide-2 ✓ Check IS the chapter sync-check; no separate sync slide is required.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-07-SUMMARY.md`.
</output>
