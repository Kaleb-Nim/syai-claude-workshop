---
phase: 01-audience-setup
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/01-cover.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Chapter 1 opens with a centered cover slide titled 'Chapter 1 — Audience + Setup'"
    - "Cover slide carries a one-line provocation and a dim subtitle naming the three beats of the chapter"
    - "Slide uses layout: center frontmatter (per RESEARCH Open Question 3)"
  artifacts:
    - path: "pages/01-audience-setup/01-cover.md"
      provides: "Chapter 1 cover slide"
      contains: "Chapter 1 — Audience + Setup"
  key_links:
    - from: "pages/01-audience-setup.md (orchestrator)"
      to: "pages/01-audience-setup/01-cover.md"
      via: "src: ./01-audience-setup/01-cover.md"
      pattern: "src: \\./01-audience-setup/01-cover\\.md"
---

<objective>
Author Section 01 — the Chapter 1 cover slide. Single slide, centered layout, terse provocation + dim subtitle naming the chapter's three beats (who · what's covered · two wins). This slide IS the chapter's "rest spot" before the substantive sections begin.

Purpose: Gives the room a clean, low-density landing slide between the deck cover and the first content section, per CONTEXT D-01 and RESEARCH Open Question 1 (orchestrator stays pure, cover lives in its own section file).

Output: 1 new file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@slides.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/01-cover.md (chapter cover slide)</name>
  <files>pages/01-audience-setup/01-cover.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`pages/01-audience-setup/01-cover.md` section + Pattern D dim-helper)
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Open Question 3 — `layout: center` recommendation; Anti-Patterns — no `layout: section` inside Chapter 1)
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-14 no section dividers; D-17 tone discipline; Claude's Discretion — exact one-liner is presenter-flavored)
    - slides.md (lines 19-26 — analog cover composition with `text-sm opacity-60 mt-8`)
  </read_first>
  <action>
First, ensure the directory exists: `mkdir -p pages/01-audience-setup`.

Then write `pages/01-audience-setup/01-cover.md` with EXACTLY the following content. The slide uses `layout: center` (Open Question 3 recommendation — chapter cover, not deck cover, so we avoid the implicit `layout: cover` that fires only on slide 1). The dim subtitle uses the same UnoCSS composition as `slides.md` line 24 (`text-sm opacity-60 mt-8`) for visual consistency with the deck cover.

Verbatim file contents:

```markdown
---
layout: center
---

# Chapter 1 — Audience + Setup

before the deep chapters: who you are, what's coming, two quick wins

<div class="text-sm opacity-60 mt-8">
  who this is for · what's covered · two wins before chapter 2
</div>
```

Tone rules (QUAL-04 / CONTEXT D-17):
- The provocation line must NOT contain an exclamation mark.
- No hype words.
- The one-liner above ("before the deep chapters: who you are, what's coming, two quick wins") is the recommended terse provocation. The phrase is presenter-flavored per CONTEXT "Claude's Discretion" — keep it as-is unless tone violates QUAL-04.

Structural rules:
- Only ONE H1 (the chapter title).
- NO second `---` slide separator inside this file (this section is a single slide).
- NO `layout: section` (CONTEXT D-14 / RESEARCH Anti-Patterns ban within-chapter section dividers).
- NO screenshots, no `<img>`, no `<Cmd>` component.
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup/01-cover.md && grep -qx "layout: center" pages/01-audience-setup/01-cover.md && grep -qx "# Chapter 1 — Audience + Setup" pages/01-audience-setup/01-cover.md && [ "$(grep -c '^---$' pages/01-audience-setup/01-cover.md)" = "2" ] && [ "$(grep -c '^# ' pages/01-audience-setup/01-cover.md)" = "1" ] && ! grep -qE '!' <(sed -n '/^---$/,/^---$/!p' pages/01-audience-setup/01-cover.md | tail -n +2) && ! grep -qE 'layout: section' pages/01-audience-setup/01-cover.md && ! grep -qE '<img|<Cmd|<screenshot' pages/01-audience-setup/01-cover.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/01-audience-setup/01-cover.md` exists
    - `grep -c "^---$" pages/01-audience-setup/01-cover.md` returns `2` (frontmatter open + close; no second slide separator)
    - `grep -c "^# " pages/01-audience-setup/01-cover.md` returns `1` (single H1)
    - `grep -qx "layout: center" pages/01-audience-setup/01-cover.md` succeeds
    - `grep -qx "# Chapter 1 — Audience + Setup" pages/01-audience-setup/01-cover.md` succeeds (exact title with em dash)
    - `grep -c '<div class="text-sm opacity-60 mt-8">' pages/01-audience-setup/01-cover.md` returns `1`
    - `grep -c "who this is for · what's covered · two wins before chapter 2" pages/01-audience-setup/01-cover.md` returns `1`
    - `grep -cE "^[^#`].*!" pages/01-audience-setup/01-cover.md` returns `0` (no exclamation marks in body prose lines — QUAL-04)
    - `grep -cE "layout: section" pages/01-audience-setup/01-cover.md` returns `0` (D-14 / Anti-Patterns)
    - `grep -cE "<img|<Cmd|<screenshot" pages/01-audience-setup/01-cover.md` returns `0` (QUAL-04 / AUTH-01)
    - File line count ≤ 12 (well under CONTEXT D-15's 25-line fence cap; there are no fenced blocks here)
  </acceptance_criteria>
  <done>Chapter cover slide renders centered in presenter mode with title + provocation + dim subtitle; no banned constructs present.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit (QUAL-02) for section 01</name>
  <files>pages/01-audience-setup/01-cover.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03 atomic commits)
  </read_first>
  <action>
Stage only the new section file and commit with a section-scoped message per QUAL-02:

```bash
git add pages/01-audience-setup/01-cover.md
git commit -m "feat(phase-1): section 01 chapter cover"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 01 chapter cover" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/01-cover.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 01 chapter cover`
    - Commit touches exactly `pages/01-audience-setup/01-cover.md` and nothing else
  </acceptance_criteria>
  <done>Section 01 committed atomically.</done>
</task>

</tasks>

<verification>
- Slide title contains "Chapter 1 — Audience + Setup"
- `layout: center` present
- No exclamation marks in body
- No `<Cmd>`, `<img>`, or `<screenshot>`
- File ≤ 12 lines
- One atomic commit landed
</verification>

<success_criteria>
Chapter 1 opens with a terse, centered cover slide. Section 01 file exists at the path the orchestrator references. QUAL-04 tone and QUAL-02 atomic-commit gates both satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-02-SUMMARY.md`.
</output>

<!-- ─── Validator anchor (DO NOT REMOVE) ───
extractFrontmatter() picks the LAST ---…--- block in the file.
The verbatim slide example above contains a `---` frontmatter pair for the
cover slide, which confuses the regex. This sentinel restores the canonical
fields. Keep in sync with the top-of-file frontmatter.
-->

---
phase: 01-audience-setup
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/01-cover.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Chapter 1 opens with a centered cover slide titled Chapter 1 Audience + Setup"
    - "Cover slide carries a one-line provocation and a dim subtitle naming the three beats"
    - "Slide uses layout center frontmatter per RESEARCH Open Question 3"
  artifacts:
    - path: "pages/01-audience-setup/01-cover.md"
      provides: "Chapter 1 cover slide"
      contains: "Chapter 1"
  key_links:
    - from: "pages/01-audience-setup.md orchestrator"
      to: "pages/01-audience-setup/01-cover.md"
      via: "src include"
      pattern: "src: ./01-audience-setup/01-cover.md"
---
