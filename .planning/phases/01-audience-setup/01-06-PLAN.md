---
phase: 01-audience-setup
plan: 06
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/05-win-statusline.md
autonomous: true
requirements: [SET-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 05 delivers Win 1 — running /statusline inside Claude Code"
    - "Slide teaches the natural-language /statusline flow (NOT a preset picker; RESEARCH Pitfall 1)"
    - "Slide shows two fenced bash blocks: the NL invocation and the ✓ Wrote / ✓ Updated confirmation lines"
    - "Closing ✓ Check is an observation-based blockquote, not a hard-coded output string"
  artifacts:
    - path: "pages/01-audience-setup/05-win-statusline.md"
      provides: "Win 1 — /statusline demo (SET-01)"
      contains: "/statusline show model, repo, and context percentage as a progress bar"
  key_links:
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/05-win-statusline.md"
      via: "src include"
      pattern: "src: \\./01-audience-setup/05-win-statusline\\.md"
---

<objective>
Author Section 05 — Win 1. Inside Claude Code, the participant types `/statusline <natural-language prompt>` and Claude generates a statusline script + wires it into settings. Slide shows the NL invocation, the confirmation lines, and an observation-based ✓ Check (a status bar is now visible at the bottom of Claude Code).

This is a CONTEXT-D-05 OVERRIDE: the original D-05 framing as a "preset picker" is WRONG in current Claude Code (RESEARCH Pitfall 1). Section 05 must teach the natural-language flow only.

Satisfies SET-01 (Win 1 of 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
@style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/05-win-statusline.md (NL flow — Pitfall 1 override)</name>
  <files>pages/01-audience-setup/05-win-statusline.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Pitfall 1 — /statusline is NL-flow, NOT preset picker; "Section 05 Win 1 demo block (re-shaped)" code example; Assumption A2 on confirmation lines)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`05-win-statusline.md` section — whole-file pattern; Pattern C ✓ Check blockquote)
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-05 ORIGINAL — for awareness; do NOT use; D-07 win order; D-08 terminal blocks no screenshots; D-15 ≤25-line fence cap; D-17 tone)
    - .planning/REQUIREMENTS.md (SET-01)
    - style.css (confirm fenced-block panel + rust left-rule contract — no per-slide CSS edits needed)
  </read_first>
  <action>
Write `pages/01-audience-setup/05-win-statusline.md` with EXACTLY the following content. Single slide. Two fenced ` ```bash ` blocks separated by a one-line prose sentence; a markdown blockquote ✓ Check closes the slide.

CRITICAL CONTENT-ACCURACY OVERRIDE: This slide must NOT mention "preset", "picker", "preset list", or named preset values. The `/statusline` flow is natural-language only (RESEARCH Pitfall 1 verbatim quote: "The `/statusline` command accepts natural language instructions describing what you want displayed. Claude Code generates a script file in `~/.claude/` and updates your settings automatically.")

Verbatim file contents:

````markdown
# Win 1: Custom statusline in 30 seconds

Inside Claude Code, type:

```bash
> /statusline show model, repo, and context percentage as a progress bar
```

Claude generates a script in `~/.claude/statusline.sh` and wires it into `~/.claude/settings.json`. You'll see:

```bash
✓ Wrote ~/.claude/statusline.sh
✓ Updated ~/.claude/settings.json
```

> ✓ Check: a status bar now sits at the bottom of your Claude Code window —
> showing the model, your folder, and a context-usage bar. Exact bar style
> depends on your model + session.
````

Rules:
- No frontmatter.
- Exactly one H1.
- Exactly two ` ```bash ` fenced blocks (4 ` ``` ` lines total).
- No occurrence of the strings: `preset`, `picker` (case-insensitive).
- The invocation block contains the exact line `> /statusline show model, repo, and context percentage as a progress bar`.
- The confirmation block contains both lines: `✓ Wrote ~/.claude/statusline.sh` and `✓ Updated ~/.claude/settings.json`.
- ✓ Check is a markdown blockquote (lines starting with `> `) using PATTERNS Pattern C wrapped variant.
- No exclamation marks anywhere in body prose or blockquote.
- No `<Cmd>`, no `<img>`, no `<screenshot>`.
- Each fenced block is ≤ 25 lines (each is ≤ 3 lines here).
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup/05-win-statusline.md && grep -qx "# Win 1: Custom statusline in 30 seconds" pages/01-audience-setup/05-win-statusline.md && grep -qx "> /statusline show model, repo, and context percentage as a progress bar" pages/01-audience-setup/05-win-statusline.md && grep -qx "✓ Wrote ~/.claude/statusline.sh" pages/01-audience-setup/05-win-statusline.md && grep -qx "✓ Updated ~/.claude/settings.json" pages/01-audience-setup/05-win-statusline.md && [ "$(grep -c '^```bash$' pages/01-audience-setup/05-win-statusline.md)" = "2" ] && [ "$(grep -c '^```$' pages/01-audience-setup/05-win-statusline.md)" = "2" ] && ! grep -qiE 'preset|picker' pages/01-audience-setup/05-win-statusline.md && ! grep -q '!' pages/01-audience-setup/05-win-statusline.md && ! grep -qE '<img|<Cmd|<screenshot' pages/01-audience-setup/05-win-statusline.md && grep -qE '^> ✓ Check:' pages/01-audience-setup/05-win-statusline.md && [ "$(grep -c '^# ' pages/01-audience-setup/05-win-statusline.md)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - Exact H1 `# Win 1: Custom statusline in 30 seconds`
    - Exactly two ` ```bash ` openers and two ` ``` ` closers
    - Invocation line verbatim: `> /statusline show model, repo, and context percentage as a progress bar`
    - Confirmation line verbatim: `✓ Wrote ~/.claude/statusline.sh`
    - Confirmation line verbatim: `✓ Updated ~/.claude/settings.json`
    - `grep -iE "preset|picker" …` returns no match (Pitfall 1)
    - `grep -c "!" …` returns `0` (QUAL-04 — note: ✓ is U+2713 not `!`, so this check is genuinely about ASCII `!`)
    - `grep -qE "^> ✓ Check:" …` succeeds (markdown blockquote ✓ Check)
    - `grep -c "^# " …` returns `1`
    - `grep -cE "<img|<Cmd|<screenshot" …` returns `0`
    - File line count ≤ 20
  </acceptance_criteria>
  <done>Section 05 teaches the NL /statusline flow with deterministic confirmation lines and an observation-based ✓ Check.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit for section 05</name>
  <files>pages/01-audience-setup/05-win-statusline.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03)
  </read_first>
  <action>
```bash
git add pages/01-audience-setup/05-win-statusline.md
git commit -m "feat(phase-1): section 05 win-statusline"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 05 win-statusline" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/05-win-statusline.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 05 win-statusline`
    - Commit changes exactly that one file
  </acceptance_criteria>
  <done>Section 05 committed atomically.</done>
</task>

</tasks>

<verification>
- Zero occurrences of "preset" or "picker"
- Exactly 2 fenced bash blocks
- Confirmation lines present verbatim
- ✓ Check is a markdown blockquote
- No exclamation marks
- One atomic commit
</verification>

<success_criteria>
SET-01 Win 1 delivered: participant has a custom statusline running at the bottom of their Claude Code window. The slide accurately describes current Claude Code behavior (NL flow, not picker).
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-06-SUMMARY.md`.
</output>
