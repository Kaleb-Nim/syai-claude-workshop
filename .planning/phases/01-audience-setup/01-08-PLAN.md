---
phase: 01-audience-setup
plan: 08
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07]
files_modified: []
autonomous: false
requirements: [QUAL-01]
must_haves:
  truths:
    - "`bunx slidev build` exits with code 0 with all 6 section files wired"
    - "Build artifact `dist/index.html` exists after the build"
    - "Presenter mode renders Chapter 1 from cover through Slide 2 of Section 06 without console errors (human-verified)"
  artifacts:
    - path: "dist/index.html"
      provides: "Static SPA build output (build success indicator)"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "bunx slidev build"
      to: "dist/index.html"
      via: "Slidev → Vite SPA pipeline"
      pattern: "bunx slidev build"
---

<objective>
Close Chapter 1 with the QUAL-01 build + presenter spot-check gate. Run `bunx slidev build` (must exit 0 and produce `dist/index.html`), then have the presenter step through Chapter 1 in presenter mode to confirm the chapter renders clean — cover slide centered, all six sections in order, no broken `src:` includes, no console errors, two ✓ Check callouts legible.

This plan modifies NO source files. It exists to gate the chapter close per CONTEXT D-01 (Plan 07/8 → "closing build plan"), REQUIREMENTS QUAL-01, and ROADMAP "Cross-cutting Quality".

Because the spot-check is human-driven, this plan is `autonomous: false` — it ends with a `checkpoint:human-verify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bunx slidev build and confirm exit 0 + dist/index.html</name>
  <files></files>
  <read_first>
    - .planning/REQUIREMENTS.md (QUAL-01 wording)
    - .planning/phases/01-audience-setup/01-RESEARCH.md ("Environment Availability" table — Slidev 52.15.1 installed; "Standard Stack" — Shiki vitesse-dark; "Code Examples" — no `vite.config.ts` allowed)
    - CLAUDE.md (Bun-only — `bunx`, NEVER `npx`)
  </read_first>
  <action>
From the repo root, run the production build using Bun's bundled runner:

```bash
bunx slidev build
```

This must:
1. Exit with status `0`.
2. Produce `dist/index.html`.
3. Emit no Slidev / Vite / Shiki ERROR-level messages (warnings are tolerable; errors are not).

Common failure signatures and what they mean (diagnose, then return for fix — do NOT mask):
- `Imported markdown file not found: ./pages/01-audience-setup/...` → orchestrator `src:` path is wrong-rooted (Pitfall 3 regression — Plan 01-01 issue).
- Missing file under `pages/01-audience-setup/0N-*.md` → corresponding section plan failed; do not patch around it.
- Shiki lang error for ` ```text ` → unlikely (Shiki accepts `text` by default), but if seen, confirm `slides.md` headmatter `shiki.langs` was not edited (Pitfall 6 regression).
- `vite.config.ts` warning → if a `vite.config.ts` was accidentally introduced, delete it (CLAUDE.md constraint, Slidev issue #2043).

Do NOT modify any source file from this plan to make the build pass. Build failures here indicate an earlier section plan has a defect; surface it as a blocker and return to the offending plan.
  </action>
  <verify>
    <automated>bunx slidev build && test -f dist/index.html && grep -q "<!DOCTYPE html>" dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `bunx slidev build` exits with status `0`
    - File `dist/index.html` exists after the build
    - `grep -q "<!DOCTYPE html>" dist/index.html` succeeds
    - Build output contains no `ERROR` lines (warnings OK)
    - No `vite.config.ts` was created or modified by this plan
  </acceptance_criteria>
  <done>Build passes; static SPA artifact exists at `dist/index.html`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Presenter spot-check Chapter 1 (human-verify)</name>
  <files>(read-only — no source files modified)</files>
  <action>See `<how-to-verify>` below. The executor (or presenter) runs the steps and resumes on the `<resume-signal>`.</action>
  <verify><automated>echo "human-verify checkpoint — see how-to-verify section"</automated></verify>
  <done>Presenter types `approved` after the per-section walkthrough completes without failures.</done>
  <what-built>
A complete Chapter 1 wired into the deck via `slides.md → pages/01-audience-setup.md → 6 section files`. Cover, audience checklist, what's-covered enumeration, project-local file tree, two wins (`/statusline` + `CLAUDE.md` rule), and the SET-02 sync-check folded into the final ✓ Check.
  </what-built>
  <how-to-verify>
1. Start the dev server in presenter mode:

   ```bash
   bunx slidev --open
   ```

2. Navigate to Chapter 1's cover slide using the slide menu (`o` key for overview, click Chapter 1 cover, OR arrow-key through from the deck cover).

3. Step forward through every Chapter 1 slide using the right-arrow key. Confirm:
   - **Cover (Section 01):** centered layout. Title `Chapter 1 — Audience + Setup`. One-line provocation visible. Dim subtitle reads `who this is for · what's covered · two wins before chapter 2`.
   - **Section 02:** H1 `Who this is for`. Two bolded sub-headers (`We assume you can:` and `We do NOT assume you've:`). 4 + 3 bullets present. `NOT` is capitalized in the second sub-header.
   - **Section 03:** H1 `What's covered`. Four numbered chapter names bolded in order: Hooks → Skills → Subagents → Agent SDK. Dim line `in that order` visible below.
   - **Section 04:** H1 `Where Claude reads from in your project`. File-tree renders as a single fenced block. Tree does NOT contain `projects/`. `commands/` is footnoted as legacy. Closing line `We'll teach the bold 3` reads with three inline-coded items.
   - **Section 05:** H1 `Win 1: Custom statusline in 30 seconds`. Two fenced bash blocks visible — invocation + confirmation. `✓ Wrote ~/.claude/statusline.sh` and `✓ Updated ~/.claude/settings.json` both present. ✓ Check blockquote at the bottom is legible against the dark palette. NO mention of "preset" or "picker" anywhere.
   - **Section 06 Slide 1:** H1 `Win 2: Make Claude listen across sessions`. Verbatim rule line `When in plan mode, always ask clarifying questions before you start planning.` rendered inside a styled text fence.
   - **Section 06 Slide 2:** H1 `Why it sticks`. Body explains rule loading WITHOUT using the words "system prompt". ✓ Check blockquote uses `grep -c 'When in plan mode' ~/.claude/CLAUDE.md` and shows the expected `1`.

4. Open browser devtools Console. Confirm: no red errors during the Chapter 1 walkthrough. (Warnings about HMR / Vue dev-mode are acceptable.)

5. Confirm `global-top.vue` progress bar (top of viewport) advances as you step through.

6. (Optional) Render-pacing for Section 04: if the file tree feels visually crowded, note "wrap tree in `<v-clicks>`" as a follow-up — do NOT modify the file now; that is a separate retrofit per RESEARCH Open Question 2.

If ALL of the above pass, type `approved` to close the chapter.

If ANY fails, describe the issue precisely (which section, which expectation, what was rendered). Return to the offending plan for revision — DO NOT patch in this plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe failed expectations.</resume-signal>
</task>

</tasks>

<verification>
- `bunx slidev build` exit 0
- `dist/index.html` exists
- Human presenter walkthrough passes the per-section checklist
</verification>

<success_criteria>
QUAL-01 satisfied: Chapter 1 builds clean and renders clean in presenter mode. Phase 1 of v1.1 is shippable; Phase 2 (Hooks) can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-08-SUMMARY.md`.
</output>
