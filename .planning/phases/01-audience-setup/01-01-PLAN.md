---
phase: 01-audience-setup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - slides.md
  - pages/01-audience-setup.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "slides.md still renders its locked headmatter and cover slide untouched"
    - "slides.md ends with a frontmatter-only slide-block that src-includes pages/01-audience-setup.md"
    - "pages/01-audience-setup.md exists as a pure src-include orchestrator pointing at the 6 section files relative to pages/"
  artifacts:
    - path: "slides.md"
      provides: "Root deck orchestrator + Phase 1 chapter include"
      contains: "src: ./pages/01-audience-setup.md"
    - path: "pages/01-audience-setup.md"
      provides: "Chapter 1 orchestrator (6 src: includes)"
      contains: "src: ./01-audience-setup/01-cover.md"
  key_links:
    - from: "slides.md"
      to: "pages/01-audience-setup.md"
      via: "Slidev src: frontmatter (resolved by @slidev/parser)"
      pattern: "src: \\./pages/01-audience-setup\\.md"
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/0N-*.md"
      via: "nested src: includes resolved relative to pages/"
      pattern: "src: \\./01-audience-setup/0\\d-.*\\.md"
---

<objective>
Wire Chapter 1 into the deck by (a) appending a single `src:` include slide-block to `slides.md` and (b) creating a pure-include chapter orchestrator at `pages/01-audience-setup.md` that references the 6 section files Plans 02–07 will write.

Purpose: Establishes the orchestrator chain `slides.md → pages/01-audience-setup.md → pages/01-audience-setup/0N-*.md` BEFORE any section file exists, so downstream section plans can run in parallel under Wave 1 without modifying shared files.

Output: 1 modified file (`slides.md`, append-only), 1 new file (`pages/01-audience-setup.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@slides.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md

<interfaces>
<!-- Slidev `src:` semantics (verified in RESEARCH from @slidev/parser/dist/fs.mjs) -->
<!-- A slide whose frontmatter contains `src:` has its content fully replaced by the imported file. -->
<!-- Relative `src:` paths resolve from `dirname(slide.filepath)`, NOT from the project root. -->
<!--   - Inside `slides.md` (repo root) → `src: ./pages/01-audience-setup.md` is correct. -->
<!--   - Inside `pages/01-audience-setup.md` → `src: ./01-audience-setup/0N-*.md` (relative to `pages/`). -->
<!-- Imported files may contain multiple slides separated by their own `---` blocks. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Chapter 1 src include to slides.md (no headmatter edit)</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (verify lines 1-26 byte-identical to PATTERNS Pattern A excerpt)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (Pattern A and "Append pattern — the only edit" section)
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Pitfall 6 — headmatter preservation)
  </read_first>
  <action>
Append EXACTLY the following 3 lines to the END of `slides.md` (after the existing closing `</div>` on the current final content line). No other edit to the file. Do not touch lines 1-26. Do not collapse the blank line that precedes the appended block — Slidev requires a blank line before a frontmatter-only slide-block.

Append-block (verbatim — note this is a frontmatter-only slide whose body is empty because the imported file fully replaces it):

```
---
src: ./pages/01-audience-setup.md
---
```

After the edit, the final file must contain the substring `src: ./pages/01-audience-setup.md` exactly once, and the first 26 lines must remain byte-identical to the pre-edit state.
  </action>
  <verify>
    <automated>grep -c "^src: \./pages/01-audience-setup\.md$" slides.md | grep -qx 1 && head -n 26 slides.md | diff -q - <(git show HEAD:slides.md | head -n 26)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^src: \./pages/01-audience-setup\.md$" slides.md` returns `1`
    - `head -n 26 slides.md` is byte-identical to the pre-edit version (verified via `git diff slides.md` showing ONLY additions after line 26)
    - `git diff slides.md | grep -E "^-[^-]"` returns no lines (no deletions; append-only)
    - `grep -c "^colorSchema: dark$" slides.md` still returns `1` (headmatter intact)
    - `grep -c "^highlighter: shiki$" slides.md` still returns `1` (headmatter intact)
  </acceptance_criteria>
  <done>slides.md ends with the new `src: ./pages/01-audience-setup.md` slide-block; headmatter and cover-slide untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create chapter orchestrator pages/01-audience-setup.md (pure src includes)</name>
  <files>pages/01-audience-setup.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`pages/01-audience-setup.md` section — "Whole-file pattern (pure includes, no inline content)")
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Pattern 1 — Chapter Orchestrator File; Pitfall 3 — path resolution; Open Question 1 recommendation: Option (a))
  </read_first>
  <action>
First, create the `pages/` directory if it does not exist (`mkdir -p pages`).

Then create `pages/01-audience-setup.md` with EXACTLY the following content. The file has NO leading deck-level headmatter (deck headmatter lives only in `slides.md`). The first `---` opens the first slide-block. Paths are relative to `pages/` per Slidev parser semantics (RESEARCH Pitfall 3) — they MUST be `./01-audience-setup/0N-*.md`, NOT `./pages/01-audience-setup/0N-*.md`.

Verbatim file contents (13 lines, ending with a trailing newline):

```
---
src: ./01-audience-setup/01-cover.md
---
---
src: ./01-audience-setup/02-who-this-is-for.md
---
---
src: ./01-audience-setup/03-whats-covered.md
---
---
src: ./01-audience-setup/04-claude-files-primer.md
---
---
src: ./01-audience-setup/05-win-statusline.md
---
---
src: ./01-audience-setup/06-win-claude-md.md
---
```

Do NOT include any other content — no headings, no prose, no headmatter block. The orchestrator is purely structural (Open Question 1 Option (a)).
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup.md && [ "$(grep -c '^src: \./01-audience-setup/0[1-6]-' pages/01-audience-setup.md)" = "6" ] && ! grep -q '^# ' pages/01-audience-setup.md && ! grep -qE '^src: \./pages/' pages/01-audience-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - File `pages/01-audience-setup.md` exists
    - `grep -c "^src: \./01-audience-setup/0[1-6]-" pages/01-audience-setup.md` returns `6`
    - `grep -c "^src: \./01-audience-setup/01-cover\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -c "^src: \./01-audience-setup/02-who-this-is-for\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -c "^src: \./01-audience-setup/03-whats-covered\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -c "^src: \./01-audience-setup/04-claude-files-primer\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -c "^src: \./01-audience-setup/05-win-statusline\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -c "^src: \./01-audience-setup/06-win-claude-md\.md$" pages/01-audience-setup.md` returns `1`
    - `grep -cE "^src: \./pages/" pages/01-audience-setup.md` returns `0` (no wrong-rooted paths — Pitfall 3)
    - `grep -cE "^# " pages/01-audience-setup.md` returns `0` (no inline headings; pure orchestrator)
    - File line count is in [12, 14] (13 lines including trailing newline)
  </acceptance_criteria>
  <done>Chapter orchestrator file exists with exactly 6 `src:` includes, relative paths correctly anchored under `pages/`, no inline content.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic per-section commit (QUAL-02) for orchestrator wiring</name>
  <files>slides.md, pages/01-audience-setup.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03 atomic commits, QUAL-02)
    - .planning/REQUIREMENTS.md (QUAL-02 wording)
  </read_first>
  <action>
Stage ONLY the two files this plan touched and commit with a section-scoped message matching the QUAL-02 pattern.

```bash
git add slides.md pages/01-audience-setup.md
git commit -m "feat(phase-1): section 00 wire chapter orchestrator"
```

Do NOT stage any other file. Do NOT amend. Do NOT push.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qE "^feat\(phase-1\): section 00 wire chapter orchestrator$" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup.md\nslides.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly matches `feat(phase-1): section 00 wire chapter orchestrator`
    - Latest commit changes EXACTLY the two files `slides.md` and `pages/01-audience-setup.md` (no more, no fewer)
    - Working tree clean for these two paths (`git status --porcelain slides.md pages/01-audience-setup.md` returns empty)
  </acceptance_criteria>
  <done>Wave 0 wiring committed atomically; Wave 1 section plans can now proceed in parallel.</done>
</task>

</tasks>

<verification>
- `grep -c "^src: \./pages/01-audience-setup\.md$" slides.md` returns `1`
- `pages/01-audience-setup.md` contains exactly 6 `src:` lines pointing at section files
- No path inside the chapter orchestrator starts with `./pages/` (Pitfall 3 guard)
- `slides.md` lines 1-26 untouched
- One commit landed: `feat(phase-1): section 00 wire chapter orchestrator`
- `bunx slidev build` is NOT run in this plan — Plan 07 owns build verification (avoids failing on the yet-unwritten section files)
</verification>

<success_criteria>
The orchestrator chain `slides.md → pages/01-audience-setup.md → (6 placeholder section paths)` exists in source. Downstream parallel plans 02–07 can each create exactly one file under `pages/01-audience-setup/` without touching shared files. Build verification is deferred to Plan 08.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-01-SUMMARY.md`.
</output>

<!-- ─── Validator anchor (DO NOT REMOVE) ───
extractFrontmatter() picks the LAST ---…--- block in the file.
Action bodies above contain literal Slidev `---` separators inside verbatim
content examples. This duplicate frontmatter block ensures schema validation
sees the canonical fields last. Keep in sync with the top-of-file frontmatter.
-->

---
phase: 01-audience-setup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - slides.md
  - pages/01-audience-setup.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "slides.md still renders its locked headmatter and cover slide untouched"
    - "slides.md ends with a frontmatter-only slide-block that src-includes pages/01-audience-setup.md"
    - "pages/01-audience-setup.md exists as a pure src-include orchestrator pointing at the 6 section files relative to pages/"
  artifacts:
    - path: "slides.md"
      provides: "Root deck orchestrator + Phase 1 chapter include"
      contains: "src: ./pages/01-audience-setup.md"
    - path: "pages/01-audience-setup.md"
      provides: "Chapter 1 orchestrator (6 src: includes)"
      contains: "src: ./01-audience-setup/01-cover.md"
  key_links:
    - from: "slides.md"
      to: "pages/01-audience-setup.md"
      via: "Slidev src frontmatter resolved by slidev parser"
      pattern: "src: ./pages/01-audience-setup.md"
    - from: "pages/01-audience-setup.md"
      to: "section files"
      via: "nested src includes"
      pattern: "src: ./01-audience-setup/"
---
