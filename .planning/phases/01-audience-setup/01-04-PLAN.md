---
phase: 01-audience-setup
plan: 04
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/03-whats-covered.md
autonomous: true
requirements: [AUD-02, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 03 enumerates the four upcoming chapters in order: Hooks, Skills, Subagents, Agent SDK (CONTEXT D-10)"
    - "Closing line reads 'in that order' rendered as a dim helper (D-10)"
    - "No exclusions list (CONTEXT D-11)"
  artifacts:
    - path: "pages/01-audience-setup/03-whats-covered.md"
      provides: "What's-covered enumeration — AUD-02"
      contains: "Agent SDK"
  key_links:
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/03-whats-covered.md"
      via: "src include"
      pattern: "src: \\./01-audience-setup/03-whats-covered\\.md"
---

<objective>
Author Section 03 — what's-covered enumeration. Single slide. Numbered list of the 4 upcoming chapters in order, followed by an "in that order" dim helper. No exclusions list (presenter handles questions out-of-band per CONTEXT D-11).

Satisfies AUD-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
@uno.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/03-whats-covered.md</name>
  <files>pages/01-audience-setup/03-whats-covered.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-10 enumeration verbatim; D-11 no exclusions list; D-17 tone)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`03-whats-covered.md` section + Pattern D dim-helper)
    - uno.config.ts (confirm `text-dim` shortcut exists at line 24)
  </read_first>
  <action>
Write `pages/01-audience-setup/03-whats-covered.md` with EXACTLY the following content. Single slide, no frontmatter. Enumeration order and wording are locked by CONTEXT D-10 — `1. Hooks`, `2. Skills`, `3. Subagents`, `4. Agent SDK`. The closing "in that order" uses the project's `text-dim` UnoCSS shortcut (PATTERNS Pattern D form 2).

Verbatim file contents:

```markdown
# What's covered

1. **Hooks**
2. **Skills**
3. **Subagents**
4. **Agent SDK**

<div class="text-dim mt-6">in that order</div>
```

Rules:
- No frontmatter.
- Exactly one H1.
- Exactly four numbered items, each with the chapter name bolded.
- NO bullet/sub-bullet describing what's excluded (CONTEXT D-11 — clean enumeration only).
- NO mention of "slash commands", "plugins", or "MCP" anywhere in this file.
- No exclamation marks.
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup/03-whats-covered.md && grep -qx "# What's covered" pages/01-audience-setup/03-whats-covered.md && grep -qx "1. **Hooks**" pages/01-audience-setup/03-whats-covered.md && grep -qx "2. **Skills**" pages/01-audience-setup/03-whats-covered.md && grep -qx "3. **Subagents**" pages/01-audience-setup/03-whats-covered.md && grep -qx "4. **Agent SDK**" pages/01-audience-setup/03-whats-covered.md && grep -qx '<div class="text-dim mt-6">in that order</div>' pages/01-audience-setup/03-whats-covered.md && ! grep -qiE 'slash command|plugin|mcp' pages/01-audience-setup/03-whats-covered.md && ! grep -q '!' pages/01-audience-setup/03-whats-covered.md && [ "$(grep -c '^# ' pages/01-audience-setup/03-whats-covered.md)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - Exact H1 `# What's covered` present
    - All four numbered items present verbatim with bold chapter names
    - Dim helper line present verbatim: `<div class="text-dim mt-6">in that order</div>`
    - `grep -iE "slash command|plugin|mcp" …` returns no matches (D-11 — no exclusions list)
    - `grep -c "!" …` returns `0`
    - `grep -c "^# " …` returns `1`
    - File line count ≤ 10
  </acceptance_criteria>
  <done>Section 03 enumerates the 4 chapters in order with the dim closing line; no exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit for section 03</name>
  <files>pages/01-audience-setup/03-whats-covered.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03)
  </read_first>
  <action>
```bash
git add pages/01-audience-setup/03-whats-covered.md
git commit -m "feat(phase-1): section 03 whats-covered"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 03 whats-covered" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/03-whats-covered.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 03 whats-covered`
    - Commit changes exactly that one file
  </acceptance_criteria>
  <done>Section 03 committed atomically.</done>
</task>

</tasks>

<verification>
- 4 numbered chapter names in correct order
- "in that order" dim line present
- No "slash command" / "plugin" / "mcp" tokens
- No exclamation marks
- One atomic commit
</verification>

<success_criteria>
AUD-02 covered: a reader of Section 03 knows exactly which four chapters are coming and in what order.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-04-SUMMARY.md`.
</output>
