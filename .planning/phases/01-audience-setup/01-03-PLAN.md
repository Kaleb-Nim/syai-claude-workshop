---
phase: 01-audience-setup
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/02-who-this-is-for.md
autonomous: true
requirements: [AUD-01, QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 02 establishes the target participant via an assumed-skills checklist (AUD-01)"
    - "Both halves are present: 'We assume you can:' and 'We do NOT assume you've:' (CONTEXT D-09)"
    - "The 'do NOT assume' half carries three bullets exactly matching CONTEXT D-09"
  artifacts:
    - path: "pages/01-audience-setup/02-who-this-is-for.md"
      provides: "Assumed-skills checklist slide — AUD-01"
      contains: "We do NOT assume you've"
  key_links:
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/02-who-this-is-for.md"
      via: "src include"
      pattern: "src: \\./01-audience-setup/02-who-this-is-for\\.md"
---

<objective>
Author Section 02 — the audience-framing slide. Single slide. Two-bullet-group checklist: what we assume the participant can already do, and what we do NOT assume. The "do NOT" group is doing the real work — it tells the audience the workshop's actual delta.

Satisfies AUD-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/02-who-this-is-for.md</name>
  <files>pages/01-audience-setup/02-who-this-is-for.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-09 — both bullet groups verbatim; D-14 no section dividers; D-17 tone)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`02-who-this-is-for.md` section — whole-file pattern)
    - .planning/REQUIREMENTS.md (AUD-01 wording)
  </read_first>
  <action>
Write `pages/01-audience-setup/02-who-this-is-for.md` with EXACTLY the following content. Single slide, no frontmatter (default layout). The bullets are verbatim from CONTEXT D-09 — preserve wording, ordering, and capitalization.

Verbatim file contents:

```markdown
# Who this is for

**We assume you can:**

- use git from the terminal
- read a JSON config
- follow a chat-LLM session
- have installed Claude Code (and run it at least once)

**We do NOT assume you've:**

- wired a hook
- written a skill
- spawned a subagent
```

Rules:
- No frontmatter (default layout per PATTERNS Pattern A).
- Exactly one H1.
- No second `---` slide separator.
- The phrase `**We do NOT assume you've:**` must appear verbatim (capitalization on `NOT` is load-bearing — it's the slide's hook).
- No exclamation marks anywhere in this file (QUAL-04).
- No `<Cmd>`, no `<img>`, no `<screenshot>`.
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup/02-who-this-is-for.md && grep -qx "# Who this is for" pages/01-audience-setup/02-who-this-is-for.md && grep -qx "**We assume you can:**" pages/01-audience-setup/02-who-this-is-for.md && grep -qx "**We do NOT assume you've:**" pages/01-audience-setup/02-who-this-is-for.md && [ "$(grep -c '^- ' pages/01-audience-setup/02-who-this-is-for.md)" = "7" ] && [ "$(grep -c '^---$' pages/01-audience-setup/02-who-this-is-for.md)" = "0" ] && [ "$(grep -c '^# ' pages/01-audience-setup/02-who-this-is-for.md)" = "1" ] && ! grep -q '!' pages/01-audience-setup/02-who-this-is-for.md && ! grep -qE '<img|<Cmd|<screenshot' pages/01-audience-setup/02-who-this-is-for.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `pages/01-audience-setup/02-who-this-is-for.md`
    - `grep -qx "# Who this is for" …` succeeds (exact H1)
    - `grep -qx "**We assume you can:**" …` succeeds
    - `grep -qx "**We do NOT assume you've:**" …` succeeds (capital `NOT`)
    - `grep -c "^- " …` returns `7` (4 "assume" bullets + 3 "do NOT assume" bullets)
    - `grep -qx "- use git from the terminal" …` succeeds
    - `grep -qx "- read a JSON config" …` succeeds
    - `grep -qx "- follow a chat-LLM session" …` succeeds
    - `grep -qx "- have installed Claude Code (and run it at least once)" …` succeeds
    - `grep -qx "- wired a hook" …` succeeds
    - `grep -qx "- written a skill" …` succeeds
    - `grep -qx "- spawned a subagent" …` succeeds
    - `grep -c "^---$" …` returns `0` (no frontmatter, no slide separator — single-slide, no-frontmatter file)
    - `grep -c "^# " …` returns `1`
    - `grep -c "!" …` returns `0` (QUAL-04 — no exclamation marks anywhere)
    - `grep -cE "<img|<Cmd|<screenshot" …` returns `0`
  </acceptance_criteria>
  <done>Section 02 file exists with both bullet groups verbatim; tone gates satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit for section 02</name>
  <files>pages/01-audience-setup/02-who-this-is-for.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03)
  </read_first>
  <action>
```bash
git add pages/01-audience-setup/02-who-this-is-for.md
git commit -m "feat(phase-1): section 02 who-this-is-for"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 02 who-this-is-for" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/02-who-this-is-for.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 02 who-this-is-for`
    - Commit changes exactly that one file
  </acceptance_criteria>
  <done>Section 02 committed atomically.</done>
</task>

</tasks>

<verification>
- Both bullet groups present with verbatim D-09 wording
- 7 bullets total
- Zero exclamation marks
- One atomic commit landed
</verification>

<success_criteria>
AUD-01 covered: a reader of Section 02 knows who the workshop is for and — more importantly — what skill delta the workshop will close.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-03-SUMMARY.md`.
</output>
