---
phase: 01-audience-setup
plan: 05
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - pages/01-audience-setup/04-claude-files-primer.md
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - "Section 04 shows the project-local ./.claude/ layout with three taught items bolded in the closing line"
    - "The tree DOES NOT include 'projects/' (RESEARCH Pitfall 2 — projects/ lives at ~/.claude/, not in project-local)"
    - "commands/ is present but footnoted as legacy/superseded by Skills (RESEARCH State of the Art)"
    - "Closing line reads 'We'll teach the bold 3' with settings.json, skills/, agents/ bolded"
  artifacts:
    - path: "pages/01-audience-setup/04-claude-files-primer.md"
      provides: "Project-local ./.claude/ file-tree primer"
      contains: "./.claude/"
  key_links:
    - from: "pages/01-audience-setup.md"
      to: "pages/01-audience-setup/04-claude-files-primer.md"
      via: "src include"
      pattern: "src: \\./01-audience-setup/04-claude-files-primer\\.md"
---

<objective>
Author Section 04 — the project-local Claude file-tree primer. Single slide. A `text`-fenced tree of `./.claude/` showing all canonical project-local items, with the three items taught in later chapters (`settings.json`, `skills/`, `agents/`) bolded in the closing line. Tree uses the RESEARCH-CORRECTED contents (drops `projects/`, footnotes `commands/` and `rules/`).

This is NOT a CONTEXT-D-12-verbatim slide — CONTEXT D-12's tree contained two errors that RESEARCH Pitfall 2 and State of the Art corrected. The corrected tree is the source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-audience-setup/01-CONTEXT.md
@.planning/phases/01-audience-setup/01-RESEARCH.md
@.planning/phases/01-audience-setup/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pages/01-audience-setup/04-claude-files-primer.md (RESEARCH-corrected tree)</name>
  <files>pages/01-audience-setup/04-claude-files-primer.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-RESEARCH.md (Pitfall 2 — drop `projects/`; State of the Art — `commands/` legacy footnote; corrected canonical tree)
    - .planning/phases/01-audience-setup/01-PATTERNS.md (`04-claude-files-primer.md` section — body pattern with corrected tree)
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-12 ORIGINAL tree — for awareness; do NOT use this verbatim; D-16 `<v-clicks>` is optional and DEFAULT skip per Open Question 2)
  </read_first>
  <action>
Write `pages/01-audience-setup/04-claude-files-primer.md` with EXACTLY the following content. Single slide, no frontmatter. The tree uses ` ```text ` (NOT ` ```bash `) — it's prose-shaped, not a command. Block stays well under CONTEXT D-15's 25-line cap.

Per RESEARCH Open Question 2: do NOT wrap the tree in `<v-clicks>` in the first draft — Plan 08's spot-check decides whether pacing demands a retrofit.

Verbatim file contents:

````markdown
# Where Claude reads from in your project

```text
./CLAUDE.md
./.claude/
  CLAUDE.md              (alternative location for project CLAUDE.md)
  settings.json          → Hooks (Chapter 2)
  settings.local.json    (gitignored; personal overrides)
  skills/                → Skills (Chapter 3)
  agents/                → Subagents (Chapter 4)
  commands/              (legacy — superseded by Skills)
  rules/                 (path-scoped; not today)
```

We'll teach the **bold 3**: `settings.json`, `skills/`, `agents/`.
````

Rules:
- The tree MUST NOT contain a `projects/` line (RESEARCH Pitfall 2 — `projects/` is `~/.claude/`, not `./.claude/`).
- The tree MUST contain `commands/` with the footnote `(legacy — superseded by Skills)` (RESEARCH State of the Art).
- Use ` ```text ` for the tree fence.
- The closing line MUST contain backticks around `settings.json`, `skills/`, and `agents/` (inline code), and the words `bold 3` must be wrapped in `**`.
- No exclamation marks.
- No `<v-clicks>` in this initial draft.
- Block stays ≤ 25 lines (the tree fence is 10 lines including fences).
- No `<Cmd>`, `<img>`, or `<screenshot>`.
  </action>
  <verify>
    <automated>test -f pages/01-audience-setup/04-claude-files-primer.md && grep -qx "# Where Claude reads from in your project" pages/01-audience-setup/04-claude-files-primer.md && grep -q '^```text$' pages/01-audience-setup/04-claude-files-primer.md && ! grep -qE '^\s*projects/' pages/01-audience-setup/04-claude-files-primer.md && grep -qE '^\s*commands/\s+\(legacy — superseded by Skills\)$' pages/01-audience-setup/04-claude-files-primer.md && grep -qE 'settings\.json\s+→ Hooks' pages/01-audience-setup/04-claude-files-primer.md && grep -qE 'skills/\s+→ Skills' pages/01-audience-setup/04-claude-files-primer.md && grep -qE 'agents/\s+→ Subagents' pages/01-audience-setup/04-claude-files-primer.md && grep -qF "We'll teach the **bold 3**: \`settings.json\`, \`skills/\`, \`agents/\`." pages/01-audience-setup/04-claude-files-primer.md && ! grep -q '!' pages/01-audience-setup/04-claude-files-primer.md && ! grep -q '<v-clicks>' pages/01-audience-setup/04-claude-files-primer.md && [ "$(awk '/^```/{c++} END{print c}' pages/01-audience-setup/04-claude-files-primer.md)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists
    - H1 is exactly `# Where Claude reads from in your project`
    - Exactly one ` ```text ` fence pair (2 ` ``` ` lines total in the file)
    - Tree contains line matching `commands/              (legacy — superseded by Skills)`
    - Tree contains NO line matching `^\s*projects/` (RESEARCH Pitfall 2)
    - Tree includes `settings.json          → Hooks (Chapter 2)`
    - Tree includes `settings.local.json    (gitignored; personal overrides)`
    - Tree includes `skills/                → Skills (Chapter 3)`
    - Tree includes `agents/                → Subagents (Chapter 4)`
    - Tree includes `rules/                 (path-scoped; not today)`
    - Closing line `We'll teach the **bold 3**: \`settings.json\`, \`skills/\`, \`agents/\`.` present verbatim
    - `grep -c "!" …` returns `0` (QUAL-04)
    - `grep -q "<v-clicks>" …` does NOT match (deferred to Plan 08 if needed)
    - `grep -cE "<img|<Cmd|<screenshot" …` returns `0`
    - Fenced block is ≤ 25 lines (CONTEXT D-15)
  </acceptance_criteria>
  <done>Section 04 file-tree primer renders the RESEARCH-corrected canonical project-local layout; the three taught items are highlighted.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit for section 04</name>
  <files>pages/01-audience-setup/04-claude-files-primer.md</files>
  <read_first>
    - .planning/phases/01-audience-setup/01-CONTEXT.md (D-03)
  </read_first>
  <action>
```bash
git add pages/01-audience-setup/04-claude-files-primer.md
git commit -m "feat(phase-1): section 04 claude-files-primer"
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -qx "feat(phase-1): section 04 claude-files-primer" && git diff HEAD~1 HEAD --name-only | sort | diff -q - <(printf "pages/01-audience-setup/04-claude-files-primer.md\n")</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject exactly `feat(phase-1): section 04 claude-files-primer`
    - Commit changes exactly that one file
  </acceptance_criteria>
  <done>Section 04 committed atomically.</done>
</task>

</tasks>

<verification>
- No `projects/` in tree (RESEARCH Pitfall 2 override of CONTEXT D-12)
- `commands/` footnoted as legacy
- Three taught items bolded/inline-coded in closing line
- Fenced block uses `text`, ≤ 25 lines
- No exclamation marks, no banned components
- One atomic commit
</verification>

<success_criteria>
A reader of Section 04 sees the accurate project-local `./.claude/` layout and knows which three items the workshop will fill in.
</success_criteria>

<output>
After completion, create `.planning/phases/01-audience-setup/01-05-SUMMARY.md`.
</output>
