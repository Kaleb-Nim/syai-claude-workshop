---
phase: 260511-e98-top-progress-bar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/GlobalTop.vue
autonomous: true
requirements:
  - QUICK-E98-TOP-PROGRESS
must_haves:
  truths:
    - "A slim accent-colored bar appears at the very top edge of every slide"
    - "The bar's width fills proportionally as the viewer advances through slides"
    - "On the final slide, the bar spans 100% of the viewport width"
    - "On the first slide, the bar is empty (0% width) or shows a minimal seed"
    - "The bar does NOT appear inside presenter chrome / sidebar thumbnails"
  artifacts:
    - path: "components/GlobalTop.vue"
      provides: "Slidev auto-injected global-top component rendering the progress bar"
      contains: "useNav"
  key_links:
    - from: "components/GlobalTop.vue"
      to: "@slidev/client useNav"
      via: "currentPage / total reactive refs"
      pattern: "useNav\\(\\)"
    - from: "components/GlobalTop.vue"
      to: "var(--accent) rust token from style.css"
      via: "CSS variable inherited via :root"
      pattern: "var\\(--accent\\)"
---

<objective>
Add a slim, top-of-screen progress bar to the Slidev deck that fills with the rust accent (`#cc785c`) as the viewer advances through slides. Bar lives at the top edge of the slide viewport on every slide; honors Phase 2 visual identity and D-14/D-15 scoping rules (must not appear inside presenter chrome).

Purpose: Give live participants and post-workshop self-paced readers an at-a-glance sense of position in the deck without disrupting the minimal aesthetic. Supplements (does not replace) Slidev's built-in bottom progress bar.

Output: One new file — `components/GlobalTop.vue` — auto-discovered by Slidev's `global-top` slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@style.css
@slides.md

<interfaces>
<!-- Slidev exposes a `global-top` component slot: any Vue SFC named `GlobalTop.vue`
     (or `global-top.vue`) placed under `components/` is auto-mounted ABOVE every
     slide on every layout, but is NOT rendered inside presenter sidebar thumbnails.
     This is the idiomatic Slidev mechanism for persistent slide-overlay UI. -->

From @slidev/client:
```ts
// useNav() returns reactive navigation state for the current deck.
export function useNav(): {
  currentPage: ComputedRef<number>  // 1-indexed current slide number
  total: ComputedRef<number>        // total slide count (excludes hidden)
  // ...other nav helpers not needed for this plan
}
```

Phase 2 design tokens (from style.css :root, already global):
```css
--accent:    #CC785C;  /* rust — use for the progress fill */
--bg-main:   #1A1714;  /* slide background — fallback track color */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GlobalTop.vue with useNav-driven progress bar</name>
  <files>components/GlobalTop.vue</files>
  <behavior>
    - Component mounts at top of every slide via Slidev's `global-top` auto-injection convention
    - Width of inner fill = (currentPage / total) * 100%, reactive
    - On slide 1 of N: width is 1/N (small seed visible) — acceptable
    - On final slide: width is 100%
    - Bar is 3px tall, fixed to top: 0, left: 0, full viewport width
    - Fill color: var(--accent) — rust #cc785c
    - Track color: transparent (so the slide background shows through) — keeps minimal aesthetic
    - Width transitions smoothly (CSS transition, ~300ms ease-out) when slide changes
    - z-index high enough to sit above slide content (e.g., 100)
    - Does NOT appear inside .slidev-presenter sidebar (Slidev's global-top convention handles this)
  </behavior>
  <action>
    Create `components/GlobalTop.vue` as a single-file Vue 3 SFC using `<script setup lang="ts">`.

    Implementation:
    1. Import `useNav` from `@slidev/client` and `computed` from `vue`.
    2. Destructure `currentPage` and `total` from `useNav()`.
    3. Compute `progress = computed(() => (total.value > 0 ? (currentPage.value / total.value) * 100 : 0))`.
    4. Render a fixed-position track `<div class="gsd-progress-top">` containing an inner fill `<div class="gsd-progress-top__fill" :style="{ width: progress + '%' }">`.
    5. Use scoped `<style scoped>` to define:
       - `.gsd-progress-top` — position: fixed; top: 0; left: 0; right: 0; height: 3px; z-index: 100; pointer-events: none; background: transparent;
       - `.gsd-progress-top__fill` — height: 100%; background-color: var(--accent); transition: width 300ms ease-out; will-change: width;
    6. No `<template>`-level wrapping inside `.slidev-layout` is needed — Slidev's `global-top` slot already excludes presenter chrome per Slidev's component conventions, satisfying D-14/D-15 without explicit scoping. Document this in a comment at the top of the file referencing D-14/D-15.

    Do NOT:
    - Compute slide index manually from the router — use `useNav()` (the canonical Slidev API).
    - Hardcode `#cc785c` — reference `var(--accent)` so it stays in sync with style.css.
    - Use `border-radius` — Phase 2 D-05 prefers sharp corners.
    - Add the bar to individual slides via markdown — the `global-top` convention is the whole point.

    Filename note: Slidev accepts both `GlobalTop.vue` and `global-top.vue` for the auto-injection slot. Use `GlobalTop.vue` (PascalCase) to match Vue convention and Slidev docs.
  </action>
  <verify>
    <automated>
      cd /Users/kalebnim/Documents/GitHub/syai-claude-workshop && \
      test -f components/GlobalTop.vue && \
      grep -q 'useNav' components/GlobalTop.vue && \
      grep -q 'var(--accent)' components/GlobalTop.vue && \
      grep -q 'position: fixed' components/GlobalTop.vue && \
      grep -q 'top: 0' components/GlobalTop.vue && \
      bunx slidev build --out dist-progress-check 2>&1 | tail -20 && \
      grep -rq 'gsd-progress-top' dist-progress-check/assets/ && \
      rm -rf dist-progress-check && \
      echo "PASS: GlobalTop.vue created, build succeeded, component compiled into bundle"
    </automated>
  </verify>
  <done>
    - File `components/GlobalTop.vue` exists with `useNav()` import and `var(--accent)` reference
    - `bunx slidev build` completes without error
    - The compiled `gsd-progress-top` class appears in the built bundle (proves Slidev auto-discovered and included the component)
    - Manual sanity (human, dev server already running on :3030): refresh browser, observe slim rust bar at top; press Right/Space to advance; bar grows; advance to last slide; bar reaches right edge. No bar visible in presenter view sidebar thumbnails (press `o` for overview to spot-check).
  </done>
</task>

</tasks>

<verification>
- Build verification: `bunx slidev build` succeeds and emits the component's CSS class into the dist bundle.
- Visual verification (human, optional — dev server live): bar appears at top of every slide, fills as you advance, matches rust accent.
- Scoping verification (human, optional): open Slidev overview (`o` key) — bar should NOT appear inside each thumbnail tile's chrome (Slidev's `global-top` convention excludes it). Open presenter mode (`p`) — bar appears in main slide pane only.
</verification>

<success_criteria>
- New file `components/GlobalTop.vue` exists and compiles cleanly via `bunx slidev build`
- Component uses `useNav()` from `@slidev/client` (not manual route inspection)
- Fill color references `var(--accent)` (no inline hex)
- Built bundle contains the component's CSS class (proves auto-injection wiring)
- No existing files modified — content under `pages/`, `slides.md`, `style.css`, `uno.config.ts` untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260511-e98-top-progress-bar/260511-e98-SUMMARY.md` summarizing:
- File added (components/GlobalTop.vue)
- Slidev convention used (global-top auto-injection)
- Build verification result
- Any deviations or follow-ups (e.g., if user later wants to remove the built-in bottom bar)
</output>
