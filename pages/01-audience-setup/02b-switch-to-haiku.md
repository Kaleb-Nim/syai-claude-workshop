# On the $10 workshop credit? Switch to Haiku

<div class="text-dim text-sm mb-6">
default is Sonnet — great, but burns the $10 credit fast. Haiku gives you ~10× more runway for today's exercises.
</div>

<div class="text-sm opacity-70 mb-2">1 · inside the <code>claude</code> session, run</div>

```text
/models
```

<div class="text-sm opacity-70 mt-4 mb-2">2 · pick <b>Haiku</b> from the list</div>

```text
Select Model
  1. Default (recommended)
  2. Sonnet
❯ 3. Haiku
  4. Opus
```

<div class="text-sm opacity-70 mt-4 mb-2">3 · confirm — the statusline now shows <code>haiku</code></div>

<div class="text-dim text-xs mt-6">
using your own paid plan or API key with budget? skip this — stay on the default.
</div>
