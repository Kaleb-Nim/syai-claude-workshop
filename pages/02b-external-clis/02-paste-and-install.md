# Connecting Playwright service to Claude Code via CLI

Paste this into Claude Code:

```
https://github.com/microsoft/playwright-cli

install this globally for me
```

<div class="text-dim text-sm mt-4">
  Claude will fetch the README, figure out the install command, and run it. No need to read the docs yourself — that's the point.
</div>
